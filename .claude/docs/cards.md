# Cards — the fv3 spec, chips, refs & cutouts
ONE CARD SPEC (cqw container), the +List chip/tag, canva cutouts, inventory refs (jref/refOf law), demo maps.
> **Ported from the vault 2026-08-16** — the page is now `index.html` at the repo root (was `redesign/k-home-dual.html`), asset paths below are repointed, anything marked "(vault)" (lab pages, the card-art batch, the labs.md catalogue) did NOT ride over and lives in the vault repo, and every git hash cited below is VAULT history.
> Carved verbatim out of the old monolithic CLAUDE.md on 2026-08-12 (full backup on Dylan's Desktop + in git history). Wording is preserved exactly — section headers were added for navigation only. New decisions on this surface get appended HERE, not to CLAUDE.md. Cross-references like “see X below” may now point at a sibling doc in this folder — check CLAUDE.md's index.

## Chip + tag, card images & refs, ONE CARD SPEC

**CHIP LAYER + TAG (same port) — SUPERSEDED 2026-08-05: the TAG and the "+ List"/"✓ Lisst" labels are RETIRED. The chip is now the bare DECK MARK at rest; the dress wipes a want-word in beside it (rest text is "", decodeInto("") is the instant clear); decked = "✓" beside the mark; the mark's shadow flips right-at-rest / left-on-dress (two-lights law, comments at the chip CSS). **ON THE PHONE THE CHIP MIMICS THE ID TAG (2026-08-08 evening, Dylan: "the least visually pleasing part of our card"):** dock-gated CSS costume, no markup or JS — the box takes fc-ref's exact padding, the mark shrinks 18px→10px-at-ref and moves LEFT via flex `order` (desktop keeps word-left/mark-right), the want-word min-width collapses (phones never decode want-words; `.ft:empty` drops out of flex), and a white "+" lives as `.fl::after` at the visual right — content exists only under the gate, so no desktop surface renders it; DECKED cards suppress it via `:has(.ft:not(:empty))` on the ✓ the JS already writes (the text IS the state — rides free on every render path incl. flights). Dressed choreography (his spec: "the fill grabs the deck icon once its passed it and pulls in the add button with the fill"): the tset fill wipes LEFT→RIGHT (origin flipped — tset is dock-gated by construction so that IS the mobile rule), the mark + its shadow flip at ~.3s (the edge crossing), the + rides the fill's own .44s/.22s clock to its seat; the softout drain slides it back out on the .93 clock. The passage below stands for the FOUR-STATE-MIRROR LAW and history.** Original: the fv3 +List chip everywhere — `TAGSVG` (price-tag glyph, `pathLength="1"` BAKED IN THE MARKUP so every render path is normalized with zero post-render JS: grids, belts, bay shelves, buildFly flights, the static lb chip) rides `.fc-list` (now inline-flex, gap 5px/2.1552cqw; space reserved at rest — the chip box never resizes). Draw-in on card hover (`idraw`, now a k keyframe); layer rung = `.d7f .fc-list:hover .ft/.tagg { color: var(--acc) }` placed AFTER the hover-white rules (wins the tie by order). The tag is mirrored across ALL FOUR state systems — `:hover`, `.tset` (+.22s delays), `.hold` (VERBATIM hover mirror — the mid-flight blink law), `.bay-fly.held` (negative delay `-.3s` renders it pre-drawn) — any future chip-state addition must touch all four. `jmeta` now carries `acc` and the `.d7f` root wears inline `--acc` (bay shelves and flights render outside the accented sections; the lb sets its own). **Card images + refs (2026-07-21): the `CANVA` map near the top of the JS points each site board at its Canva cutout (`site/img/cards/canva/cl<N>/hs<N>-top|bottom.png`), and card refs are derived from that map (`INVREF`) — the Canva sheet numbering is the canonical inventory ID, so refs are non-contiguous on purpose (classic-01 = CL 3). data.js's site-ordinal refs are only the fallback (originals — no canva batch, they keep Vision cuts). Mapping receipts: `canva-review.html` (vault). Demo-state maps (`JSPARK`/`JGONE`/…) are keyed by the NEW refs.** Gotchas: the lightbox frame is `object-fit: contain`, so its fill color shows as letterbox bands — near-black on dark, white on light (anything else reads as a stripe); the lightbox pull-forward animation looks for `.frame, .panel` since j treatments have no `.frame`. **ONE CARD SPEC (2026-07-22): `.d7f` is a CSS container (`container-type: inline-size`) and every internal dimension is cqw** — a px line at the 232 reference precedes each cqw line as a pre-2022-Safari fallback (old browsers get the previous approximate look). Grid slots still render via `lockCardScale` (232 then transform-down — true reductions that duck minimum-font clamps); belt slots and the fv3 lightbox card are exact reductions/enlargements by construction, so the hand-scaled `.card .d7f` belt pass and the `#lb.fv3` geometry overrides are DELETED — `#lb.fv3` keeps only the ambient shadow, frame-chrome kill, nav, and product slimming. The lb `.frame` rides the same square spec as `.stage` (shared `.d7f .stage, .d7f .frame` selectors; img abs-centered at max 79%/78% — the iOS-safe pattern); the nav seat/arrows are cqw at a 430 reference (N px at 430 = N × 0.232558cqw) so the seat tracks the rail dots at any card width. Card aspect ≈ 1.51:1 everywhere → the open/close flight anchors whole-panel-to-panel in fv3 (`anchors()` helper; other treatments keep square-to-square vs the stock lb) and lands every edge — this is what fixed the mobile bottom-off landing and the desktop double-frame mid-flight. The lift-out (slot goes empty, card flies back to it) runs on EVERY pointer now — and **THE HOLE MIGRATES WITH THE EXCHANGE (2026-08-06, Dylan's catch: inspect hs14, arrow to hs09, close — "09 lands ontop of its duplicate and hs14 pops back"):** every shuffle LANDING re-seats it (`migrateHole`, called at all three landing beats: the exchange timeout, `xactSettle`, and the reduced/no-geom path) — old card back in its slot, arriving board's card out, so the close flight always lands in a hole. A decked target empties `lbSrc` (its slot is already someone else's hole; the close routes to the fade via the indeck guard) and a later undecked landing RE-ATTACHES the hole — not an early return, or the twin comes back two boards later. Case/shelf (bay-item) inspections keep their shelf hole. Belt holes survive because the inspected card's rail is FROZEN for the whole inspection (the recycler stomps inline visibility per frame on unfrozen rails). And `#lb` is the mobile scroll-sheet on ALL sizes: below the live topbar, transparent scrim, flex-centered card, plain scroll on overflow, with a desktop height guard (`max(300px, min(430px, (100dvh − tbh − 120px)/1.53))`) that shrinks the whole card via cqw instead of clipping. `#vlb` keeps the stock dark-scrim centered layout on desktop. Page-scroll lock is `dockActive()`-only — desktop keeps its scrollbar (the vanishing bar re-columned the shelf grids 3→4 at some widths; `lockScroll()` pads body by the measured bar width on the paths that do lock, and `scrollbar-gutter: stable` was tried and REVERTED — the spec only reserves the gutter for overflow scroll/auto, not hidden). The sheet is bar-less >720 (`scrollbar-width: none`, topbar precedent). And any future display rule on `#lb` must not outgun `#lb[hidden] { display: none }` — an id-strength `display: flex` once beat `.lb[hidden]` and shipped an always-visible empty lb shell over the whole site. 

## Canva cutout pipeline

- Canva cutout pipeline — **CLASSICS DONE (2026-07-21): all 54 classics exported, bg-removed, pre-named as `card-lab/canva/cl<N>-top.png` / `cl<N>-bottom.png` (vault)** (107 files; cl54-bottom missing — that photo was never taken, re-shoot the teal board's underside). The winning flow (repeat per collection): (1) Canva **Sheet** with columns name/size/top/bottom, one board per row, bg-removed images in the cells; (2) name the sheet after the collection (design titles inherit it); (3) sidebar **Bulk create** → data range incl. header row → pick `classics-card-template` (2000×1500, two images, NO text element — a text layer bakes into the export and pollutes the cutouts, learned the hard way) → auto-match → generates one design per row titled `(Bulk N) <sheet-name>`; (4) `search-designs` for those titles (paginate, ~25/call), `export-design` each `{png, transparent_background, lossless}`, curl down; (5) local script slices each card L/R and keeps the largest connected alpha component per half (scipy `ndimage.label` — this also strips any stray marks), crops to bbox, writes `cl<N>-top/bottom.png`. Row order = board order = names, so **no pixel-matcher naming step needed** — batch-review.html (vault) is now only for the legacy `batch1-pNN.png` files (superseded for classics, ignorable). Old `(Bulk N) name` designs in Canva are the text-bearing v1 batch — trash freely; `(Bulk N) classics` are canonical.
  **ORIGINALS RESHOOT PLANNED (Dylan, 2026-07-25): both faces, same canva flow as cl/hs** — until it lands, originals flips use the flip-lab's (vault) interim blank-back treatment. **First fruit landed 2026-08-07: `~/Desktop/newforavrg/` holds 13 bg-removed SIDE-PROFILE shots of complete originals (trucks + wheels — a new angle, 3375px, alpha verified; pairs b1/b11 · og/og1 · peach/peach2 · rainbow/rainbow1 · short steep/3 · yblue/yblue1 + yellowblue3). Incomplete per Dylan ("still need a shot or too"); destination undecided — don't wire in unprompted.**
  **SHAPED DONE (2026-07-21): 24 boards as `hs<N>-top/bottom.png` (48 files: hs1–hs21, hs22.5, hs23, hs24).** Second run refinements: name the sheet rows `hs<N>` directly and the generated designs inherit clean titles (no `(Bulk N)` indirection); export can be fully manual (Projects → select all → Download → transparent PNG zip) — no browser automation needed, only the slice script runs locally. **Recurring Canva Sheets gotcha: it duplicated one row's images again** (hs22 came out pixel-identical to hs13) — ALWAYS hash-check exported cards before slicing (`md5` the files, then pixel-hash the sliced halves; identical byte-size is the tell). The missing board was hand-fixed in the hs22 design and re-exported as `hs22.5` (name kept, it's a distinct board — white AVRG stamp; its cutout is larger, ~360×1000, because hand-placed images beat bulk-create's auto-fit). Loose thread: `~/Desktop/backgroundremovalcanva/shaped/` has 52 photos but only 24 boards made the sheet — 4 photos unaccounted (junk frames or up to 2 missed boards); check sheet vs folder next Canva session.

## Inventory-ID plumbing (port to build_site.py eventually)

- Inventory-ID plumbing: index.html's `CANVA` map (board→cutout file + canonical cl/hs ref) is a JS-side bridge. The permanent home is build_site.py → data.js: carry each board's inventory ID and cutout path in the data so every page gets them without a hand-kept map. Do this when porting to production `site/`. The mapping itself was derived by pixel-match + sheet-order (receipts in `canva-review.html` (vault)); classics sheet order = site order with inventory boards interleaved.

## Real board dims — DIMS_MM (2026-08-13)

The dims machinery finally eats real data. `DIMS_MM` in index.html (beside `PH_DIMS`) is the hand-authored source of truth: keyed by PADDED inventory ref, value `[width, length, tailWidth, noseWidth]` in mm, tail/nose optional (HS 10/15/18 have none — Dylan measured no parens for those). Width is the widest point; the parens in his 2026-08-13 message were "width at tail, width at nose". `dimsStr()` derives the card string (`32.4 × 95 MM`); `jmeta.dimsPh` is now `dimsStr(DIMS_MM[ref]) || PH_DIMS[ref] || ""`, so the hover name→dims decode (`data-dims` via `nmt`) fires with real numbers on all 14 hand-shaped boards. The card decode stays width × length ONLY — tail/nose were "too much noise on a card decode" (his words); the full line lives in the lightbox (`#lb-dims`, see lightbox.md). `PH_DIMS` shrank to `OG 01` only (HS 01's placeholder retired when its real row landed) — it remains a card-decode demo and NEVER prints in the lb. Hand-authored in the page like SHORT/SUB/BARLINE because data.js is generated. **HS 16 CONFIRMED** (2026-08-13, same session): Dylan's list typed "HS18" twice — not only a typo, he'd written it down twice; the inferred HS 16 assignment was right and the near-twin numbers to HS 07 are real. Second batch same day: 12 classics landed, width × length only (popsicle — no tail/nose measured). Still unmeasured: CL 03/06/08/12/16/19/26, all originals, resale.

## THE CARD ART MOVED IN (2026-08-16 — the v1 port)

**The cutouts stopped being lab output and became site assets.** The LIVE SUBSET rode into v1: 66 of the vault batch's 168 exports — the 33 faces the `CANVA` map actually names, plus each one's flip partner (the other face `BOT` reaches for) — now sit at **`site/img/cards/canva/`**, and the 6 originals Vision cutouts at **`site/img/cards/cuts/`**. `CUT` and `BOT` are repointed to both (they read `card-lab/canva/` + `card-lab/cuts/` in the vault); the map itself, the `INVREF` derivation and the jref/refOf law are untouched — only the delivery address changed. **THE FULL BATCH STAYS IN THE VAULT** — all 168 canva exports and 39 Vision cuts, `cutout.swift`/`trim.swift`, the Canva sheets and `canva-review.html` (vault) — because the toolchain is a workshop, not a shipped asset, and a public repo has no business carrying 100 unused faces. **CONSEQUENCE FOR NEW BOARDS: card art comes from the vault** — export the pair out of the existing batch, or re-run the canva flow there (the pipeline entry above is still the recipe), then drop the two faces into `site/img/cards/canva/` and add the board to `CANVA`. Both faces, always: a board whose flip partner never crossed over reads as a no-pair board and loses its flip, exactly like originals and resale do today.

## The A/B pass — card items (2026-08-16, ported from the winning vault run)

- **THE FLIP CHIP IS THE MINI ALONE** — the `fw` "flip" word came out of both `.fflipc` producers (the fv3 template + the lb's static chip); the hidden-face mini stands alone at the corner (`object-position: top right` pins the art; aria-label keeps the verb for readers). The tap target is the mini's own box — pad the button, never reintroduce text, if it ever feels thin.
- **THE IN-VIEW DIMS DECODE IS RETIRED** — `armDimsDecode` (the IntersectionObserver armer) is deleted with its call site; mobile grid cards rest on their print names. Dims decode survives exactly where there is no hover to earn it: the lb (dockDress/stayLand) and YOUR PICKS (static land); desktop hover untouched. The armer lives in git history (this repo's `7f27209`) if in-view ever returns.
- **THE BLANK CARD** — a deck/picks-held board's grid slot is a VISIBLE blank now (`.scard.indeck`): panel + border + inframe drawn, board img/texts/chips at `visibility:hidden` (never display — lockCardScale measures the .d7f), `box-shadow:none` on the panel, `site/img/avrgbg-buttonflip.png` centred at 58% width / opacity .5 via `.panel::after`, `pointer-events:none` on the slot. **BELTS STAY TRUE HOLES** (`.card.indeck { visibility:hidden }`) — the recycler stomps inline visibility per frame and a costume would fight that write. All flight guards are class-checks — costume-safe. One visible new beat: mid-flight on a deck return the blank shows under the incoming fly until touchdown (the empty seat awaiting its card).

## THE DIMS REST — picks + shelves rest on their dims (2026-08-18)

**The card's standing name line is now its DIMS on the two surfaces that hold boards already CHOSEN: YOUR PICKS and the drawer's shelves.** The collection grids and the home belts — where a board is still being met — keep resting on the print name, and their hover decode (name→dims, ref→AVRG, status in) is untouched. This generalizes the 2026-08-16 A/B ruling (picks rested on dims, but only under the phone gate) and answers the same question for the deck's desktop half: a decked board's identity is settled, so the read that earns the line is the size.

**THE WRAPPER IS THE LAW.** `DIMS_REST = ".lisst-card, .bay-item, .dimsrest"` beside `DRESS_TEXTS`, with three readers built on it: `restsDims(card)` (a `closest` test), `restNm(card)` — **the ONE reader of a card's resting name**, precedence flipped→UNDERSIDE, then surface dims, then the print name — and `landDims(root)`, the static land for a fresh render. `renderLisst` calls it phone-gate-free (the old `if (M)` dims loop is gone) and `renderBay` calls it after seating its items, so a removal re-stack or a mid-flight preseat re-lands the rest without replaying a scramble. `cardDressOff` now writes `restNm(card)` instead of picking between `dataset.nm` and UNDERSIDE itself — **that is what makes a desktop hover on a shelf or picks card return to the dims, not the name.** Add a surface to the law by adding its wrapper to the one selector; do not add a second rest-name reader (the `FACE` law's shape, for the same reason).

**Two exemptions, both older laws kept.** An unmeasured board (empty `data-dims` — CL 03/06/08/12/16/19/26, originals, resale) falls back to its print name everywhere, so the tail never rests blank; and a flipped card rests on UNDERSIDE on every surface. `setFlip` is deliberately NOT routed through `restNm` — it is lb-only, and the lb card's flip-back restore keeps its own `dockActive()` dims clause (the lb rests on dims by `stayLand`/`dockDress`, not by wrapper).

**Rest == rest at every landing.** `cardDressOn` gained a changed-only guard on the dims decode (`nmt.textContent !== dims`) — decoding dims INTO dims is the exact re-animation `stayLand` rules out, and a dims-rest card is already showing them. `buildFly`'s mid-air `rest()` drains the name line to `restNm(flyCard)` instead of always the print name, and the two bay-bound flies wear the `.dimsrest` marker (`seatFly` — every add path, chip/drag/lb — and `lbToShelfArc`), so a card drains to the dims the shelf under it is already showing. Verified in flight: the fly launches carrying the grid card's name, drains to `33.75 × 92.7 MM` at t≈.55, lands on a shelf resting on the same string. Marker class only — no CSS.

## THE FLIP MARK — the corner gets its verb back (2026-08-19, ported; his pick: lucide refresh-ccw)

The flip chip (`.fflipc`, top right — the hidden face as a mini) had carried no verb since the FLIP word retired: a board mini in a corner and nothing saying it was a control. `redesign/mark-lab.html` (vault) ruled the shape — **drawn, not typed** — and Dylan named the glyph: **lucide `refresh-ccw`**, verbatim from `~/Projects/web-assets/icons/lucide/`. It sits **dead centre ON the mini board, in white** (his call, same day — the first cut put it above the mini where the word had been), wearing the lucide dress the site already carries (`.bay-mic`/`.bay-sk` — fill none, currentColor, 2px round caps at 24 units, which scale without the deck mark's crispEdges sub-pixel mud). Sized in cqw: 5.6034cqw ≈ 10px on a phone grid card, ≈ 16px on the phone lb.

**Centring is measured against the PAINTED board, not the slot.** The mark is `position:absolute` + `translate(-50%,-50%)` and `pointer-events:none`, so the chip's box stays exactly the mini's box and the button keeps the whole hit area. "Centre of the slot" is only "centre of the board" while the slot's ratio matches the cutout's — the canva set measures 0.345–0.391 wide-to-tall, `object-fit: contain` fits by HEIGHT and `object-position: top right` pins to the right edge, so a slot much wider than ~0.38 puts the mark visibly left of the board. This repo's slot is 9.4827/25 = 0.379, so both its surfaces land clean (dx ≤ 0.4px); the vault had to narrow its desktop ghost slot for the same reason. **Remember the coupling if either box is ever resized.**

**Two producers, ONE copy of the paths**: the template's chip gets `FLIP_MARK` inline; the lightbox's older STATIC chip (`#lb-flipc`) has the same constant injected once at boot rather than pasted a second time.

**THE DESKTOP GHOST CAME WITH IT (2026-08-19, his call: "lets fix the v1 ghost too").** Porting the mark surfaced that T9's ghost had never reached this repo at all — the lb chip's display rule still sat inside the `max-width: 940px` block, so the desktop lightbox had no corner and still wore the Top/Bottom words the ghost was built to replace. Ported in full: `#lb-card.canflip .fflipc` and `#lb-card .find { display: none }` move to top level (the words are now hidden at every width — their retirement reached without ripping the markup out, so their click door is inert rather than gone), and the ≥941 block gains the ghost's dress — face opacity .6 rising to .85 on hover (**on the FACE, never the button**, or it ghosts the mark too), the 0.379-ratio slot, and the mark's own 4.3399cqw. **The CSS was only half of it:** the chip's click door opened with `if (!chip || !dockActive()) return`, so a desktop ghost would have been a dead button — T9 moves that gate BELOW the lb branch (the lb chip answers on every width; grid chips stay the phone's flip door). A ported ghost without that line looks right and does nothing, which is exactly how it read on first verification.

Verified served (:8124, own headless Chrome with touch emulation) at 393×852: the mark renders on the phone grid card and the phone lb, the chip's tap still flips the face, no console errors.

## THE ORIGINALS GET THEIR FLIP — the reshoot starts landing (2026-08-22)

Since 2026-07-25 originals were the one collection with **no card flip**: `BOT()`
resolves the *other* face of a CANVA pair, originals had no canva batch, so
`canflip` never armed and their card was one-sided. Dylan's removed top+bottom
camera-roll shots are that reshoot. **Bluepiro (`originals-07`) is the first one
through**, and it needed **zero new code** — `canflip` is toggled purely on
whether `BOT()` resolves, so a pair in `site/img/cards/canva/` plus one CANVA row
is the entire switch. Verified end-to-end: card face `og7-bottom`, corner chip
mini `og7-top`, lightbox turns to UNDERSIDE, strip thumb follows.

**THE NUMBER IN THE FILENAME IS THE BOARD'S POSITION, and that is load-bearing.**
Adding an original to CANVA moves it off the `PREFIX + (k+1)` fallback and onto
`INVREF`, which reads the ref back out of the filename — `og7-bottom` → `OG 07`.
Match the position and the ref is unchanged (verified: OG 01–07 all print the same
strings as before, so `JSPARK`/`PH_DIMS`/`DIMS_MM` keys keep resolving). Get it
wrong and you have silently renumbered a board's ref everywhere at once.

**Front face = whichever face the board leads with.** Bluepiro leads bottom-up
(`"originals-07":"og7-bottom"`), the same shape as the `hs*-bottom` rows.

**The canva pair SUPERSEDES `cuts/`.** Once a board has a CANVA row, `CUT()` never
reaches `site/img/cards/cuts/` again — delete the orphaned `originals-NN-1-cut.png`
in the same pass rather than leaving a dead asset in a public repo. 01–06 still
ride their cuts until their pairs land.

## THE ID FLIP — the ref chip rests on the brand, decodes to the board (T4, 2026-08-28, Dylan's ruling)

The corner chip read backwards: the board id sat there at rest and hover scrambled it INTO "AVRG". Flipped — **rest is AVRG (branding up front), the decode carries the info (the padded ref)**. The animation grammar is untouched; only the two strings swapped roles. `data-ref` still holds the board's id, now as the decode TARGET. The producers, all of them: the template renders "AVRG" (data-ref keeps the id) · `cardDressOn` decodes to `dataset.ref` · `cardDressOff` rests "AVRG" · `buildFly`'s mid-air `rest()` drains to "AVRG" · `populate` writes the "AVRG" rest (dataset.ref = the id) · `stayLand` lands `dataset.ref` — **the stay'd lb card shows the REF for the whole inspection**, because the persistent dress IS the decoded state (the fills-just-stay law; hover on it still answers nothing) · **`dockDress` gained the ref land** (changed-only guarded) — the phone has no hover, so the landing is its decode beat, and without it the phone lb would have LOST the board id entirely (it used to sit there statically). Touch grid cards rest on AVRG like every other surface. The chip box shrink-wraps, so it IS narrower at rest now (4ch vs 5ch mono) — it is absolute in `.corner`, top-left anchored, so the decode grows it rightward and nothing reflows; the mono glyphs keep the scramble frames at target width from frame one (the existing law at the fill-size comment). The dead touch pos-decode branch in `populate` (`LB !== "deck"` under a pinned `LB = "deck"`) was left as found.

**THE MOBILE AMENDMENT (T9, 2026-08-28, Dylan's ruling):** the flip is a HOVER trade — branding at rest, info on the decode — and the phone has no hover to pay the info back, so an AVRG rest there would bury the ids entirely. **Touch grids rest on the BOARD ID** (the pre-flip read): the one change is in the `JCARDS.framev3` template — the chip renders `canHover ? "AVRG" : ref` (`canHover` is the site's one hover gate, the same `matchMedia("(hover: hover)")` const that already arms/disarms the whole dress choreography, so every template-rendered surface — grids, belts, picks — inherits the right rest without asking). Desktop keeps T4 exactly: rest AVRG, hover decodes to the id; the flanking deck cards' rest-AVRG/land-id stands as merged. **The phone LIGHTBOX keeps the flip's ceremony and it was verified real, not adjusted:** `populate` writes the AVRG rest at the tap, the open flight carries it (~0–430ms, chip visible undressed), and `dockDress` decodes the id at the landing — CDP-polled at 60ms: AVRG through 422ms, scramble frames at ~480–540ms, the ref landed by ~600ms. AVRG-before-the-board is the phone's one taste of the brand beat; the grid is where the ids live.

## THE GRID MINI JOINS THE DESKTOP DRESS (2026-08-29, Dylan: "have the flip side mini board fade in and out in collections on hover")

The corner flip mini (`.fflipc`'s `.fface`) was phone-grid-only on grid cards; the desktop's one chip was the lb ghost. Now desktop grid cards (`.scard`) show it too — as a **PASSIVE PREVIEW, not a control**: seated at rest but `opacity: 0`, fading up (.35s) with the hover build and `.lit` (the canonical `:is(.d7f:hover, .d7f.lit)` pair), draining on leave. The **mark is hidden and pointer-events are off** up there — the card's one click stays the inspect, and the lb ghost stays the desktop's one flip DOOR (T9's `dockActive` click gate untouched; the grid chip handler would no-op on desktop anyway, but a verb glyph on a non-control would lie). Gated `(min-width: 941px) and (hover: hover)` so wide-touch keeps today's bare card rather than an element nothing can ever reveal. The transparent chip keeps its real box, so the lazy face warms as the grid scrolls (the 0×0 law — `display: none` was what starved it before). `.scard.indeck`'s visibility-hidden costume still wins over the new display rule by channel (visibility vs display). Verified real Chrome 1440×900 on #classic: rest 0 → hover 1 with the full dress → leave 0; face loaded pre-hover.

**REVISED SAME NIGHT (Dylan: "ghost it down and add the flip icon. basically give it the lb card dressing"):** the passive-preview cut lived one commit. The grid mini now wears the LB GHOST's dress at grid scale — face ghosted .6, rising .85 under the chip's own cursor (opacity on the FACE, never the button), the refresh-ccw mark full white on the board — **and it is a real flip DOOR**: the delegated handler's `!dockActive()` grid fence is RETIRED (a verb glyph on a dead button would lie), so grid chips flip on every width; where no chip renders (wide-touch ≥941, tiny renditions, flights) the CSS is the gate. The `.lisst-card` bubble-through and the lb branch stand untouched. COUPLING: the .6/.85 pair is the lb `ghost-opacity` param hand-restated in the `.scard` block — a retune carries both (manifest note, C-side).

## THE DIMS GRAMMAR (2026-08-29, Dylan's react: "33.0mm x 95.85mm")

His diagnosis: "the varying dim lengths in digit count and the mm being the same... i feel like it could be more easily readable." The dims string is re-cut everywhere it prints: **every measurement carries at least one decimal** (`dimsNum` — 33 → 33.0; 95.85 keeps its own) **and its OWN lowercase mm, rendered much smaller than the digits** (`.du` span, .62em, text-transform none against the caps line). ONE producer (`dimsStr`) + one demo string (`PH_DIMS`) emit the new plain string; the small-mm is markup added at WRITE time by `dimsRich`/`dimsWrite` — the plain string is what `dataset.dims` and every changed-only guard compare, and **textContent reads identically through the spans, so no comparison in the file moved**. The unit gate is the digit+mm pattern (names can hold mm-the-letters — "SUMMER" — but never digit+mm). Writers converted: `decodeInto` finishes rich when the target carries units (churn plays plain, the mm settle small at the landing), `decodeCollapse` builds unit chars as SMALL `.dcell.du` cells (the position lock freezes their real widths — traced live: phase B churns 15 cells, 4 small, lands rich), the static writers (`landDims`, `cardDressOff`'s rest, both reduced branches, `setFlip`'s back write) go through `dimsWrite`, the lb spec line gains mm on tail/nose too (`dimsNum` on both), and **`copyCardTexts` copies innerHTML now** — a text copy would land a fly with full-size mm against a small-mm rest. Verified real Chrome: grid hover, lb collapse (fresh + carried-from-hover), bay shelf rest (OG 01 rich, unmeasured boards keep the name fallback), zero errors. Bench: dims format isn't a charted dial; the lb-dims wing (type/reserve) untouched.

## THE ADD CHIP IS AN EMPTY CARD STACK (2026-08-29, Dylan: "its too easily missable")

His ask, whole: *"lets talk the add chip, its too easily missable. lets make them universally like the mobile version, could we also make the empty card in the chip look more like an empty card stack in the header (minus the count indicator). will probably need to re design it to get it to read as a blank card in such a small space."* Three things landed together, and they are one change.

**ONE COSTUME AT EVERY WIDTH.** The phone chip's dress — dock-gated since 2026-08-08, re-cut round 5 on 2026-08-13 — came out of the `max-width: 940px` gate and is now the chip's only costume: **`.fc-list` takes `.fc-ref`'s exact padding** (`7px 9px 6px 10px` / `3.0172cqw 3.8793cqw 2.5862cqw 4.3103cqw`), an empty `.ft` drops out of flex, and the box shrink-wraps. The 940 gate for the chip is **gone**, not narrowed — there is nothing left inside it. The desktop's 2026-08-05 tuck + 2026-08-08 1px trim retired with it (both were cut for a bare 18px mark).

**THE FAN WAS SCRATCHED SAME NIGHT (Dylan's react to the shipped stack: at word states — the phone lb's ADD — the three-card fan mushed into a white blob; "this chip we had, with the inner frame dressing"). The glyph is ONE MINI CARD now — and FLATTENED on his second mock same night ('thats all it needs though. no depth or stack'): outline + hairline INFRAME + the two side gap dots, centred, NO shadow — dropping the depth is what gave the inframe its room (card 15×21 in the 24 box). Same const (`STACKSVG`), same class (`.fstack` — the bench's chip-stack-size anchor and the nine colour rules point at it; the name outlived the fan), same `.fplus` stand-down. The paragraph below records the fan for the one commit it lived.** ~~THE GLYPH IS THE HEADER'S HAND, DRAWN.~~ `STACKSVG` (beside `FLIP_MARK` in the JS) replaces BOTH the deck mark (`.fscript`, desktop) and the blank mini (`.fmini`, phone). It is the `.hpicks` fan at chip scale — front card bottom-left, two cards rising behind it to the right, no count — but a literal clone of the header markup is mud at ~20px, so the fan is **drawn with the occlusion baked in**: the front card is an evenodd ring carrying the add `+` in its empty middle, and the two behind are drawn as only the edges you would actually see (top rail · right rail · the bottom stub past the front card). One `currentColor` family in a 24-unit viewBox, `shape-rendering="crispEdges"` on every rectilinear path — so the dress's ink→white flip carries the whole object with the existing `:is(.ft, .fstack)` colour rules, and there is no second ink channel to keep in sync. **`.fplus` is a group so `:has(.ft:not(:empty))` can stand the + down** the moment the chip has a word — the ✓/`Mine !` of a decked card, or the phone lb's ADD landing. That is the 2026-08-08 `:has` mechanism kept verbatim: no new plumbing, and no layout change, because the + lives inside a fixed-width svg.

**SIZE IS THE MISSABILITY ANSWER.** `9.4827cqw` (22px at the 232 reference) — deliberately the SAME cqw as the flip chip's corner mini, so the card's two corners carry objects of one weight. It replaces an 18px hairline ring on desktop and a 10×14 blank mini on the phone. Measured at 1440×900: the grid chip box went **76.3 × 29.0 → 41.0 × 35.0** — narrower, taller, and full of ink instead of reserved air.

**THE WANT WORDS ARE RETIRED, AND THAT IS WHAT FREED THE BOX.** `WANTS` + `wantWord()` (the "Want? / Like? / Need? / Mine !" scramble on card hover) and the 2026-08-08 **chip-aim pair** that decoded it to "ADD+" are both gone, with their callers in `cardDressOn`, `cardDressOff` and `stayLand`. They only ever fitted because `.fl` reserved `calc(6ch + .96em + 9.9138cqw)` for the widest word — **that reservation is deleted**, which is what lets the box shrink-wrap. The mark's shadow-flip half of the aim answer (`.fscript .sh`, four mirror rules) went with the mark. **Nothing writes into `.fc-list .ft` on desktop any more**; the only writers left are membership (the ✓ a click lands) and the phone lb's ADD (`dockDress`) / `landWants`' `Mine !` — and both grow the box **LEFTWARD off the pinned bottom-right corner**, which is THE ID FLIP's ruling for the ref chip and exactly what the phone has shipped since 2026-08-13. So the never-resize law still holds where it was written to hold: **no hover and no decode moves the chip box.**

**THE FOUR-STATE MIRROR IS UNTOUCHED IN SHAPE** — `chipwipe .44s` at `transform-origin: right center`, with `:hover`/`.lit` at 0, `.tset` at +.22s, `.hold`/`.redress` verbatim, `.dressed`/`.bay-fly.held`/`.bay-item`/`.lisst-card` at −.44s. The only edit inside it was swapping `.fscript` for `.fstack` in the nine `:is(.ft, .fscript)` colour rules, so the new glyph inherits every state the mark had. Verified computed: hover fill `matrix(1,0,0,1,0,0)` + glyph `#fff`; `.bay-fly.held` `chipwipe` at `-0.44s` pre-drawn, glyph `#fff`.

**TWO SINGLE-PRODUCER SEATS.** The template's chip renders `STACKSVG`; the lb's older STATIC chip (`#lb-fv3-list`) has it **injected once at boot** into its `.fl`, the `#lb-flipc`/`FLIP_MARK` arrangement — the artwork is never pasted twice. **`#picon`'s `.deckmark` is now the ONLY copy of the deck-mark artwork** (the bar's `.bar-script` seat traded it for the card stack 2026-08-28; the chip retired it here) — the "edit the survivors together" note is spent.

**BENCH:** `chip-clock` (.44s/.22s) is unmoved. **`chip-reserve` is RETIRED** — `.d7f .fc-list .fl`'s `min-width: calc(6ch + .96em + 23px)` / `calc(6ch + .96em + 9.9138cqw)` no longer exists. Its natural replacement as the chip's one size dial is `.d7f .fc-list .fstack`'s `width`/`height` (`22px` / `9.4827cqw`).

Verified in a private headless Chrome (own port, own profile) at **1440×900** and **393×852**: grid cards on `#classic`, the home belts, the bay shelves (drawer opened via `#bay-panel`), My Lisst / YOUR PICKS, and both lightboxes — rest, hover, the ✓/`Mine !` decked read and the phone lb's ADD all render one identical glyph, zero console errors on every route, and reduced motion still lands ref→id, name→dims, status in with the chip silent.

## THE SPARK GROWS TIERS (2026-08-29, Dylan: "this batch of boards gets the highest tier makers mark")

The quality-identifiers thread's first real build (open-threads item 3 — the premium-finish tier he shaped 08-28). `JSPARK`'s value is the TIER now: 1 = the ✱ builder's mark (mechanism untouched, currently unheld), **2 = the highest maker's mark**, and his named batch holds it: **HS 06 · HS 07 · HS 16 · HS 17 · HS 18** — the first real seeding since the demo seeds came out. ONE producer (`sparkSpan(m, cls)`) renders both corner seats (the fv3 `.tspark` and the j-treatment `sparkHtml` corner); the tier picks the glyph, the seat picks the class. Tier 2 wears `.spark.prem` — INK, not the ✱'s accent orange (the top tier out-ranks by weight) — with `PREM_MARK`, an inline svg sized per seat (grid 5.1724cqw · belt 10px · legacy 13px). **The shipped glyph is an INTERIM pick — the countersink (a chamfered hole seen from above: outer ring + sunk dot, the finish itself) — pending Dylan's four-way collide verdict** (countersink · black-chip ✱ · lucide award · filled ✦; crops in the 08-29 session). Swap `PREM_MARK` + the `.prem` seat rules to re-dress it. Tooltip language is placeholder ("Maker's mark, highest tier — absurdly well finished") — the tag wording is still the DM conversation. The displacement rules (`.tspark ~ .find/.fflipc` drop) and the `.scard.indeck` hide ride the shared `.tspark`/`.spark` classes untouched — verified: all five marked cards drop their flip mini 30px, zero errors. Still open in the thread: real ✱ curation, and the mark saying its name on touch (the lb surfacing).

**HIS COLLIDE VERDICT (same night): the highest tier is the FILLED ✦, and the BLACK-CHIP ✱ became a second live mark.** Tiers RE-NUMBERED so tier order is rank order — 1 = the accent ✱ (unheld) · 2 = the ✱ in the black chip costume (`.chipm`), held by **HS 08 · HS 10 · HS 14** · 3 = the ✦ in ink (`.prem`), the highest, held by the HS 06/07/16/17/18 batch. The interim countersink svg retired with the verdict; both new dressings are text glyphs + CSS (SPARK_GLYPH/SPARK_CLS lookup rows beside SPARK_TIP — one producer still). Tier-2's tooltip is placeholder like tier-3's ("a cut above") — the language is the DM conversation. Seat sizes are cqw with px fallbacks per ONE CARD SPEC (.tspark.prem 6.4655cqw · .tspark.chipm 4.3103cqw + padding), so belts scale for free.
