# index.html — page-level core
Grounds (black/white), pinned CARD/LB constants, the interim gpick, treatment plumbing.
> **Ported from the vault 2026-08-16** — the page is now `index.html` at the repo root (was `redesign/k-home-dual.html`), asset paths below are repointed, anything marked "(vault)" (lab pages, the card-art batch, the labs.md catalogue) did NOT ride over and lives in the vault repo, and every git hash cited below is VAULT history.
> Carved verbatim out of the old monolithic CLAUDE.md on 2026-08-12 (full backup on Dylan's Desktop + in git history). Wording is preserved exactly — section headers were added for navigation only. New decisions on this surface get appended HERE, not to CLAUDE.md. Cross-references like “see X below” may now point at a sibling doc in this folder — check CLAUDE.md's index.

## The page, its grounds, and the pinned treatments

- `index.html` — the h-blackout home page (same engine, conveyor shelves and all) **THE COMPARISON SWITCHER IS GONE (2026-08-01)** — Dylan's call once the deck's look landed ("totally not torn on the cards now"). All that survives on screen is `.gpick`, a plain black/white **ground** pair at the BOTTOM LEFT (opposite the deck's right-edge case, so it can never read as drawer chrome), still on `avrg-k-ground`. It is INTERIM by his note — *"it will get a site button in time, same with the black white selection. neither will be in the shape they are in now"* — so do not invest in its look; the real control arrives with the light/dark extraction at the port. **CARD and LB are now PINNED CONSTANTS, not stored preferences** (`CARD = "framev3"`, `LB = "deck"`): read-from-storage would strand anyone holding a stale `paper`/`thumbs` on a treatment with no control left to escape it — verified by booting with both seeded and confirming the pins win. The losing treatments' CSS (photo/paper/zoned) and the pre-rework thumb-strip markup are still in the file, unreachable, and go at the port; `setCard` went with the row, so nothing rebuilds the rails mid-session any more. Dark is the h-blackout CSS verbatim; light is a `body.light` override block using the j-cards-white (vault) idiom (#fff ground, #000 max-contrast, #444 soft, #767676 hint). Treatments keep their own ground on both page grounds by design (the collision is the point); the only adaptations are fluid sizing (width 100%, square stage, so one card fits belt slots and grids), a taller belt (`body:not([data-card="photo"])`), an ink border on zoned in light mode, and a belt furniture pass (`.card .wchip/.st/…`) for the d5/paper cards — the d7f's hand-scaled belt pass retired onto the cqw spec (see ONE CARD SPEC below). 

## THE LOGO DRAWS ITSELF (2026-08-16 — thread 1's foundation; his file, inlined)

The home landing's PNG logo is retired — the spine's brand seat holds his `avrg-single-stroke-aligned(2).svg` INLINED (the source file sits untracked at the repo root; the page carries the markup). The choreography is the file's own: the two dots anchor at load, the monogram draws as ONE stroke (per-segment pacing baked into its keyTimes — it hesitates and rushes like a hand), the circle closes as two arcs that stop SHORT of the dots — the finished mark ends in the drawn bar's own ring-gap-dot grammar, so the logo and the buttons speak one language. **`playLogo()` rewinds the timeline on EVERY home arrival** (route's home branch; the draw is an arrival beat, not a load beat — see the SMIL gotcha for why inline + setCurrentTime is the whole mechanism). Colour is `currentColor` — white on the dark ground, #111 under `body.light`; the PNG's `invert(1)` filter retired with the PNG. Reduced motion jumps past the timeline's end (the finished mark, no theater). Sizing: 150px base / 72px narrow-desktop, `height:auto` off the viewBox ratio. ~~Open (thread 1 proper): what the rest of the landing does AROUND the draw~~ — **CLOSED 2026-08-22 by THE LANDING's bake (below).** Both surfaces now have their choreography: the PHONE's load intro poses this same svg full-screen for the draw and shrinks it home (2026-08-16, thread 3's mobile pass; the record lives in mobile.md), and the DESKTOP arrival is THE LANDING. Replay policy is settled on both: **ceremony on fresh boot, plain in-place rewind draw on a mid-session arrival.** Draw pacing still lives in the SVG's own begin/dur attributes and **is never rewritten there** — the desktop's `drawSpeed` scales from a cached base and restores (the law is in THE LANDING's section, and the phone is the reason).

## LIGHT IS THE ONE GROUND — dark mode retired (2026-08-20, the pre-launch ruling landed)

Dylan's ruling ("light is the direction") executed for the Saturday launch. **The pin follows the CARD/LB idiom exactly** (same reasons, recorded at the pinned comment where the ground machine lived, above the CARD const): `<body>` ships `class="light"` in markup — the very first paint wears the light dress, no dark flash before the main script — and the old `avrg-k-ground` storage is **never read**, so a stale "black" pick (Dylan's own browsers hold one) can't strand anyone on the retired dress. Verified: a seeded "black" is ignored, the pin wins.

**What came out:** `setGround()` and the whole ground machine; the `.gpick` chips (markup + CSS); the mobile menu's GROUND row (markup, its flip handler, the `.drow.ground` CSS token — DM is the menu's last row now); the ground half of the 940-crossing handler (the deck hand-off keeps the crossing); the bar-logo/foot ground-swap — both marks ship `site/img/logo-circle.png` directly in markup/`colnavHtml`. Comments that cite the GROUND row as a gotcha's discovery site (the synthetic-pointerleave family) were left as history on purpose.

**What stays:** the `body.light` override block IS the live dress — do not touch it as "dead dark-mode code". The **dark base costume** (h-blackout declarations underneath) is now unreachable and goes in a later **proven-identical sweep** (the TOMBSTONES precedent — not attempted two days before launch; flattening ~300 override rules is where regressions come from). The parked future of the thread is the three-scheme sun / cloudy-sun / moon picker (open-threads).

Bench: charted same-session per the change-with-charting law (page-chrome wing + ogth-ring note, the CompUI side).

**THE DRAW IS ×2 AND EVERY PATH IS OPACITY-GATED (2026-08-16).** Every SMIL begin/dur is HALF the drawn original — monogram begin .225s dur 2.05s, arcs begin .568s dur 1.707s, everything landing at ~2.28s (keyTimes are fractions of dur, so the per-segment hand pacing rode along; both surfaces share the one inline svg, so the desktop arrival draw runs the same clocks — the intro's beat map is derived from the draw end, mobile.md). **Each animated path carries `opacity="0"` + a `<set to="1" begin=<its own draw begin> fill="freeze"/>`** — WebKit paints a round line-cap as a speck at zero dash progress (the gotcha), and at intro scale the arcs' parked caps read as stray dots; the gate means nothing exists before its pen touches down, the set re-fires on playLogo's rewind, and freeze holds through reduced-motion's jump-past-end. **The phone seat is 188px** (`.masthead svg` in the ≤940 block; base stays 150 / 72 narrow-desktop) — the intro pose MEASURES the seat, so the seat is the one knob for the landed size and the big pose derives itself.

## THE LANDING — THE DESKTOP ARRIVAL, **BAKED 2026-08-22**

**IT IS THE DESKTOP ARRIVAL NOW — the switch is gone and thread 1's desktop half is CLOSED.** The desktop home arrival had no ceremony: `startTick()` raced the logo draw and the page sat there fully dressed while the mark drew in place. Four arrivals were built to collide; **Dylan picked D**, A/B/C were deleted, he reacted on it twice, tuned it on the bench, and called the bake.

**HIS BAKE INDEX, verbatim (2026-08-22):** *"BENCH BAKE 2026-08-22 — index / land-travel: 850ms → 1000ms (BENCH.LAND_TUNE.travel) / land-pose-y: 0.333 → 0.420 (BENCH.LAND_TUNE.poseY) / land-draw-speed: 1.00 → 2.00 (BENCH.LAND_TUNE.drawSpeed) / land-vid-at: 0ms → 300ms (BENCH.LAND_TUNE.vidAt)"* — **four clocks moved and nothing else did.**

**THE SECTION CHOREOGRAPHY IS FINAL, in his words:** *"you guys nailed the section load in ask. no tweaks there. its solid."* The cascade's seven numbers (`cascade`, three `head`/`car` pairs, `headDur`, `carDur`) are ruled, not open.

**WHAT THE BAKE CHANGED IN THE CODE:** the `?land=` parse comes out of the arm entirely (any `?land` value is now inert query string) and the arm gates on **bare home route + min-width 941 + motion allowed** — the phone intro arm's shape, one width apart, sitting right below it. The `land-d` class went with the param (it named variant D of a collide that no longer exists; no selector ever keyed off it). **Replay policy is unchanged and now permanent: ceremony on FRESH BOOT only** — the arm runs once per load, so a mid-session home return gets the plain in-place rewind draw, with no state kept anywhere. Every fence comment was rebranded from `DEV SWITCH — the landing, remove when it bakes` to **THE LANDING (permanent)**; the machinery all stayed.

**Round 1 notes:** *"D is the move. move the draw in up so its 2/3 up the page instead of dead center. as soon as the logo starts to makes it move towards its landing lets break our site fade / load in into chunks - originals - then shaped - then routed- also logo lands and video comes in from left of screen. then clamp photo behind it."*

**Round 2, off frame-by-frame screenshots:** *"right now sections fade in top to bottom (barely) and then swipe in top to bottom"* — the swipe was **the veil's own edge** crossing bands that were still arriving. And the clamp correction: *"'behind it' i meant just the clamp photo below, after. so video comes in then the clamp photo. my bad."* Plus the ruling that made this round: **"give this the bench treatment."**

**THE RIDE:** the mark draws BIG in the upper third of a white screen → it starts home **and the veil leaves first**, over a page that is still empty, so its edge has nothing to wipe → the mark seats, the tick decodes, the video slides in from off-screen left → the clamp photo lands **below** it → and on clear ground the sections land **one at a time**: a header, its carousel a beat later, the next header starting while the last carousel is still finishing.

**THE BEAT MAP** (nothing invents a clock — three derivations): `drawEnd = DRAW_BASE_END / drawSpeed` · `shrinkAt = drawEnd + DRAW_HOLD` · `seatAt = shrinkAt + travel`, and the cascade, video, clamp and clear all hang off `seatAt`. **At the BAKED values** (measured on the served page, 1440×900, headless Chrome; ms from ride start): `0` pose · `1238` shrink + bar + **veil rides down** (measured 1230) · `2238` seat — the tick decodes (2231) · `2538` video from screen-left (2531) · `2658` cascade starts (2651) · `3238` clamp (3231) · `3938` tail (3930) · `4178` last carousel (4150) · `~4978` clear, derived so a knob move carries it. The pre-bake map (`2375` shrink · `3225` seat · `~5965` clear) is history — the whole ride is a second faster because the draw runs at ×2.

**EVERY BEAT IS A KNOB — `LAND_TUNE`.** Pose (`poseW`/`poseH`/`poseY`), `drawSpeed`, `travel`, the cascade (`cascade` + per-section `head`/`car` offsets + `headDur`/`carDur`), the spine (`vidAt`/`vidDur`/`clampAt`/`clampDur`), the veil (`veilAt`/`veilDur`), `tailAt`, `clearPad`. Visual speeds ride custom properties (`--landTravel`, `--landHeadDur`, `--landCarDur`, `--landVidDur`, `--landClampDur`) written from the tune each ride, so a bench override drives the same numbers the ride does. **`LAND_TUNE` goes to the bench BY REFERENCE through the shim**, alongside `STRIP_TUNE`/`NB_TUNE`/`SUN` — plus **`benchLandPlay()`, which replays the arrival in place.** That replay door is what makes the tune usable at all: the ride runs once at boot, so without it a knob written from the bench could never be seen without a reload, and a reload throws the write away.

**`drawSpeed` SHIPS AS A RESCALE, NEVER AS A MARKUP REWRITE — the law the bake turned load-bearing.** Baked at **2**, and the 2 lives in `LAND_TUNE`, not in the svg. `drawScale()` rewrites the inline SMIL `begin`/`dur` from base values cached on each node at first touch (so repeated changes never compound), before `playLogo`'s rewind — and `drawRestore()` puts the base back at the foot of `standDown()`, so the mutation is now **ride-scoped**. **THE REASON IS THE PHONE:** both surfaces share the one inline `<svg id="logo-draw">`, and the phone intro derives its whole beat map from the MARKUP's clocks (`DRAW_HOME` = 2375 = the base draw end + a 100ms hold). Halve them at the source and the phone's pen lifts a second before an intro still waiting 2375ms for it. Restoring also keeps the plain mid-session home draw on the pace it has always had. **Proven at the bake:** on a phone boot the desktop driver never runs at all — no node carried a `data-land-b` cache and the intro measured `2378 / 3377 / 4126 / 5078` against its shipped `2375 / 3375 / 4125 / 5075`. On desktop the clocks read base ÷ 2 mid-ride (`0.1125/1.0250`, `0.2840/0.8535`) and base again after the clear. Pre-bake measurement, still true of the knob: ink drawn at a fixed 800ms is `.21` at speed 1, **`1.00`** at speed 3, **`.08`** at speed .5. `drawRestore` sits LAST in `standDown` on purpose — the tick and the veil are what a stand-down owes the reader; a clock rewrite owes them nothing.

**SECTIONS ARE KEYED BY COLLECTION IDENTITY, never document order** — the driver stamps `data-land-chunk` by looking each id up in `COLLECTIONS`, so his order (originals → shaped → routed, his "routed" being `classic`) survives any reshuffle of the data, and any band that never got a stamp is swept up by the tail beat. The old `nth-of-type(2..6)` ladder is gone.

**Gates (baked):** desktop 941+, motion allowed, bare home route. No param term — `?land=d`, `?land=a`, anything: inert. Verified identical beat maps with and without.

**The fence, five sites in `index.html`** (all marked `THE LANDING`): the CSS block at the foot of the sheet; the arm script at the top of `<body>`; the `.land` term on the `startTick` boot gate; `LAND_TUNE` + the driver at the script's tail; and the two shim lines at the IIFE's foot. **The shim survives the bake** — `window.BENCH.LAND_TUNE` (by reference) and `window.benchLandPlay()` retire with the pilot like the rest of the shim, not with the ceremony; `LAND_TUNE` is the shipped table now, not a switch's scratch pad.

**No new z rungs** — the ride reuses the focus veil (6400) and the intro's masthead lift (6450); the shield is the shields rung (6700). The pose is **transform only**, so the bench's `logo-size` param never moves.

**THE CLAMP PHOTO** is a real element the driver builds and inserts after `.video-wrap`, dressed to the video it follows (radius, ink ring, hard shadow, column width). It takes its box at arm time so its arrival is a fade and never a reflow. **Its `.in` class is its own, not the body's, so it survives the clear** — the one beat that leaves something behind; a ride cancelled before it lands removes the node instead. **But the print is taken back at the START of the next ride (T2 bench-find, 2026-08-21):** surviving the clear also carried it into a replay already arrived (measured in at ~16ms instead of its beat), which made `clampAt`/`clampDur` unjudgeable on a warm stage. `playLanding` now removes any existing node before building, so the arrival replays; the persistence-through-the-clear and cancel-before-landed semantics are untouched. **Known:** at 1440×900 it sits below the fold (the video ends ~888), so the beat is only visible on a tall window or after a scroll — **RULED 2026-08-21 (Dylan): "leave the clamp below the fold, thats a fun find."** A deliberate below-fold discovery per the hidden-delight thesis; do not "fix" it upward. Same file as the `.made-row` figure further down; he knows.

**Three specificity/flush bites, all the same family, all worth keeping:**
1. The video's slide SNAPPED while its fade played — the shared hold selector is *more specific* than the video's own rule, and **a state's `transition` REPLACES the base list rather than merging** (the `.fveil` law, hit from the specificity side).
2. The clamp was held past its own beat and only appeared when the TAIL swept the spine — it lands inside `.spine`, and that hold (1,4,1) far outranks `.land-clamp.in` (0,2,0). Both organs are now excluded from the shared hold and carry their own. **One organ, one hold.**
3. `standDown()` at the top of a first run painted the dressed page and faded it out under the draw — its `fveil.clear()` carries a forced reflow, which **commits the undressed state** to the transition engine. Only a replay resets now, and it takes the start pose in one frame under `.land-snap` (transitions off + reflow, the `fveil.clear` idiom scoped to our own organs).

**The cancel machinery holds through all of it:** every beat rides a flushed `timers[]`, with a `hashchange` listener, a live `matchMedia` re-read + `change` listener, and a self-contained 12s belt in the arm (the only failsafe that survives a dead main script — **and more load-bearing since the bake, because what it guards is now the default arrival rather than a param somebody typed**; the derived clear lands ~5s in, so 12s never races it). Cancel lands the video **at rest, never half-slid**, takes the clamp's node back if it had not landed, and since the bake also puts the SMIL clocks back. The arm's belt is shared by every ride on an armed stage, so a slowed replay still running 12s after LOAD gets swept — bench-only, noted at the belt. **Both cancel doors measured at the bake** (cut at t≈1500ms, mid `land-pose land-shrink land-bar`): hashchange and a resize under 941 each left zero land classes, a cleared `#fveil`, the clamp node gone, the video at rest (opacity 1 / transform none), the tick running, the brand's inline transform cleared and the clocks back at base.

**THE RESTORE HALF-ERASED THE MARK — drawRestore now ends in a HEAL SEEK (2026-08-22 evening, Dylan's catch: "half drawn" after the landing).** The bake's restore assumed rewriting `dur` back to base re-resolved the frozen draw against document time — Blink instead keeps a frozen animate's elapsed ACTIVE TIME through the rewrite, so a pen frozen at the scaled end (active 1.025s) re-froze at 1.025/2.05 = **exactly half** once base clocks returned, and it never rejoins a still-live interval. Every completed ride since the bake ended with the mark half-drawn until the next `playLogo` rewind healed it (the bake verification measured the attributes after the clear, not the ink — and the ride itself always looked right, which is how it hid). Nudge seeks do NOT recompute the stale value (measured: ±1ms..±500ms, same-turn or deferred, all no-ops). The one reliable heal, now the foot of `drawRestore`: **`setCurrentTime(0)` then `setCurrentTime(9)`** — the rewind resets every element to unbegun (stale active time cannot survive it), the jump past end is playLogo's own reduced-motion idiom, and both freeze values are clock-independent so no pending-rebuild race can corrupt the ink. Timeline parks at 9s, which every arrival's rewind already owns. **Behavior change ridden in deliberately:** a cancel mid-draw now COMPLETES the mark instantly instead of "finishing at the shipped pace" — that promise was never real (Blink froze the pen at half there too). Measured after the fix: ride ends 0px ink, resize dance across 941 keeps it, mid-ride cancel lands the finished mark, mid-session return replays and finishes, zero console errors. His "I set the draw too long" self-diagnosis was innocent — the bug lives at any drawSpeed ≠ 1; the baked tune did not move.

**THE DERIVED CLEAR COUNTS BOTH CASCADE ORGANS (T2 bench-find, 2026-08-21).** `endAt` took its cascade term from the carousels alone, on the assumption a section's carousel always lands after its header — at bench-reachable values it doesn't (`sections[1].head` 1500 with `headDur` 910 ran ~90ms past the clear and read as a cut, measured −88). `lastHead + headDur` is now a term beside `lastCar + carDur`; shipped values are unchanged (both land at 2120 past `cascadeAt`, clear ≈ 5.97s). The arm's comment says the clear is derived, not a constant.
