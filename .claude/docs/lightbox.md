# The lightbox — inspection view
fv3 lb card, the sun, dress regime, landing/sound, undersides carousel, the deck (neighbours/exchange/deal), deck header, collection strip.
> Carved verbatim out of the old monolithic CLAUDE.md on 2026-08-12 (full backup on Dylan's Desktop + in git history). Wording is preserved exactly — section headers were added for navigation only. New decisions on this surface get appended HERE, not to CLAUDE.md. Cross-references like “see X below” may now point at a sibling doc in this folder — check CLAUDE.md's index.
> **PORTED TO v1 2026-08-16** — everything below described `redesign/k-home-dual.html` in the vault repo; the page is now this repo's ROOT `index.html`, with media repointed (`../site/`→`site/`, `card-lab/canva|cuts/`→`site/img/cards/canva|cuts/`, `media/`→`site/media/`, `../videos/web/`→`site/media/`). Every named lab page stayed behind in the vault's `redesign/` — a "(vault)" mark says so at each mention, and the unmarked plain "the lab" readings mean the same thing.

## The fv3 lightbox: card, nav arrows, sun, dress regime

While frame v3 is selected, the lightbox becomes the frame v3 card blown up (`#lb.fv3` CSS + `.d7f` class on `#lb-card`, so the delegated hover choreography — rails, chip wipes, ref/name/status decodes — runs on the big card): cutout hero on open, product box slims to the DM cta. (The thumb strip and the arrows' photo-stepping RETIRED 2026-07-26 — see THE LB DECK below; arrows are board nav now and the real photos are unreachable from the lightbox by decision, Dylan being "not too precious" about them since they are the same board on a different background.) Nav arrows live ON the card at the mid-rail dots (sprint item 1, done): seat = flush square chip tucked to the rail (DECIDED — empty circles clashed with the card's angular language; the fill-disc variant was built and rejected). The desktop lb also runs the **lb sun** (2026-07-22): cursor-as-light-source over the card — the board's hard shadow throws away from the cursor through the board, and the board itself strains toward the hand. FIVE channels (`SUN` knobs by `sunFrame()`, all ref-px so one tune fits every card size): DIRECTION (shadow 2–17 by cursor distance, fast ease — the angle snaps to the hand; 0 at dead center, the overhead vanish) + LIFT (+7 shadow bonus swelling on its own slow ease paced to the board grow — the old hover's depth cue, drains fast on leave; it DUCKS below `duck` ref-px of directional length so a center-cross slides the shadow UNDER the board instead of whipping a lift-pinned 7px shadow around it) + PULL (gravity as TWO always-on springs, no park detection — a fast ARRIVE (8) and a slow DRIFT (6) both chase the cursor with targets ramped by d^1.7 (gentle mid-card, steep rim — the boost lives at the outermost edge, parked rim total ~24 ref-px). In motion the drift trails far behind and reads as weight; parked, it keeps closing the last distance — the creep IS the lag, no on/off seam. GROUNDED: 85% of the pull is subtracted from the shadow offset so the shadow stays planted on screen while the board slides over it — the drift is board-vs-shadow parallax, not the unit dragging its shadow along) + GROW (the 1.08 board scale, JS-owned while live since pull rides the same inline transform; the CSS :hover grow remains as the reduced-motion/pre-takeover path) + FADE (alpha follows cursor distance d, NOT drawn length — the lift floors the length and capped the dark: .53 dense contact pool at the overhead vanish → ~.35 mid-card (aPow .7 keeps the pool a near-board thing) → .19 faint at a full graze; settles the nervous near-center slivers by making them read as weight. Dylan flagged low confidence on the .53/.19 pair 2026-07-22 — expect retunes). Shadow totals ~11 near the board, ~17.5 mid-card (= the old fixed hover), 24 at a corner graze. Seeds shadow AND pose from computed styles so mid-hover-grow takeovers are seamless, eases home to the house (7,9) diagonal on leave and hands everything back to CSS (`.suntrack` sets `transition-property: none` on the img while live — transitions restarted per pointermove trail the hand; `closeLb` force-releases before the return flight). **The SUN TUNING LEGEND banner comment at the `SUN` const documents every knob, its unit, and which way to turn it — start there for any feel adjustment.** One gesture on card hover (~.96s, board+shadow growth spans it), **rebuilt 2026-08-01 so the line HANDS OFF to the box**: rails close onto the dots → line grows from each dot (SVG dash, `.34→.48s`) → the black box wipes out of where the line landed (`chipwipe .48→.78s`, growing from the rail side) → the chevron draws inside it (`.76→.96s`, once there is black under it). Opacity is a fast early ramp (`.3→.42s`) that must finish BEFORE the fill starts or the wipe plays at partial alpha; nothing is visible during it, so it costs no beat. **THERE IS NO REST STATE** — Dylan, *"remove the icon pre draw (the faded state)"*: at rest the arrow is opacity 0, fill unwiped, chevron undrawn, so no ghost control sits on the card waiting to be used. It is BUILT by the gesture, in the order the eye follows. The old `.sq` half-rects that drew a seat outline are gone too (a box around a box, same reason `.bay-navfx` went). Exit drains vertically into the frame (chipdrop via the shared `.left` class; prev up, next down, keeping the card's diagonal). **Arrow's own hover TIGHTENS, it no longer inverts (2026-08-01)** — Dylan carried the bay-close react onto the card ("the fill unfil on those arrows were always going to get tweaked or replaced"), so the black seat shrinks in around the glyph and the glyph never moves or changes colour; the old white fill, black glyph and inset ink ring are gone. `inset`, NOT a transform — and here that is load-bearing rather than merely tidy, because the seat's transform belongs to `chipwipe`/`chipdrop` with `forwards` and a filling animation outranks anything a transition could write there. The shrink is PROPORTIONAL, not the drawer chip's flat 2.5px: this seat is cqw and scales with the card, so it is the same fraction of the box (3px at the 430 reference = `.6977cqw`, px line first per the ONE CARD SPEC). **THE SEAT IS THE DRAWER CHIP NOW (same day, Dylan: "the same sq boxed arrow as the drawer... same black box breathe on hover, the only slight difference being the line still connects the box to the inner card frame").** The `‹ ›` mono glyphs are gone for lucide `square-chevron-left`/`-right`, and `.bxi` is SHARED with the drawer so the two controls cannot drift apart — **but the nav carries the CHEVRON PATH ONLY, no rect** (Dylan, same session: *"no black border, but a black inside"*). The drawer keeps the whole icon because its chip is a white glyph on black; here the box IS the `::before` fill, so a stroked rect would be an outline drawn inside its own fill. Consequence: `.bxi` is a shared STROKE SPEC, not shared artwork — the two svgs differ in content on purpose. Colour is FLAT WHITE with no transition, since the chevron only ever exists on top of the black fill; that also removed a timing dependency, because the old colour ramp had to finish before the glyph appeared. **The connector stops 1.5086cqw short of the seat, which is the card's OWN rail-to-dot clearance** (rails end `2.5862cqw` shy of centre around a `2.1552cqw` dot) — his ask was that the gap "match the look of our card dot line connection to its side", so it is derived from that geometry rather than eyeballed; measured live at 1.508cqw both sides. The drain keeps the chevron DRAWN (`.left` pins `stroke-dashoffset: 0`) exactly as the header engulf freezes its children: the assembled chip falls as one object, and a glyph un-drawing itself mid-fall would read as a second gesture. **This REVERSES the square-seat note that stood here hours earlier:** that reasoning was "no rounded icon to match", and now there is one, so the corners soften. The disc rejection still stands — this is a soft square, not a circle. Radius, icon size and inset are the drawer's PROPORTIONS rather than its pixels, because this seat is cqw and scales with the card: radius 5/32 of the box, fill inset 2.5/32, icon 24px (1:1 with the 24-unit viewBox, so strokes land crisp at the reference). px line first per the ONE CARD SPEC. **THE LB DRESS REGIME (2026-08-01, settled across three of Dylan's calls in one session — read the final shape here, not the interim commits): TWO ARRIVALS, TWO GRAMMARS.** **From COLLECTIONS the draw rides over** (his words: "in collections the card is already drawing its connecting lines and fill boxes, so once in lightbox view it should just stay drawn (or complete its draw on the way there)"): `openLb` adds `.stay` + `.dressed` + `.navdraw` — `.dressed` renders every fill pre-finished from frame one (the `.held` negative-delay idiom) so the dress rides the open flight with zero replay, the source card's four texts ride over (`copyCardTexts`), and `stayLand()` completes any decode the click interrupted: only reads DIFFERING from the landed targets decode, so a carried "AVRG" never re-scrambles. The delegated decode handlers skip a stay'd card BOTH ways — no re-decode on entry, no `.left` drain on exit — and the `.dressed` rules outrank the `:hover` mirrors with constant computed values, so hover toggling can never restart anything (the `.hold` lesson made permanent; the chip's accent rung is re-mirrored above the dress whites, scoped on `.stay` so it wins over `.dressed` and `.redress` alike). **The ONE exception is the board: grow, panel shadow and the sun stay hover-live** — the card still answers the hand, nothing else moves, and a cursorless landing leaves the board at rest scale because grow answers the hand, not the landing. **ON A ROTATE THE LANDING IS THE ENTRY** (his flow, restored from the arrow-button pass and made cursor-free: the card "clicks" into center and the redraw starts at that beat — it used to require the cursor sitting on the landed card): the shuffle stays what it always was, PLAIN clones conveying (a first cut dressed the clones mid-flight — reverted, "the way the cards animated in light box, from the shuffle to the center was fine as it was"), the whole dress leaves with the hidden card (every fill class shed at exchange start; the teardown transitions play out invisibly behind the visibility), and the landing frame adds `.redress` + `.navdraw` so the arriving hero plays the ENTIRE entry with or without a cursor — rails close on the base .56s transition, fills wipe from scaleY(0), tag draws, arrows assemble (opacity .3 → line .34 → box .48 → chevron .76), texts decode. **`.redress` SHARES `.hold`'s ruleset** (one selector list, two names — both are "the hover build without the cursor", and a separate copy could drift, where any computed difference blinks); a cursor that IS on the card at the reveal computes identical values through `:hover`, so the build plays exactly once either way. **`stayLand()`'s changed-only rule yields both behaviors from one law:** populate resets texts to print before every landing, so a rotation decodes everything, while the from-collections open (texts carried) decodes nothing. BOTH landing paths re-add the build — the exchange timeout AND `xactSettle`, which is load-bearing: `step()` settles BEFORE its end clamp, so a rapid press at the collection end produces ONLY the settle landing. `xactReveal` deliberately does NOT re-add (`closeLb` also reveals; a close mid-shuffle flies home bare — the dress left with the card). `shelfToLbArc` keeps its fly dressed on desktop (it lands on a `.dressed` card; touch keeps the mid-air drain — its card is still mid-tset-wipe at touchdown), `buildFly` treats `.stay` as a held source (an Esc-close with the cursor off-card still flies home dressed), and every state class is shed at hide alongside tset/hold. **EVERY ARRIVAL AT CENTER SEATS WITH ONE `kclick`** (Dylan, same session: the collections open "deserves the same sound as a side card ... taking center stage — it feels kinda naked in comparison"): the open flight's landing (tokened 430ms — voided by a close or re-open inside the flight, `lbFlying` blocks it during a return) and the shelf→lb arc's landing now fire the exchange settle's voice; the close keeps `landBeat`'s own click, no doubles. Touch is untouched: the dock gate keeps tset. Verified in real Chrome by event listeners, not sampling: zero animation/transition events across real hover cycles on a settled card, a strict cursor-free rotate (`:hover` false throughout) builds identically, and a Web-Audio spy counts exactly one voice per arrival with interrupted opens silent. Stock pucks hide in fv3. Other card styles keep the stock lightbox on purpose. Light-mode logo handling: the bar swaps to `logo-circle.png`, the masthead keeps `logo-line-white.png` under `filter: invert(1)`. 

## The landing, sound kit, undersides carousel, LB deck & deck header

**THE LANDING (2026-07-22): the close flight arrives intact and ends in a button press.** `closeLb` no longer sheds chrome at close start — `lbFlying` gates the delegated decode handlers (the card sliding out from under the cursor is not a hover exit), `.tset` rides until the lb hides, and `.hold` (added when the pointer is still on the card, i.e. Esc/scrim close) pins the hover-landed fills through the flight. GOTCHA: `.hold`'s rules mirror the `:hover` rules **VERBATIM** — any computed difference, even an animation delay, restarts the fill animation and blinks it mid-air (this is why `.tset`, whose fills carry a .22s delay, can't double as the hold). Touchdown = `landBeat(backCard)` inside the close timeout: `.landpress` on the slot card's panel (ONE transform presses card + board + both shadows — depth motion only, per the card-shadow law), `kclick()`, and a LAND-row flourish — fixed-position one-shots measured off live rects at touchdown, cleaned by animationend + a timeout fallback (a tab hidden at the landing frame freezes CSS animations and would strand them). Sound kit by `kthunk`: `kctx()` shared context (+resume for sticky activation — the voices fire outside the click's gesture window), `kthunk` = dock seat, `kclick` = button snap (10ms bandpassed noise tick over a small triangle pitch-drop). **LAND is NONE — PICKED 2026-07-26 and the row RETIRED: the landing is the press and the click, felt rather than annotated.** Both flourishes (pad = a line flashed under the slot, flash = the panel border redrawn) are DELETED along with `avrg-k-land`, `.landfx-pad`/`.landfx-flash` and the `landpad`/`landflash` keyframes — and with them the measured-off-live-rects + animationend-with-timeout-fallback dance that existed ONLY to stop those fixed-position one-shots stranding when a tab hid at the landing frame. `landBeat` is now just `.landpress` + `kclick`. SND (`avrg-k-snd`) **lost its UI on 2026-08-01 but kept its gate**: Dylan retired the row with the switcher — *"it will get a site button in time"* — so every voice still checks `SNDON` and a muted browser stays muted, but there is nothing on screen to flip it until that real button is designed at the audio pass. Sound-on is the DEFAULT, not an override. **UNDERSIDES — THE FLIP IS LIGHTBOX-ONLY (kick RETIRED 2026-07-26).** The lb card runs the `carousel`, triggered by its board-image click or its `words` indicator, with the name⇄UNDERSIDE decode riding along. The grid/shelf `kick` and the click-split that routed to it are DELETED — Dylan: *"i dont think kick belongs on a collections board click. it gets in the way of the lightbox / could inadvertently hide a card click to inspect."* Every collection surface (grids, belts, bay shelves, flights, lb neighbours) is plainly click/tap-to-inspect. Gone with it: the whole `.stage`-scoped kick apparatus (gesture-scoped perspective/preserve-3d, the `.fset` 2D re-expression, the flipland press), the two-face flipbox in the rendered template — **rendered cards now carry ONE image instead of two** — the `boardHit()` alpha probe on grids, and setFlip's kick re-arm. `boardHit()` itself SURVIVES: the lb board-image click still uses it so transparent air around the board doesn't flip. `@keyframes flipland` moved next to the lb's landed rule, its only remaining consumer (deleting it in place would have silently broken the lightbox's landing press — it was defined in the kick block but used by the lb). What made this a clean removal is the stage/frame split: kick was `.stage`-scoped, the carousel is `.frame`-scoped, and they never shared a rule. `BOT()` (next to `CUT()`) resolves the canva pair's OTHER face — hs2/3/4 show their `-bottom` as the card FRONT, so their flip shows the `-top`; the words call the front "Top" by display convention (relabel only if it ever matters). No pair (originals/resale) = no `.canflip`, no flipbox, no words. **THE 3D IS GESTURE-SCOPED (load-bearing): at rest every card is 2D** — bot face `visibility: hidden`; 78 resting preserve-3d/perspective contexts black-wedged a compositor — `.flipped`/`.unflipped` mount perspective + preserve-3d + backface, and landing adds **`.fset`, the static 2D re-expression of the flipped rest** (kick: flipbox `transform: none` + face swap via visibility and `--face: 0`; lb: `animation: none` rules) — REQUIRED on the lb because a both-fill animation outranks the sun's inline writes, and on grids it drops the 3D context. `setFlip` re-arms an fset unflip (inline transition mute + forced reflow) so the return animates from 180, not from a none-jump. Face transforms COMPOSE from `--face`/`--bgrow` vars (parked rotation and hover grow share one property); the `.sold` grow-kill is `--bgrow: 1` for `.d7f` while `:not(.d7f)` keeps the old `transform: none !important`. **CLICK SPLIT — RETIRED 2026-07-26, see UNDERSIDES above. Kept for the reasoning only.** (Was: board silhouette = flip, everything else = inspect — a late-registered capture click handler routes through `boardHit()` (64px canvas alpha probe of the VISIBLE face; **the outside-rect early return is load-bearing** — without it the clamp maps stage air onto the board's opaque edge pixels and every stage click flips, bitten). Scope: `.scard` grids only (lisst included), `canHover` only — belts/case/flights/touch fall through to inspect untouched. The `.find` words (panel child, top-right; `.tspark ~ .find` drops below the ✱; hidden on `.card`/`.bay-item`/`.bay-fly`) are clickable flip controls. **LB carousel:** the lb flipbox is `display: contents` — no box, no stacking context, so the round-7 preserve-3d trap CANNOT exist and the grid's `.stage`-scoped kick rules never reach it (the stage/frame split IS the physics split, no !importants); `#lb-bot` rides in the frame; choreography (Dylan's landed sketch, re-drawn 2026-07-26 — the lab's symmetric far-station mirror was code drift, not the pick): OUT = across the face in front → fully outside the LEFT border (−63%) → swallowed back to the parked sliver (−6%/.46); IN = rightward hidden behind the card → progressive emergence through the RIGHT border (47→58% — the border is the reveal, the half-out pose IS the beat) → around the edge → in front over the face straight to center, one continuous sweep, no far station. **THE LEGS RIDE OVERLAYS (2026-07-26, the clip saga): Dylan's live Chrome clipped in-card legs at the card box while every isolated primitive passed headless probes on his exact binary (stale-clip/raster-cull family, root cause never pinned) — so the gesture runs on CLONE faces in `#carback` (z0, under the card z1: the opaque card swallows by plain painter's order) and `#carfront` (z2), positioned over the frame's live rect by setFlip, visibility-swapped mid-keyframe at the layer beats (the carOutF/B + carInB/F quartet; landing = carInF's animationend → flipLanded(lbCard)). No overflow window (the panel's load-bearing hidden overflow is back to FULL-TIME), no z-index inside keyframes; the REAL faces hide under `#lb.caring` and take back over at the `.fset` landing; only the resting parked pose keeps the z−1-behind-panel trick (static, probe-verified on his build). **SHADOW LAW (Dylan, 2026-07-26, learned the hard way): the card + its hard offset shadow are ONE OBJECT** — the black slab is the card's BODY in the fv3 language, not a ground shadow. Back legs paint UNDER both (the board emerges from behind the whole unit); front legs ride over both. A `#carshadow` slab that re-homed the shadow below the back legs (board-over-shadow "physics") split the object — the board slid into the seam between card and slab — REVERTED same day; what actually kills the shadow-band-cuts-the-board read is the per-board cleared stations. Any future between-layer (a mid z between card and its shadow) is wrong by law. The outgoing's retreat is retimed to linger beside the card (50→70% linger, 70→100% retreat) so both boards move through the exchange together — the lock-step law; the stations are PER-BOARD CLEARED (`--carox`/`--carix` = card half + that face's `offsetWidth` × .31 + 26px): each leg's outer stop fully clears the card AND its shadow band — no more tail-straddling-the-shadow dwell (Dylan's graze catch; offsetWidth is layout width, immune to sun/hover transforms; the widen is lb-only breathing room by his call); per-keyframe `animation-timing-function`s keep each leg ONE glide (every keyframe boundary re-eases and reads as chop — the emergence is a single 0→62% ease-out); clones preload at populate + `will-change: transform` (first-gesture decode reads as chop too). `src=""` on an img requests the PAGE — clear face srcs by removeAttribute.** ALL travel is `calc(var(--carw) * factor)` px (setFlip measures per gesture, sets the var on #lb) — NEVER cqw, which dies inside keyframes on some builds (see Gotchas); `.flipping` gates the sun + re-clicks; `#lb.fv3 { overflow-x: hidden }` keeps the legs from growing the sheet a scrollbar; the landed press rides the PANEL with the parked face `visibility: hidden` for the .22s (a transform animation makes .panel a stacking context, which would hoist the z−1 sliver over its own white). **The lb board img click is the FLIP while `canflip`** (cutout view only — populate arms it at j===0 and swaps the img title); photo views and bottomless boards keep the old click-to-collection departure. **SUN:** `sunImg` (re-aimed at flip landing) replaces the hard `lbBig` refs; `setFlip` force-releases, `.flipping` gates pointermove, landing hands the sun the new face. **RESETS — every flight flies top-up:** populate (each step), openLb's sourceEl, closeLb (a close-while-flipped pops top before the return flight), thumbs click (a photo disarms the flip), the chip-pluck capture, dragLift. Landing events keep the lab's split: kick = flipbox transitionend (transform), carousel = carIn's animationend. **THE LB DECK (2026-07-26, stage 1 of Dylan's lb rework — `LB` switcher row, `avrg-k-lb`, `deck` default / `thumbs` = the pre-rework lightbox verbatim for A/B):** the thumb strip RETIRES — the flip owns face-switching now, and `step()` had been double-jobbing as a photo stepper (walking the current board's photos first, crossing boards only on running off the end), which is the duplication the deck removes. In deck mode `step()` is BOARD nav only (`j` forced to 0), so one arrow press = one board = one neighbour exchange, which is what makes the header honest. **The x-of-x header** (`#lbhead`) is the old `.lb-count` promoted: same facts, but stacked (collection name stays the 10px `.label`, position at 18px centred beneath — Dylan's pick), visible on EVERY size (the count was bottom-fixed on desktop and `display:none` under 720), `position: absolute` and JS-placed 22px above the card so it travels with the card through the sheet scroll instead of hanging in the chrome. Because it lives above the card, the CARD'S OWN TOP PADDING reserves its room: roomy sheets keep the base 24px and the card doesn't move, but on a short viewport `margin:auto` collapses, the card rides at the sheet top, and clamping the header up there painted it ON the card (measured at 560px tall) — so `placeDeck` adds the deficit to `padding-top` instead (the sheet already scrolls at that height, so it costs nothing). The header also freed the corner ref chip: it had been hijacking itself into an `n/N` decode on touch ONLY because nothing else did wayfinding there, so touch now matches desktop's ref→AVRG. **THE DECK HEADER — REWORKED 2026-08-01 (v1 list item 2, "gets tweaked"). It is no longer the collection name over a fraction; it is the BOARD'S NAME over its position:**


    HS 15        36px, ground ink
    8 / 14       20px, dimmed by opacity


The tag is `refOf()`, the SAME string the card's corner chip prints, so the header and the card can never disagree about which board this is; the collection name it replaced is carried by the ref's prefix. **This stacked because the tag and the position are DIFFERENT NUMBERS** — refs are non-contiguous by design (see the inventory-ID thread: classic position 1 is CL 3, hand-shaped position 5 is HS 10), so a one-line "HS 10 / 14" put an honest tag beside a total that no longer said where you were. Shown that collision, Dylan took both. **TWO COSTUMES WERE BUILT AND BOTH REJECTED** — a filled black chip and an accent-outlined soft-square box (radius 5px, the bay-close/nav-seat number) — his call being *"not especially stoked on either. lets just keep it simple. no colored text. no black back drop."* **The hierarchy is carried by SIZE alone**, and the reason to keep: both costumes were spending a DRAWN OBJECT on a label, and this page already has its drawn object — the card. The dim on the position is opacity, not a colour, so it reads the same on either ground. **PLACEMENT IS PROPORTIONAL:** `NB_TUNE.headAt` (.667) puts the header's CENTRE at that fraction of the gap between the sheet top (the topbar's underside) and the card's top edge, with `headClr` (12) as the minimum clearance at both ends; the old fixed `headGap` is gone because a px gap sits somewhere different in that space at every viewport height. The gap's minimum is `(headClr + H/2) / (1 - headAt)`, taken out of the card's top padding — **add only the MEASURED shortfall per pass, never a predicted one**: the card is `margin:auto`-centred, so padding moves it P/2 while free space lasts and P once the margins collapse, and solving for either regime analytically overshoots badly in the other (a 2× first pass computed 272px where 165 was right and hung the card off the sheet). `headTop` is also clamped, so a short viewport degrades instead of painting the header on the card. **THE TWO NUMBERS COUNTER-ROTATE ACROSS A SHUFFLE** (Dylan's choreography): the board number exits UP and enters from the RIGHT, the position number exits DOWN and enters from the LEFT — opposed on both axes, which is what stops the pair reading as one block sliding. Only the NUMBERS move: the prefix and the total are constant inside a collection (the deck's ends clamp rather than wrap) and animating an unchanging character is motion with nothing behind it. The swap spans exactly one shuffle — exit from the press, new values landing with the card ("start on card swap and end on new card land", measured 444ms against the card's 439) — split by `NB_TUNE.headOut` (.42) and tokened on `xactN` with `headClear()` in `xactSettle` and `closeLb`, so an interrupt cannot strand a number translated at opacity 0. **NOTE THIS IS NOT THE FADE HE KILLED.** The static ruling (*"right now the counter fades between card rotations. lets make it static"*) was about the whole header blinking out and back on its opacity transition — it used to sit the shuffle out with the rest of the furniture, and coming back re-triggered the open fade. The header block still does not move, fade or reflow; the digits inside it change with a gesture. Different object, different verb. **AND THE DIGIT SLOTS ARE RESERVED** (`min-width: calc(2ch + tracking×2)`, value centred): the static parts were never animated, they were being REFLOWED — a centred line narrows when 18 becomes 4 and drags the prefix and the total sideways, by different amounts per line since the two sit at different sizes. The label font is monospace so 1ch IS the digit advance, but CSS puts a letter-space after EVERY character including the last, so N digits occupy N×(1ch + tracking). Two digits covers the inventory (highest ref CL 27, largest collection 19); a three-digit board wants 3ch. The background fade behind an inspection went `.22 → .12 → .08` across this same session, the last nudge because the header's new seat lands on the collection page's own ghosted title. **THE COLLECTION STRIP — PORTED 2026-08-12 (strip-lab (vault) → k, Dylan: "lets give it a go on the actual site"), and THE DECK HEADER'S TAG LINE RETIRED WITH IT** ("remove the HS03 id tag... the new card row can take its place"): the whole collection strung as mini cards above the counter — `#lbstrip` + `#lbslane` (markup after `#lbhead`), engine after the `placeDeck` resize listener, knobs+legend at `STRIP_TUNE` (mini 46 · gap −8 · pop 1.45 · lift 18 · foff/fade · maxw .86 · dur/hdur/sit 900/400/250 · stag 50 — Dylan's slider-screenshot clock, same day · drop 26 · clr 26 · gapB 46 · scoot 60 — his "lower + breathing room" pass). **Strip-live, the header block is TOP-ANCHORED chrome (fixed stack: headClr+drop+band+clr, gapB minimum below), replacing the proportional headAt seat which still governs when the strip stands down. And THE DEAL IS ONLY SPENT UNDER `.in`** — an early RO placement played the whole entrance at opacity 0 and nobody ever saw it (the reason his "use the DEAL animation" ask existed); sFresh parks until the sheet's fade is live, and a same-collection reopen re-arms it (`lb.hidden` at populate = the open path). **TWO CLOCKS (same day, Dylan: "not firing nearly as cleanly as the lab"): a PRESS splits the exchange's xDur exactly as headSwap does (sit 176 / lift 244 — the pop lands WITH the card; the single 250/900 clock had it trailing the hero by ~700ms), the HAND keeps the lab's 250/400 — stripTo writes `--ssit`/`--slift` per gesture, phases ride `.sitting`/`.lifting` (source order after `.hovlive`, later phase wins). And a deal's stagger transitionDelay is force-cleared on any non-enter pose — the transitionend cleanup can miss and a leftover delay lagged the first riffle.** **THE HAND GLIDES, THE PRESS SEATS (same day, his second catch: "you can skip cards that dont animate at all"): the seat driver is one sit-then-lift at a time, so cards swept past mid-cycle never animated — even a slow sweep outran the 250ms sit. The riffle now retargets INSTANTLY per move (glide cross-fade on the hovlive clock, a pending press sit yields to the hand); the seat grammar belongs to the press alone, where the swap-clip actually mattered. Verified: 10/10 swept slots clearly raise, 0 dead. The lab got the same fork (SWAP governs its STEP only).** **THE MINIS WEAR THE DRESS (same day, Dylan: "use the decoded version of the cards"): the `.bay-fly.held` rest-dress idiom verbatim on `.lbsmini` — fills pre-finished, glyphs white, rails closed, mark shadow flipped — because the two chip fills ARE the stack's wayfinding: center z-order peeks the left flank's top-left bars and the right flank's bottom-right bars. Pop 1.9 → 1.45 in the same pass.** **SITE-SCALE LEGIBILITY (same day, his lab-vs-site screenshots: "the tops of the cards are hard to see"): a pure photographic reduction puts the big card's 1.5px edge at a third of a pixel at 46px — the STRIP COSTUME overrides (scoped `.lbsmini`, cqw so still photographic): panel + inframe borders 2.1552cqw (1px at 46, the lab minis' own proportion), board bumped to 93/92% of its stage. The big card's spec untouched — the general law: a photographic reduction is honest but sub-pixel furniture is not; tiny renditions may re-proportion their INK, never their geometry.** **THE DEPLOYED-REACT PASS (same day, his screenshots): (1) the minis' rest-DRESS came OFF again — at 46px both chip bars showed on every card, muddier than the lab's one-bar-per-flank read; minis now match the flankers' bare rest (the dress pass lives at git 6d42983). (2) THE INSPECTION'S SHADOW GOES SOFT, desktop-scoped: his settle-pop catch — exchange clones wore the hard slab while the revealed hero wore slab+ambient, so the landing swap snapped. Hero + flankers + clones (one `.lbnb`-inclusive selector) share a single faint cqw ambient throw (0 4.186cqw 11.163cqw .35); the hard slab stays the card's body everywhere else (grid, strip, PHONE — the mobile softout drain interpolates its two layers and must keep them). Consequence: `SX = 0` in deckGeom (the shadow-edge gap law's premise is gone here; derivation kept in the comment). (3) flankers out to gapX 44 — the hero's rail sits about where the neighbour's board would start. (4) the fv3 lb's Interested?/DM cta retired (`#lb.fv3 .lb-cta`); stock lb keeps it. NOTE: his message truncated mid-sentence after the cta ask ("…below the light box card and") — something more was coming, unasked (he later confirmed the thought was lost to a trash run).** **DESKTOP SIGNED OFF at wrap (2026-08-12: "really like this change... solid on the desktop").** 

## Strip minis, riffle grammar, neighbours & the deck in motion

Minis are the REAL fv3 template at the 232 reference, transform-scaled per pose (lockCardScale's font-clamp duck; one spec, no drift) — QUIET objects, every pointer job on the ONE static lane (the moving-hitbox law; lane pads by the popped end-card's overhang, index by clientX vs live rect, never offsetX). THE RIFFLE + SEAT grammar: sweep raises the card under the cursor, the raised card SITS before the next lifts (`stripTo` — `sLifted` trails `sFocus ?? sPend ?? cur.k`; a sit in flight never restarts, its fire re-aims; z swaps at rest); click jumps — adjacent rides the conveyor, FAR jumps ride the stock slide-fade (the four-body exchange is a one-board grammar); cursor-off hands the pop back. The TAG is CSS-hidden (`#lb .lbhead .lbh-tag`), NOT deleted — setHead still writes it (one write path) and headSwap's cleanup re-pointed lbhNum→lbhCur (hidden elements never fire animationend). ROOM: deckGeom budgets `stripBand()` into `need` and the headTop clamp floor (header degrades BELOW the strip), and the SCOOT (24px standing pad while the strip is up) answers "scoot the big cards down a bit" — margin:auto splits it. DECK-HELD BOARDS: no slot, no ghost (`.lbsmini.held` display:none), EXCEPT the inspected board itself (case inspections see themselves — the flagged edge resolved); `nextVis()` walks steps, FLANKS and the exchange's enter/exit actors past held boards so strip/flanks/arrows never disagree, and the counter number-jumps (verified live: held 1/3/5/8 → 2→4→6). GATES: `stripLive()` = deck+fv3+`!dockActive()`, the display rules carry the 941 media gate (stale posed minis must never PAINT on the dock side), and a matchMedia boundary listener clears/rebuilds mid-inspection. `stripReset()` in closeLb. Verified real Chrome 1440: open/step/riffle/far-jump/Esc-reopen/boundary-crossing/both grounds. **NEIGHBOURS KEPT deliberately — Dylan's three asks didn't touch them; thin only on his react.** **NEIGHBOUR CARDS** are the shared `JCARDS.framev3` template at a smaller width — `.d7f` is a cqw container carrying `width:100%`, so a neighbour is an exact photographic reduction with no override block, the same free ride the bay shelf cards get. Knobs + legend at `NB_TUNE` (desktop `scale .42 · gapX 18 · drop .46 · fade .85 · hov 1 · lean 8 · headGap 22`; a `mob` sub-object merges under 720). **Z-ORDER IS LAW: neighbours sit UNDER `#carback` by SOURCE ORDER, never a negative z** — a negative z falls behind `#lb-scrim` and eats their own clicks. Stack is `scrim < neighbours < carback < card < carfront`, so the flip's back legs are still swallowed by the card's body by plain painter's order and the front legs sweep over everything; nothing may sit between the card and its shadow. They are `position: absolute` SIBLINGS of the card, not children — children would ride the open/close flight — and `placeDeck` positions them off the card's OFFSET box (transform-immune, so the sun, the hover grow and mid-flight poses can't move them, and there is no settle timing to get wrong; `offsetIn` sums the offsetParent chain up to `#lb`). Placement hooks: an RO on the card (reference HELD — an unreferenced observer is GC-eligible), a `resize` listener, and a deterministic call inside `openLb`'s rAF (populate runs while `#lb` is still hidden, where the card measures 0 and placement can only early-return). Neighbours are QUIET objects: `pointer-events: none` on everything inside (so no delegated hover choreography and their +List chip is not a live button — a neighbour add is undesigned), the click is the wrapper's and means "step to this board", and their Top/Bottom words are suppressed alongside the other quiet-card surfaces in the `.d7f.canflip .find` group. **THE ENDS CLAMP, they do not wrap** (the old arrow wrapped): a wrap would put board 1 beside board 14, or the SAME card on both flanks in a 2-board collection, breaking the one-card-per-board law that deck holes exist to enforce — but "a live affordance must never play dead" forbids a silent no-op, so the end is STATED TWICE, an empty flank plus the arrow at the house `.22` faint (`.lb-fv3nav.off`, which must outrank the card-hover reveal). The clamp lives in `step()` too, so it holds even against a programmatic click that bypasses `pointer-events`. **THE DECK IN MOTION (same day, Dylan's law: "if the cards dont move like cards then it breaks the whole visual language"** — every other object on this site earns its position by travelling to it, so a neighbour that simply APPEARED read as scenery; a fade was the one arrival grammar nothing else on the site used). **`deckGeom()` is the SINGLE geometry source** — it returns the three seats (`H`/`L`/`R`) plus the off-sheet staging boxes and the behind-the-hero deal pose; `placeDeck()` applies them to the resting elements and `deckExchange()` interpolates between the same boxes, so a knob change moves the rest pose and the motion together and they cannot drift. **THE EXCHANGE is a four-body conveyor, one press = one shuffle:** the hero demotes to the far flank, the near flank promotes into the hero seat, the next board along enters from off-sheet, and the board falling off the back exits — the cast drops to THREE movers at either collection end (conditional, guarded on index). Every mover is a CLONE off `JCARDS.framev3`, rendered at the HERO's width and transform-mapped onto its start/end seat (`.lbnb.xact`, `transform-origin: 0 0` — that origin is what makes `translate + scale` an exact box mapping, and rendering every clone at one size means a single reduction path). Clones are why the real hero and neighbours can repopulate underneath and be revealed at landing, and they keep the **contested `#lb-card` transform out of the choreography entirely** — `sunRelease()` plus a silent flip reset run first, because the sun writes inline transforms and a settled flip holds one with a both-fill animation that outranks inline writes (the discipline `closeLb` already runs); the clones always render the FRONT face, so the reset is also what keeps them matching what is on screen. Actors are inserted before `#carback` to inherit the neighbours' stacking slot. **`xactSettle()` IS LOAD-BEARING, not tidiness:** `step()` derives its target from `cur.k` and `cur` only advances at the landing, so without landing an in-flight shuffle first a second press inside the 420ms window recomputed the SAME target and silently ate the press — it now settles instantly on the next press (the `lbArcKill` idiom), so three rapid presses advance three boards. `closeLb` voids a pending shuffle (`xactN++`, `xactPending = null`), bins the clones and runs `xactReveal()` — the sat-out furniture must never be left hidden by an orphaned timeout. **THE DEAL is option A** (Dylan's pick over three real grid lift-outs — "three traveling cards out of the line up is a bit much", gutting three slots misrepresents the collection): neighbours slide out from behind the hero on open and tuck back under on close, so there are NO grid holes and the perfected panel-to-panel open flight is untouched. The deal needs **no fade** — the neighbours paint under the card, so the card itself is the occluder, and opacity is pinned at rest for the whole slide (the base rule's .4s + .2s delay would otherwise still be resolving after they had arrived). Both inline `transform` and `opacity` MUST be handed back at the end of a deal-in or they outrank the hover lean and hover lift for the rest of the inspection. Knobs `xDur 420 · dealDur 380 · dealBack 220` (dealBack is deliberately shorter than the card's .55s return so the flanks are away before the hero has travelled far) on the house flight ease; landing fires one `kclick`, the re-stack's single voice at settle.

## LB deck — shipped verdicts + next

- **LB DECK — SHIPPED 2026-07-26 (stage 1 + the motion), desktop.** Dylan's lightbox rework: thumbs out, x-of-x header in, the collection's neighbours on deck at the diagonals, the four-body exchange and the deal (full mechanics in the k-home-dual bullet's THE LB DECK and DECK IN MOTION passages). **His verdict: "this is near perfect... its working solid."** Motion CONFIRMED on his own browser at localhost 2026-07-26 ("the lightbox card movement all looks good") — which is the bar that matters here, since the pane's dead frame loop cannot play a transition and the undersides saga proved his build is the only render worth reacting to. And the rationale worth keeping, because it names the problem the deck actually solves: *"the collections still read like framed galleries (i like) but it eliminates the back and forth if you are trying to really get in there and inspect the whole lot of them."* The grid stays a gallery you browse; the lightbox becomes the place you work through the inventory without bouncing out to the grid between every board. Judge future changes against that sentence.
  **THE LOOK IS SETTLED TOO (2026-08-01)** — after three sessions of "still torn visually", Dylan's verdict on returning to it cold: *"totally not torn on the cards now. really like where we landed on them last session."* So FUNCTION and LOOK are both decided, the `thumbs` fallback has done its job, and the Lb switcher row is RETIRED (`LB` is a pinned const). Reaction history kept because it bounds the knobs if this is ever reopened: pass 1 read BUSY (neighbours competed with the inspection) → smaller + tucked lower; then `.37` opacity read as *"a background artifact rather than something ready to be swapped in or out"* → `.85`. **Both ends of the opacity range are known, so do not re-walk that ladder — opacity is the wrong knob for busy-ness, OCCLUSION is the right one** (crop harder: bigger tuck, further off the edge — also more on-language, since the carousel's trick is the border doing the hiding).
  NEXT, in order:
  1. *Completing the lightbox scroll leaves an invitation to a collections list* (Dylan confirmed 2026-07-26 this is the LIGHTBOX's end, i.e. reaching 14/14, NOT the page scroll). Note this is also a candidate answer to the long-parked "more board sections toward the end of the shop scroll / bolt-on point undecided" thread below, and it rhymes with what the lightbox already does — the end of a collection is stated twice at card scale (empty flank + faint arrow), and this is the same signal one level up.
  2. *Then mobile.* **SEQUENCING RULE (Dylan, 2026-07-26): dial the DESKTOP view in first, then mobile — deciding for both at once has burned him before, and he can't intuit the mobile behaviour until the phone is actually in his hand.** So build and react desktop, and treat the `NB_TUNE.mob` values as placeholders (a stronger tuck at tight views is already agreed in principle). Open mobile items when it's time: the phone fades the site to 0 rather than `.22`, so the neighbour opacity band has no floor there.

## Undersides — the decision thread

- **Undersides — DECIDED 2026-07-25 late (the flip-lab (vault) reaction session, 8 rounds): board CLICK flips to the underside. **AMENDED 2026-07-26: LIGHTBOX ONLY** — the `carousel` (the behind-the-back switch) stays, the collections/grid `kick` is retired (it competed with the inspect click; see the k bullet's UNDERSIDES passage). Indicator = `words` (Top/Bottom stacked top-right, shown face full ink, hidden face at the .22 faint, words clickable). No hover peek on shelf cards. Originals flip = interim blank-back until their canva reshoot lands (paper vs block unpicked, demoted).** The flip-lab (vault) bullet (Layout) holds the full mechanics + gotchas. **PORTED 2026-07-25; carousel SETTLED on Dylan's own browser 2026-07-26 after the render saga (cqw-in-keyframes → px vars, in-card clip → overlays, slab revert — see the k bullet + Gotchas) — his verdict: "fully functional."** See the k-home-dual bullet's UNDERSIDES passage for the shipped mechanics. Port-time picks (Dylan): board silhouette = flip / rest of the card = inspect (pixel-tested), belts stay inspect-only. Still open from the port: bay shelf cards left inspect-only (smallest cards, crowded click ecology — extend kick there if wanted), the touch translation (grid taps still inspect; the sheet lb card's board tap runs the carousel — real-thumb react pending), hs2/3/4 word labels (their fronts are the `-bottom` shots; words call the front "Top" by display convention), originals flipless until the reshoot, and the sun-reacts-to-the-flip aspiration (first pass = release/re-seed, not a live handoff).

## The dims detail line (2026-08-13)

`#lb-dims` — a `.lb-dims` line directly under `#lb-desc` in `.lb-rest`, mono 10px letterspaced caps (the spec register), printing the board's FULL measurements: `33.75 × 92.7 MM — TAIL 30.85 · NOSE 31.3`. This is where the tail/nose widths live — Dylan: "too much noise on a card decode, but could add it below on desktop where things like 'way to much time was spent hand sanding...' sits in small font." The hand-sanding line IS `lb-desc`'s fallback (the hand-shaped collection description), so the dims line sits right beneath it. REAL DATA ONLY: populate fills it from `DIMS_MM[ref]` (see cards.md) and never prints a PH_DIMS placeholder — a board without real numbers shows no line (`:empty` collapses it). Desktop-only by construction: it joined the `.lb-desc, .lb-product, .lb-count` mobile hide list (the card is the hero on mobile, no text under it). `body.light` override `#444`, paired with `.lb-desc`'s. NOTE the fv3 desktop reach: `.lb-rest` sits below the fold at 1440×900 — the sheet scrolls ~183px and the desc+dims lines land at the bottom of the viewport at full scroll. Reachable, but only by scroll; if Dylan wants dims visible without scrolling, that's a new seat decision, not a bug in this one.

## The toss exchange (2026-08-15)

`tossExchange()` (beside deckExchange) — the shuffle a DECK TOSS leaves behind: the desktop lb carry no longer closes the inspection (see the LB CARRY entry in deck-drawer.md), so tossing the hero into the case plays a three-actor exchange — left flank promotes to hero, new left flank enters, far flank holds its seat via a stationary clone; no demote actor because the hero's flight is the drag fly arcing into the case. Same tokens (xactN/xactPending/xactSettle), same landing (populate + migrateHole + redress/navdraw + xactReveal + kclick), so interrupts and closes void it exactly like an arrow shuffle.

## The video lightbox (2026-08-16)

`#vlb` — the one TRUE full-screen modal on the page, and the only organ that rides ABOVE the topbar (z **7000**, the top of the ladder against the bar's 6600): its dark scrim (`#vlb-scrim` on the shared `.lb-scrim` rule, rgba(24,19,14,.88)) has to cover EVERYTHING, where the card lb deliberately does not — the card lb fades the site over its own ground so the lifted card's empty slot stays visible, and nothing lifts out for a video. Three clips in the `VIDEOS` table, all under `site/media/`: `covercard.mp4` (id `cover`), `clip-1.mp4`, `clip-2.mp4`, each with its `-poster.jpg`. **The cover slot IS the live spine `<video>`** (`clipEls.cover = vid`, the `placeVideo` pattern) — enlarging never restarts it, and `closeVlb` hands it back to `placeVideo()` still playing; the other two are lazily constructed elements cached in `clipEls` and swapped IN PLACE the way board photos do (`vlbShow` pauses + hides every sibling, appends into `#vlb-frame`, turns `controls` on). `#vlb-thumbs` are the posters, `.on` marking the live clip. **OPEN** is `openVlb`, fired by a click on the spine video itself and guarded on `vlb.hidden`: a FLIP off the video's own rect onto the frame's — transition muted, transform written, reflow, released — so it reads as the same object pulled forward, with `body.lb-fade` + `lockScroll()` riding along; `reduced` skips the flight and just shows. **CLOSE** is `closeVlb(instant)` off `#vlb-close`, the scrim, or Escape — `.in` comes off, 330ms later it hides, `unlockScroll()` runs, and the spine video goes home muted with `controls` off. **ITS RELATIONSHIP TO THE PHOTO LIGHTBOX IS EXCLUSION, NOT LAYERING — the two never coexist:** `route()` closes an open `#vlb` on every hash change (`closeVlb(true)`, beside the card lb's own `closeLb(true)`), the belt frame loop early-returns while it is open (the VIDEO lb still pauses every drifting rail — the card lb deliberately does not, freezing only the inspected card's own row), and every deck/bay gesture gate (Esc-closes-the-bay, the wheel and drag handlers, the bay's own `#lb, #vlb` closest-check) tests `vlb.hidden` alongside `lb.hidden`. Geometry is `9/16` — `#vlb .lb-card` at `min(340px, 78vw, calc((100vh - 240px) * 9 / 16))`, the height term keeping a tall card on screen — and it keeps the STOCK centred layout on desktop rather than `#lb`'s scroll-sheet.

## The A/B pass — lightbox items (2026-08-16, ported from the winning vault run)

- **THE COLLAPSE DECODE** (his knob grammar: "effect flicker 90ms, 1.5s swap speed, position lock on, collapse"): `decodeCollapse(el, text)` beside decodeInto — the current text corrupts ends→center over 45% of the budget, the new text resolves center→out over 55% (CLPS_TICK 90 / CLPS_MS 1500), per-char `.dcell` spans FROZEN at measured widths (position lock — the churn never shoves the line; cells rebuild once at the phase boundary). Handle is `el._t`, NULLED on every clear — any existing `clearInterval(el._t)` + textContent write kills a running collapse and flattens the spans, so every external reset path works unchanged; the final flatten to a plain text node is load-bearing (copyCardTexts / stayLand's changed-only compare / populate read plain textContent). Callers: stayLand's nmt land (changed-only guard kept) + dockDress's arrival. Grid hovers keep the fast decodeInto; setFlip's UNDERSIDE stays house grammar (the flip is its own verb). **THE LAB IS THE AUTHORITY: the vault's `redesign/decode-slowmo.html` — every dial name in the knob grammar maps to a dial there.** (This pointer is the A/B experiment's one prescribed doc line — item 2 was unimplementable without the lab.)
- **THE TEXT UNDER THE CARD**: `#lb.fv3 .lb-desc { display:none }` — the description retires from the inspection; `#lb-dims` grows to 15px and is THE read down there, detailed tail/nose where measured. fv3-scoped (#vlb + stock lb keep theirs); `:empty` still collapses no-data boards, so an unmeasured board shows nothing — flagged react point.
- **THE DECK NAV**: the on-card assembled arrows are RETIRED — `.lb-deknav` prev/next are plain 18×30 chevrons **perched 10px above each flanker's inner top corner, inner edges flush** (his corrected spec; seated by placeDeck off the flanks' own deckGeom — a diagonal deck's two arrows may sit at different heights, correctly). Ids `lb-fv3-prev/next` kept so NAVS, step() wiring and the `.off` toggle run untouched; ink per ground (#fff dark / #000 light); `.off` = .22 + pointer-events none (the ends-clamp law); ≤940 display:none; wide-touch iPads keep them.
- **THE HEIGHT BUDGET**: the fv3 card guard's real stack is ~320px (head 253 + pad 24 + dims ~39) — guard `max(260px, min(430px, (100dvh − tbh − 320px)/1.53))`. At 1440×790 the whole inspection incl. the dims line fits (measured 786 ≤ 790); the 430 cap binds from ~1012px windows, so assets genuinely come down on smaller displays by construction. The dims line wraps to two lines at 790 (one on taller windows) — flagged react point.
- **THE MOBILE BAND BELOW THE CARD, lb-side mechanics**: deckGeom's phone `need` shrank to `chrome + drop`; the band's whole room (clr + band + 12) rides the CARD'S OWN paddingBottom (in-flow, so margin:auto centres card+band as one block); the fold cap measures against `min(lb.clientHeight, innerHeight − lb.offsetTop)` — an INTERACTIVE band must not live behind iOS's floating bar (the card may, the standing ruling); `g.stripLine` feeds placeStrip's synthesized headTop. Scrub/riffle math is x-only, unchanged.

## THE DESKTOP STRIP RESET — the counter leads, the row goes flat, the dot comes up (2026-08-19, ported)

Five asks, desktop only; the phone costume is untouched (every value moved is one `STRIP_TUNE.mob` already overrides). The counter used to sit BELOW the band and leads now — the stack from the sheet top is `headClr 12 → counter ~29 → drop 26 → band ~106 → prog 18 → dot 5 → clr 26 → card`, with `gapB` retired (it was the counter→card air) and `drop`/`clr` re-pointed to their new meanings. `deckGeom` computes a desktop `g.stripLine` so **the band's own rest-bottom anchors both costumes through one code path** in `placeStrip`; the phone's `belowBand` flag became `belowCard`, gating the ARROWS alone. The row went FLAT (`foff`/`fade` → 0 — one height, one ink, only the lb card's partner lifted; the phone's 2026-08-12 ruling generalized), minis 46 → 40, gap -8 → -10 (**-14 was tried and read as a squeeze**). `stripChrome(prog, arrows)` splits so the line—dot—line rides both while the arrows stay the phone's; the dot needed no new machinery, since `stripTo`'s press-glide stamp had been running inert behind the CSS gate. Height budget re-derives 320 → 285.

Verified on :8124 at 1440×900 (counter leads, prog under the band, arrows absent, minis one width/one top/one opacity at pitch 30, two chevron presses walk the counter and the dot, riffle raises a far mini and hands back), 1440×790 (34px slack) and phone 393×852 (mini 30, arrows live, band below the card). No console errors.

### React 1 on the reset — the scoot, the drag, and the board's 20% (2026-08-19, ported)

**THE SCOOT:** `drop 26 → 12` and `clr 26 → 46` — the same air either side of the band+line group, so trading between them slides the group up ~21px while the card holds its seat. Minis `40 → 37` with it. **THE DOT DRAGS BEHIND THE RIFFLE:** `progTo`'s second argument is a three-way mode (raw scrub · the .6s press glide · `"drag"`), and `stripTo`'s HAND branch writes `progTo(target, "drag")` — a 1.1s ease-out so the dot reads as towed by the raised card, with the same travel carrying it home on cursor-off; `stripReset` drops both classes so a fresh open still SEATS it. **THE BOARD GROWS 20%** on the desktop lb card (79/78% → 94.8/93.6%), the ONE CARD SPEC base untouched — and the **clones ride the same numbers** (`.lbnb`), or a promoted flank pops the board size at every exchange landing. It is near the ceiling: the board is HEIGHT-bound at 94.8%, ~9px of air from the AVRG chip and the name line.

Verified on :8124 at 1440×900: counter 76 → row 160 → line 234 → card 277, minis 37, board 94.8%, the drag sampled mid-travel and settled. No console errors.

### React 2 — the counter becomes the lifted mini's label (2026-08-19, ported)

`clr 46 → 72` gives the card its breathing room from the band. **The counter is seated over the POPPED slot** at the popped mini's own width (53.6), 12px above it, type `20px → 13px` to match that scale; `white-space: nowrap` is **load-bearing** ("7 / 19" measures ~70 in a 53.6 box and breaks after the slash without it) and the line overhangs ~8px a side, centred and `pointer-events:none`.

**Its one cue is the lightbox, never the scrub** — and that falls out of WHERE `seatHead` is called, not from a new gate: the riffle re-poses through `poseStrip`, which never touches the header, while `headSwap` re-seats **at its swap timer**, the instant between the fade out finishing and the fade in starting, where the number is at opacity 0. `sHeadK` tracks the number `setHead` last wrote (not `cur`, which only advances at the landing), so a mid-exchange `placeStrip` off the card's RO cannot yank the counter back; `stripReset` nulls it so a fresh open never inherits a seat. The fade TIMING needed no change — `headSwap` already splits `xDur` at `headOut .42`, completing the in on the card's landing.

Budget re-derived 285 → 321 with the taller stack.

Verified on :8124 at 1440×900: seat dead-centre over its mini (639 = 639), a full scrub to another mini leaves it untouched, a step shows the number changed and the seat moved at opacity 0, landing re-aligned (666 = 666). Fits at 1440×790 and 1440×900. No console errors.

### React 3 — pop down, the seat fixed at its source, SOLD no longer see-through (2026-08-19, ported)

**POP `1.45 → 1.3`** on the desktop base (`mob` keeps its own), which shrinks the band and the whole stack with it.

**THE FIRST-DRAW SEAT was a real race.** `populate` calls `setHead` BEFORE it assigns `cur`, so any `placeStrip` landing in that window (the card's RO fires constantly through a populate) seated the counter on whatever `cur` still pointed at — or on **slot 0 when `cur` was null, i.e. every first open**. Correction depended on a later re-seat firing, which is why it read as intermittent. Fixed at the source: **`setHead` writes the seat as it writes the number**, so the two can never disagree and no caller needs to know about `cur`; and **`seatHead` lost its fallback slot**, so with nothing to seat on it leaves the counter alone rather than guessing 0. `sHeadK` moved above its writer (setHead runs long before the strip block's statements — a `let` reached in its TDZ kills the script). `headSwap`'s explicit re-seat is gone; it already calls `setHead` inside the fade at opacity 0.

**SOLD DIMS ITS COLOUR, NOT ITS CARD.** The blanket `.sold { opacity: .55 }` made the whole card translucent, so wherever cards overlap — the strip's shingle above all — the neighbour read straight through. `.d7f.sold` is opaque and the board carries it (`grayscale(.85) brightness(1.12)`). The filter rides the **img, never `.stage`** (a filter there makes a stacking context and traps the flip faces' parked `z:−1`), so the drop-shadow is restated with it — and again at **hover weight**, since `:is(.d7f:hover, .d7f.lit) .stage img` outranks the sold block and washed the grey out on hover. Legacy specs keep the old blanket rule.

Budget re-tuned 321 → 292 (the landed stack exceeds the knob sum — the scoot's margin:auto share grows with the card — so the constant is tuned against measured slack, not derived).

Verified on :8124: first-draw seat delta 0 at 1440×900 and 1280×800, sold opaque with the greyed board, nothing over the fold. No console errors.

### The dot and the gap were RACING — one value, three readers (2026-08-19, ported)

`--px` fed **three separate transitions**: the dot's `transform`, segment A's `width`, segment B's `left`. A transform transition goes to the **compositor**; width and left are main-thread **layout**. One clean travel stayed locked (9.00–9.02px of gap), but the riffle **retargets on every slot crossing**, and each interruption restarts the three from whatever each currently reads — not the same instant for a composited property as for a laid-out one. Measured over a two-pass scrub the gap drifted **41.56px**, the dot ending up past the break in its own line.

**Fix:** `@property` registers `--px` as a `<length>`, making the custom property itself interpolatable; the transition moves to the parent and the three elements become plain readers of one animated number, so they cannot disagree at any frame. Same scrub after: **0.02px**.

**The curve was tested and kept** — a near-zero onset never builds speed under constant retargeting (22px travelled against 490 of row) and softer ease-outs stepped less evenly; the shipped `cubic-bezier(.33,1,.68,1)` both follows best and has the smallest step-to-step change. The race was the judder, not the curve. Do not re-walk it.

Verified on :8124: 0.02px gap error through a two-pass scrub.

### The dot rides the cursor while the hand is in (2026-08-19, ported)

Locking the three readers to one value killed the RACE; the remaining judder was the other half — a 1.1s transition **re-aimed at slot centres several times a second**, and a restarted tween has a velocity discontinuity by definition, so no curve makes a chain of them smooth.

The lane's `pointermove` now writes the dot **raw and continuously** — `progTo((x − pad − w/2) / pitch, false)` in FRACTIONAL slot space, so it tracks between minis rather than hopping — written on **every** move, deliberately before the slot early-return that guards the mini pop. Nothing animates during a scrub, so nothing restarts. Same raw grammar the phone's finger scrub already used.

The drag clock keeps one job: **the way home** — `stripTo`'s hand branch writes it only when `sFocus == null`. Press glides untouched.

Verified on :8124: the dot sits under the cursor through a sweep at 0.00px gap error, clamping in the lane's pad at the ends, and travels home onto the lb card's mini (477 = 477).

**Consequence:** while the hand is in, the dot no longer trails — it IS the cursor. A trail during the scrub cannot come from a transition (that IS the judder); it would need a frame-driven lerp. Not built.

### THE DOT'S CHASE — a frame loop replaces the trail's transition (2026-08-19, ported)

Raw cursor-tracking killed the judder but took the trail with it. The trail is back as a **frame loop**, not a tween, because **a transition cannot trail a moving target** — re-aiming one restarts it, and a restarted tween has a velocity discontinuity by construction. That is the whole judder story.

`dotAim(o)` / `dotStep()`: exponential smoothing normalized by real elapsed time, `x += (target − x) · (1 − e^(−dt/tau))`, identical at 60 and 120Hz, with `dt` clamped to 64ms so a backgrounded tab's one enormous frame cannot teleport the dot. `STRIP_TUNE.dotTau` (170ms) is the feel. The loop **self-cancels** the frame it arrives. It owns BOTH the chase and the way home, so there is no mode change to stutter at the hand-off; `.lbs-prog.drag` and `progTo`'s `"drag"` mode retire with it.

Resuming from a press reads the **computed** `--px` (interpolated, thanks to the `@property` registration) before dropping `.glide`, so entering the lane mid-glide picks up where the eye left it rather than snapping to the destination.

Verified on :8124, per animation frame through a 30-step sweep: **zero reversals**, peak 10.2px/frame against a target jumping ~28px/frame, median frame-to-frame velocity change 0.38px, p95 2.11. Gap error 0.01px, travel home lands on the lb card's mini. No console errors.

### React 4 — the bar scoots to the minis (2026-08-19, ported)

`prog 18 → 12` with `clr 72 → 78` — the same trade the band's own scoot used one level up: the two knobs are the air either side of the line, so moving between them slides the line without touching the card. Verified on :8124 at 1440×900 and 1440×790: band→line **12**, card unmoved. The phone keeps its own `mob.prog: 18`.

**The strip pass is settled here** — his sign-off on the lerp: "works great!"

## THE ORIGINALS GO DEEP — photos + info return, og-scoped (2026-08-20, ported same session)

**Dylan's ruling** (after parking the plate-system direction — see open-threads + the vault's plates-lab): *"the best move is to simply add lots of photos and info to each originals lightbox view."* This is the ORIGINALS RETHINK's "deeper per board lightbox" half, landed. **It is a scoped REVERSAL of two standing retirements:** the 2026-07-26 deck ruling (real photos unreachable from the lb — kept for every other collection) and the A/B pass's `.lb-desc` retirement (kept for every other collection). The machinery never left: populate still built the thumb strip and still carries the photo-view path (`j>0` = `IMG(b.photos[j])` in the card frame); the whole feature is a scope class plus two un-hides plus one new tile.

**Mechanics:** populate stamps `#lb.og` (`fv3 && c.id === "originals"`). CSS (beside the deck-mode gate): `body[data-lb="deck"] #lb.fv3.og .lb-thumbs { display:flex }` + `#lb.fv3.og .lb-desc { display:block }`, both under `@media (min-width: 941px)` — **desktop-gated; the phone lb stays card-only until the mobile pass** (the sequencing law). For og boards populate REBUILDS the strip with a **CARD tile in front** (`data-j="c"`, src `CUT(b)`, `.on` at `j===0` — in fv3 `j===0` IS the cutout view, so the generic tj===j marking would lie): a photo view must never be a one-way door. The thumbs click handler gained the `"c"` branch — back to `CUT(b)`, `cur.j = 0`, no canflip re-arm (og boards carry no flip pair). Photo views keep every standing behavior: flip disarmed, img-click departs to the collection, exchanges re-populate to the cutout, `.on` ring rides the collection accent (`--acc` = og orange). The desc prints the board's own description unless it starts "Replace this" (falls back to the collection line — currently what all six show, since **the real per-board descriptions are still unwritten: Dylan's writing fills this seat**).

**Open/react points:** the .lb-rest stack (thumbs + desc + dims) deepens the below-fold scroll at 1440×900 — the standing "reachable by scroll" ruling covers it, but if og inspections should fit unscrolled that's a height-budget decision; the marble photo letterboxes on the WHITE card stage (reads as the specimen mount — looked right on first verify); **clips are the named next step** ("they each need a clip when available") — the #vlb machinery is the obvious door, unwired on purpose; and the mobile og view is undesigned.

Verified both repos (vault :8123, v1 :8124, real Chrome 1440): open on OG 01 = `fv3 og`, strip `c*,0,1,2`, desc block with collection copy, photo tile swaps the frame, card tile returns the cutout, arrow exchange lands OG 02 with the strip rebuilt and the card tile lit, classic boards show none of it, zero console/boot errors.

## THE OGARR LAB — og-dossier arrangement collider (2026-08-20, TEMPORARY)

Dylan asked for arrangement work on the desktop og view (the .lb-rest below-fold react point above). **Three variants live IN the page** behind `body[data-ogarr]` — `?ogarr=a|b|c` at boot, or keys **1/2/3** while an og inspection is open (**0** = shipped; no param = shipped, so the live site is untouched). Two fenced blocks, both marked OGARR LAB: the CSS beside the og go-deep rules, the JS at the IIFE foot beside the bench shim. Retires whole on his pick.

- **A — FIT THE FOLD:** the whole dossier inside the window; the card pays via an og-scoped height budget (base formula's 292 → 448, thumbs 44px, tighter margins). Card ~356→~253 at 900-tall — the honest cost of fitting below.
- **B — THE SIDE DOSSIER:** the whole `.lb-rest` absolute off the card's right shoulder (76px out — clears the on-card stepper's seat, 48 didn't), 2×2 thumb grid + left-aligned desc + dims. Full-size card, no scroll. Real copy may run the column down toward the right neighbour — judge with real descriptions in.
- **C — THE FLANKS:** thumb rail vertical off the LEFT shoulder, desc column off the RIGHT (44px both), dims keeps its shipped under-card seat. `.lb-rest` stays static so the card anchors the columns.
- **D — THE SPREAD (added same day on Dylan's react + sketch — key 4 / `?ogarr=d`):** his ruling grew the direction: *"the originals need the whole page in light box view. no flankers."* Flanker cards hidden (`.lbnb` + the `.live` restatement — the deck's own `.lbnb.live{display:block}` ties bare `.lbnb` and sits later in source, so the hide must out-spec it; the deknav chevrons stay and still step), the CLIP off the left shoulder (200px, vertical, autoplay/muted/loop — `#og-clipseat`, lab-INJECTED markup, one STAND-IN cut serving every board: `site/media/og-clip.mp4`, ported from the vault's `redesign/originals-lab/media/`, cut from IMG_3060.MOV), the extended photo set as a 2-up grid of 107px tiles + left-aligned desc off the right (76px), and the strip up top goes **bigger/wider through the real machinery** — the lab writes `STRIP_TUNE` {mini 50, gap 8, pop 1.2, maxw .92} (shipped values saved/restored around the variant; the resize door rebuilds) rather than CSS-scaling, so every strip interaction stays true. `#og-clipseat`'s base `display:none` lives OUTSIDE the ≥941 block on purpose — inside it, the seat fell back to block under the card below the gate (bitten at the pane's 735px native width). Verified 1440×900: whole spread inside the fold, exchange lands OG 02 with the spread intact, key 0 restores shipped STRIP_TUNE exactly.
- **His remaining asset ends for D** (the direction needs his hands): per-board CLIPS (cut from the vault's raw videos — the stand-in proves the seat), the NEW angled top/bottom photo shoot (also meant to set og cards apart on the collections page), and the per-board descriptions.

**Mechanics worth keeping:** the card's scoot padding is JS-written, so absolute columns can't top-align with the panel in pure CSS — the lab mirrors it into `--ogpt` via a ResizeObserver on `#lb-card`; a key flip also fires the resize door so placeDeck re-derives neighbours around variant A's card size. **Bench note:** the lab does not chart — shipped values are unchanged (default = shipped). The PICK, when it lands, is the charted change (og wings lb-og-thumbs/lb-og-desc + the card formula if A wins).

**Pane gotcha, re-bitten during verify:** on a fresh load the pane froze the `.lb-rest` opacity transition at 0 (and the strip's) — reads as "the lab is broken" when it's the frozen-transition wedge. The probe rule (`transition:none !important`) reads the truth; real Chrome unaffected. **And its inverse, bitten the same evening: leave the probe IN and the lb's close/open choreography stalls half-done (hidden stamped with `.in` still on, the page fade stuck) — killed transitions never fire `transitionend`. Probe for the screenshot, remove it before driving the lb.**

## THE OG SPREAD SHIPS — D wins, the lab retires (2026-08-20 late, Dylan: "this one looks solid")

**The spread is the og lightbox now — no switch, no variant gate.** The a/b/c losers + the ogarr switch JS live in git history (`8817076`→`d9df671` had the lab; the ship commit removed it). What shipped, and where the dials are:

- **CSS:** one block at the old go-deep seat, headed THE OG SPREAD. **Every dial is a `--og*` var on the `#lb.fv3.og` rule** (≥941): `--ogClipW` 200px · `--ogClipGap` 76px · `--ogTile` 107px · `--ogTileGap` 10px · `--ogRestW` 224px · `--ogRestGap` 76px · `--ogDescSize` 13px · `--ogColTop` 0px. Consumer rules read the vars — tune the vars, never the consumers. `--ogpt` is NOT a dial (the scoot mirror, ResizeObserver at the IIFE foot). The flanker hide keeps the `.live` + `[data-lb]` restatement (specificity, see the lab entry's lesson).
- **Markup:** `#og-clipseat` is real tree now (inside `#lb-card`, before `.lb-rest`) — video + `.ogclipcap`, still the ONE stand-in cut. Its base `display:none` sits outside the media block (the 735px lesson); it rides the `.lb-rest` open/close fade rules (all three widened to cover it).
- **JS:** `STRIP_TUNE.og` `{mini 50, gap 8, pop 1.2, maxw .92}` — a third costume through `ST()`'s one door, exactly the mob idiom. Gated on the `og` CLASS, not `lb.hidden`: populate stamps the class before the strip is dealt on the open path (lb still hidden there) and re-stamps per open so it can't go stale. Dock outranks og — the phone strip keeps mob on og boards.
- **Verified** (1440×900, served): plain URL opens og as the spread (clip left, grid+read right, flankers `display:none`, og minis), classic lb keeps everything base (flanker back, base minis, no og furniture), zero console/boot errors.
- **Charted same session** (the change-with-charting law): the **`og-spread` wing ("originals lb") LANDED — CompUI `3631c61`, census 103→116.** Two banks: nine CSS vars (the eight above + `--ogTileRatio`, added when Dylan's angled shoot arrived portrait — tile height = width × ratio, range .75–1.6) as scoped-built overrides on the one `#lb.fv3.og` rule, plus the four `STRIP_TUNE.og` knobs through the shim with repose. Six old og params locked-with-a-reason (their axes moved to the spread's vars — the manifest rows name where each went); `ogth-wrap` closed as ANSWERED (the 2-up grid IS the row-vs-wrap call). Bench-verified end to end incl. og-vs-classic isolation.
- **THE CLIP GATE (same session, C4's find):** the seat's video shipped `src` with implicit preload — the 279KB cut rode EVERY page visit on every route. Now `preload="none"` + no autoplay + a MutationObserver on `#lb`'s class/hidden (the IIFE foot, beside ogptSync): play on og-open (first play triggers the fetch), pause on close/exchange-away. Verified: plain visit fetches only the 18KB poster; og open fetches + plays; close pauses. The poster still rides every load by nature of the attribute — accepted at 18KB. C4's other finds live with Dylan: `ogth-ring`'s 2px ring reads heavier on 107px tiles (now a slider), `ogd-leading` 1.75 was tuned for a 40ch centred line and the read now sits ~28ch (now beside the size slider), `og-strip-maxw` is honest headroom (drives below ~.3, charted live with that stated).

## THE ZONE LANDS — the og LB renders the hand-composed sheet (2026-08-22 night, the first board through)

The ZONE ruling (open-threads' og entry) got its site side the same night Dylan composed the first sheet: **OG 05 (Short Steep) is the first original through the loop** — composed on the kit's "07 · OG Sections" canvas, pulled by MCP at the canvas's own 2× (960×1280), seated at `site/img/og/og-05-section.png`, and rendered by the new seat. The pieces:

- **`OG_SHEETS`** (beside `DIMS_MM`, its idiom): hand-authored manifest, padded ref → sheet path. One line per landed export — this is the wiring step of the per-board loop; a board with no entry keeps the spread's photo-grid right column, so sheeted and sheetless originals coexist while the other six compose.
- **`#og-zone`** (markup beside `#og-clipseat`): an img seat, base `display:none` OUTSIDE the media block (the 735px law), fade-riding all three `.lb-rest` opacity rules (widened again, clipseat precedent). `populate` is the one writer: sets `lb.ogz` + the img src on demand (first sheeted open fetches — the clip-gate philosophy), swaps per exchange, strips both on sheetless boards.
- **CSS on the charted og rule:** `--ogZoneW: min(480px, 33.33vw)` (480 at the 1440×900 reference, riding the window below it) + `--ogZoneGap: 40px` (tighter than the old column's 76 — card-right + 76 + 480 overran the reference by ~16px, measured). Under `.ogz` the spread's thumbs/desc/dims stand down (the thumbs hide must match the `body[data-lb]` show compound — the `.lbhead` specificity bite, hit again and commented at the rule). `.lb-product` untouched. **No fold-fit clamp on purpose** — the ruling defines the box at the reference; small-window tails past the fold are the spread's existing behavior (and an honest CSS clamp is impossible from the `#lb` rule: `--ogpt` is `#lb-card`'s, vars don't inherit upward — measured reading the 230 fallback).
- Verified (:8124, real Chrome): open on OG 05 = `ogz` + sheet rendered (960×1280 natural) + grid/desc/dims hidden; exchange to OG 06 = `ogz` drops, spread returns, src stripped; back = re-engages; zero console errors. **Known:** ~5px of the sheet's blank margin clips at a 960-wide window (share math vs the height-driven card); invisible in the sheet's own white border, fine at the reference.

**Still open on the og thread:** the left "short text" seat (the ruling's clip + short text left — his per-board copy still doesn't exist, so the clip cap stands alone), per-board clips (the stand-in cut still serves), the other six sheets, og mobile, and **the og-wing re-chart** (the zone dials + `.ogz` touch the charted rule — dispatch queued behind the pick-flight charting run).

**BENCH BAKE 2026-08-23 (his index, tuned against the zone landing):** clip 200→300 · clip-gap 76→140 · col-top 0→80 (the zone rides the drop too) · strip og mini 50→49 / gap 8→22 / pop 1.2→1.225 — plus the legacy right column's moves (tile 107→150 · ratio 1→1.52 · tile-gap 10→15 · rest-w 224→326 · rest-gap 76→140 · desc 13→15.5), baked faithfully though sheetless boards are their only wearer now. The landing rows in his index were no-ops (all four unchanged). Manifest sync: the inline lane, same session.

**C8's SITE FIND, FIXED (2026-08-23):** the zone's stand-down hid only the column's three organs — `.lb-rest` itself stayed as an invisible 326px box floating over the zone (every `.lb-product` child is already hidden under fv3, so it held nothing — but it was one background away from a real overlap). The parent now hides wholesale under `.ogz`; the thumbs specificity compound retired with it (nothing renders inside a display:none parent). Verified: sheeted = rest none + zone up; sheetless exchange = rest block, grid + read back, zone down. Bench-side, the pick-flight wing (C7) + the zone re-chart & bake sync (C8) both LANDED — CompUI 5d342ef + 4dfd245, census 144; the manifest keeps the zone's min() wrapper as shipped behavior and the legacy column stays live-not-locked (six of seven boards still wear it).

## 2026-08-27/28 — THE ZONE CENTERS, THE SLAB RETURNS, THE GROW BOUNCES

**The C sheet (2026-08-28, RULED same day: C is THE sheet, A/B scrapped):** OG 05's third layout came through the standard loop (pasted canvas link → MCP `download_assets` export, 960×1280 native — no scale flag needed, the frame IS the 2×) as `site/img/og/og-05-section-c.png`, and `OG_SHEETS` points at it. Dylan's ruling supersedes "b stays": **A and B are deleted from the tree** (git history keeps them), C is the one sheet. Verified served at the 1440×900 reference: zone renders C, the ~70px right-glass overrun is the standing no-clamp behavior.

**The "keeps cropping different" mystery, closed (2026-08-28):** each time Dylan compared Figma against the site, the site was serving the PREVIOUS export (avrg.cards on B while Figma showed C) — the loop's export was always faithful (proven by in-app node render vs REST export, identical). The lesson: until a push lands, the live site is one sheet behind by construction; compare against localhost. **The crisp pass** rode the same session: three Variant5 layers carried sloppy-nudge rotations (grip underside 89.882°→90, profile shot −0.07°→0, an arrow 179.992°→180), snapped exact via `use_figma` with centers preserved, re-exported over the same filename. Also learned: OG 05's Figma layer names are shuffled (the layer named IMG_7212 is the bottom PROFILE shot; the grip underside is IMG_7475, portrait only via its 90° box rotation) — don't trust layer names on that page, and OG 05 grew a component set (Default/V2/V3/V4) with Variant5 loose beside it; the loose Variant5 (183:864) is the exported source.

**The B sheet (2026-08-27, RULED 2026-08-28: "b stays"):** OG 05's second hand-composed layout landed as `site/img/og/og-05-section-b.png` and IS the board's sheet — the A/B react closed same week; the A export stays on disk as history only, nothing references it. The loop gained a lesson: Figma copy-as-code is NOT an input — the zone renders one baked image, and the MCP export of the selected frame is the whole recipe. A measurement misalignment he caught (the 29.75MM arrow floating inboard of the deck) was IN the frame, not an export artifact — proven by canvas-render vs export comparison, then fixed by driving Figma (`use_figma`: both arrow halves stretched to span the deck rails 63→275 at y718, label re-centered; the arrowheads are stroke caps so resize doesn't distort them). Figma MCP gotcha hit repeatedly: responses >~20KB die in SSE truncation — page node reads in small chunks (names-only, boxes-only for listed indices).

**THE ZONE CENTERS + BREATHES (2026-08-27, site `01fc845`, bench CompUI `34587f4`):** Dylan's react at 1920 — the sheet v-centers on the card panel (`top: calc((100% + var(--ogpt,230px) - 24px)/2)` + `translateY(-50%)`; the 24px is the card's static bottom pad, --ogpt the scoot mirror) and `--ogZoneGap` 40→140, matched to `--ogClipGap` so both shoulders breathe alike. **The zone left `--ogColTop`** — that dial now moves only the clip seat + legacy column. The 1440-reference fit is knowingly spent: the sheet runs ~70px past the glass at 1440 (measured), reacted-to at 1920 under the standing no-clamp ruling. Cartographer find, unfixed: the centring calc repeats the card's literal 24px bottom pad as a magic number — if that padding ever moves, the sheet drifts off-centre silently; a `--ogpb` mirror on the same ResizeObserver would make it one source.

**THE INSPECTION GETS ITS SLAB BACK (2026-08-28, `b0ba3a1` → trimmed by `a3848d0`):** his catch — the 2026-08-12 "shadow goes soft" pass had stripped the card's hard slab from the whole desktop inspection (phone kept its). The slab returned as the card's BODY on hero AND flankers over the kept ambient; the 08-12 landing-pop fix survives because every actor (`.lbnb` covers the clones) wears the IDENTICAL two-layer cqw throw — the pop was actors differing, never the slab. **THE GROW BOUNCED same day:** hover/blift rules swelling the panel slab to collections-hover depth were built, verified, and pulled on his react ("distracts from the board, the cooler effect, and where you want the eye") — the slab is STATIC, the board's grow/sun family stays the only hover answer. Tombstone at the suntrack rule; the rules live in `b0ba3a1`.

**BENCH BAKE 2026-08-28 (his index, with the slab restore):** nb gapX 44→82 · drop .46→.48 · fade .85→.76 — the flankers breathe around their returned shadows. Bench sync dispatched (nb values + the shadow shape question) — ticket in flight at write time.
