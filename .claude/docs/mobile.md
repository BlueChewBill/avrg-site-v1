# Mobile — the phone grammar
Phone passes, mobile strip, the deck-is-a-page rework (YOUR PICKS), pinch/swipes, and the retired takeover-case history.
> **Ported from the vault 2026-08-16** — the page is now `index.html` at the repo root (was `redesign/k-home-dual.html`), asset paths below are repointed, anything marked "(vault)" (lab pages, the card-art batch, the labs.md catalogue) did NOT ride over and lives in the vault repo, and every git hash cited below is VAULT history.
> Carved verbatim out of the old monolithic CLAUDE.md on 2026-08-12 (full backup on Dylan's Desktop + in git history). Wording is preserved exactly — section headers were added for navigation only. New decisions on this surface get appended HERE, not to CLAUDE.md. Cross-references like “see X below” may now point at a sibling doc in this folder — check CLAUDE.md's index.

## The mobile strip + the deck becomes a page (2026-08-12)

**THE MOBILE STRIP LANDED THE SAME EVENING AND WAS REACTED THROUGH SIX ROUNDS IN ONE SITTING (2026-08-12 late — every round signed off: "proper", "nailed it", "works great"). The live phone grammar:** the strip runs under dockActive (`STRIP_TUNE.mob` through the `ST()` merge — mini 30 · pop 1.45 (the desktop number) · **foff/fade 0 — NO falloffs on the phone** (a taper at 30px reads as fade; every mini full-size full-ink, his round-2 call) · drop 4 · clr 24 · chrome 34; the band hangs from the chrome row's underside, and deckGeom's FOLD CAP width-shrinks the card to the room the band leaves — the 852 law). **THE PHONE COUNTER IS GONE** (round 2: the strip does the wayfinding, CLOSE holds the chrome row alone; hidden NOT unwired — setHead/lbhCur keep writing since desktop shares the path, headSwap's timeout fallback covers the animationend a hidden element never fires, and the hide selector must match the show rule's full compound `body[data-lb="deck"] #lb.fv3 .lbhead` or it silently loses the specificity fight — bitten). **CARD SWIPE = ONE BOARD, THE BAND IS THE THUMB-THROUGH** (round 4, his ruling: "swipe is one card at a time. if you want to thumb though, thats the minis job. so keep the tie there"): the pre-train swipe tracker is restored verbatim (finger-tracked real-card drag, 70px commit → step/slideExchange, spring home, ends rubber at a third, committed wall swipe exits to the collection), while the carousel engine survives DEMOTED to the band scrub's motor — a touch-drag on the lane maps the finger's slot ABSOLUTELY onto caro.pos in band pitch (scrollbar semantics: the mini under the finger IS the board at center), bare clones convey FLAT at gap 34 under the hidden hero, the riffle/buzz ride caroPose's crossing hook, release SNAPS positionally (no momentum — a coast would fight the finger-under-mini promise), and a sheet touch during the snap settles the train instantly so a pinch can never seed on the hidden hero. The momentum train (flick/coast/catch) and round 3's arc pose (edgeScale/edgeDrop grow-shrink — built, then removed same day: "remove the grow and the shrink") live at git d25ee4a. **The lane's hover handlers (riffle + cursor-off hand-back) are MOUSE-ONLY** — the synthetic-leave gotcha's third bite: a lifted scrub finger's pointerleave glided the pop back to the PRE-scrub cur.k mid-snap (populate only runs at the snap's landing); on touch the scrub's own crossings write sFocus and caroSettle clears it. Tap-jump resolves on pointerup with its own x (the round-1 gotcha).** **AND THE MOBILE DECK IS A PAGE (round 6 — Dylan: "the deck drawer should away on mobile =(", reacted "works great"): the takeover case is RETIRED from the phone** (openCase is DOOR-LESS — kept only so route()/boundary crossings can stand a stray state down; caseCatch lives at git 473b4bc). Three re-homes: (1) **the bar holds the picks** — `#picon` renders real fv3 minis instead of mark+count (`renderPicks` via lisstTab: the 232 template scaled to a 25px token, 12px shingle with the newest on top, flex-shrink auto-compress into a 96px seat, a BLANK card when empty; ink re-proportioned to 4.31cqw per the tiny-rendition law) and is a plain `#my-lisst` link again (the angry-0/shake escalation retired — the blank card is the empty state, and its tap opens the empty page which says it in words). (2) **YOUR PICKS is the page** — renderLisst's dock costume: collections-style masthead (the h2 posts ride free), the same 2-wide shelfgrid, an **OFFER? row under each card** (label + ruled input, the letter grammar one level up) writing straight into `bdmComments[id].text` — **the desktop composer's own ask string, so the same-string law crosses the boundary** — and the **copied-send flow** at the bottom: `im interested in <tags>` line, COPY press (bdmCopy rides the real gesture, the popup-jail family), then the doors reveal with the full letter prefilled into reddit + mailto (chip labels ARE the destinations). Desktop keeps My Lisst + the inq form verbatim (a branch; the 940 boundary crossing re-dresses a mid-page visitor). (3) **the bar catches the add** — `pickCatch` flies the real card up into the mini stack on the house bayFlight arc (negative hook = up-right swing; the bar rides ABOVE flights in the z-ladder, so the card tucks under the chrome and the touchdown-rendered mini IS the reveal; silent landing + buzz, the phone dialect). The left-swipe-on-browse commit routes to `#my-lisst` on the same mid-move beat. **Open from his react: the iPad / hover-capable-tablet resolution still shows the old buttons + desktop lisst page — his note "we will address it soon"; and DM kept its bar seat though his spec said "just the logo menu top left" — one line to remove on his word.** Next: the mobile-layout tidy; see .claude/HANDOFF.md. 

## YOUR PICKS gets dressed (2026-08-12, the mobile-tidy session — built per the handoff's polish list, AWAITING DYLAN'S REACT)

The round-6 page's functional furniture took the house grammar in three moves, no new organs: (1) **the OFFER row completes the letter grammar** — label · 5px structural dot · ruled line (the sheet's TAG · DOT · WRITING LINE flattened into the flex row; the dot self-centres — a flex baseline has nothing to offer a textless box), the label prints at rest (the opacity crutch retired per tags-print-at-rest) and **INVERTS while the caret is in the line** (`:focus-within` — the desktop tag's own caret answer, crossing the boundary like the string it decorates). (2) **the COPY press wears the send button's rest costume** — `· COPY MY MESSAGE ·`, white word (14px, .22em with the text-indent take-back) between white 5px structural dots on the black slab, 1.5px border; NO engulf (hover choreography is desktop grammar) — `:active` flashes the engulf's LANDED state instead (white slab, ink word + dots). (3) **the doors are the envelope back face's own chips** (`.bb-chan` re-seated as `.picks-door`): black 44px soft square, white glyph, destination as the 10px label — and the artwork is **CLONED at render out of the live `#bb-reddit`/`#bb-mail` markup** (`doorArt` in renderLisst — one artwork two seats, the deck-mark edit-together law made mechanical; the bay markup is static so the source exists whether or not the composer ever opened); the press TIGHTENS (the bay-close inset breathe on `:active`, never a transform). The whole block **CENTRES** now (`.picks-send`, 16px side inset = the mobile shelfgrid's own frame) — the ceremony echoes the back face it re-homes; the interest line caps at 42ch. Verified 15 Pro sim (dress, caret invert, 16px no-zoom floor held, copy → doors reveal) + pane DOM audit ("$40 firm" typed on the rule rides into BOTH door hrefs — same-string law green) + 1440 (desktop costume untouched: My Lisst + inq render, zero mobile furniture; note the Claude pane's "desktop" preset is ≤940 wide — a real width must be forced for a desktop-branch check). **Open:** header-minis size/shingle untouched (his react pending from the handoff list), the centred-vs-left block alignment is Claude's call awaiting his eye, clipboard on real hardware still the one unverified beat.

**TIDY REACT ROUND 1 (2026-08-12, same session — his four-item list, all landed):** (1) **the phone bar's DM seat is GONE** (`#nav-dm` markup + its label writer deleted; the lb's DM cta is the one bar-adjacent DM left to write) — the round-6 spec "just the logo menu top left" finally executed; **the retired element was carrying the bar's `margin-left: auto`**, so the right-push moved onto `.picon` itself — and NOT via a new early rule: the round-6 costume block eleven lines later re-set `margin-left: 14px` (the old gap-from-DM) at equal specificity and silently won, the source-order gotcha again; the fix lives in THAT rule. (2) **the stack tightened + the count returned**: `.hpick` sliver 12 → 8 (the deck reads held, not fanned), and `.hcount` rides the stack's top right — a naked 9px mono number on a white seat, the desktop bar's mark+count grammar at phone scale; rendered by renderPicks ONLY when the deck holds cards (the blank card stays the whole empty state — no angry 0 revival). **BITE: pickCatch's getEnd aimed at `hpicksEl.lastElementChild`, which the count span now is** — a flight into a 9px number; re-pointed at `.hpick:last-of-type`. (3) **the picks cards sit FULLY DECODED** (his words): the deck's own dress — the `.bay-item` pre-finished idiom (negative-delay chipwipes, white glyphs, rails closed, mark shadow flipped) scoped to `.lisst-card` under the dockActive gate, plus the TEXT half landed statically by renderLisst (the status word; rest IS the landed state, no scramble replay). Desktop My Lisst keeps rest cards + real hovers — the gate is the boundary. (4) **the colnav is gone from the phone's picks page** (`M ? "" : colnavHtml(...)`) — the page ends on the send ceremony; collections live behind the back-link and the AVRG menu; desktop keeps its footer nav. Verified 15 Pro sim (bar right-seated, count 5, dressed grid, page ends at COPY, lb add OG 04 → flight into the stack, count 6, newest mini on top, grid hole) + 1440 pane (My Lisst/inq/colnav intact, rest cards, lb-dm wired, console clean).

**TIDY REACT ROUND 2 (2026-08-13 — his outlined-field sketch + the · COLLECTIONS · reference, four items, all landed):** (1) **the OFFER row became the OFFER BOX** — the Material-outlined-field GEOMETRY (label breaking the top edge, top-left) in the house drawn-box LANGUAGE: 1.5px ink rect, the two mid-edge structural 5px dots (the card frame's own posts), the label as a white-seat chip riding the border line. NO placeholder (his call; the stored-offer prefill on re-render stays — that is persistence, the same-string law, not a placeholder). Round 1's caret answer carries: `:focus-within` inverts the label to the black chip — and the frame thickens by an **inset box-shadow, never a border-width change** (a 1px width step shifts the whole interior). Label copy is OFFER (the "?" retired with the rule it hung on). (2) **the MESSAGE BOX** — the same box one size up around the pre-written line, labelled MESSAGE; display furniture, not a field, so no focus states; shared CSS with the offer box (one rule pair, two seats). (3) **the refs in the line went BLACK CHIP** (his "black square white fill") — the drawer head manifest's own species (black fill, white letters, .14em with the text-indent take-back), replacing the · separators; inline-block so the line wraps BETWEEN chips, never inside one. (4) **the press reads · SEND MESSAGE ·** (was copy my message; costume unchanged — the copy still happens at the press, the doors are the send). Verified 15 Pro sim (boxes drawn, label invert + inset thicken on caret, "$30?" typed w/o zoom, chips wrap, SEND MESSAGE seated) + 390 pane DOM audit ("$55 cash" rides into the reddit href, doors reveal, chips/labels right).

**TIDY REACT ROUND 3 (2026-08-13, same session — two spacing/weight notes off his screenshot):** the box line goes **2.5px at REST** ("as thick as the selected versions") — the old focus thicken IS the resting line now, its inset-shadow rule deleted, the caret's answer is the label invert alone; dot and label offsets track the border centre (a knob pair: dot inset = half-dot + border/2 = 3.75, label top = border/2 = 1.25 — re-derive both if the width ever changes again). And the offer box's air above now MATCHES the 30px the grid row-gap leaves below it — margin-top 36px, the 30 corrected for the card's shadow reach per the SHADOW LAW (≈6px of the ~165px slot card's drop paints into that margin): the two gaps READ equal instead of measuring equal. Both boxes ride the shared rule (the MESSAGE box thickened with its sibling — one grammar, his ask named the offer box but the pair was built to not drift).

**TIDY REACT ROUND 4 (2026-08-13, same session — the ✕ and the want words):** (1) **the picks remove wears the case's own square** (his screenshot = `.bay-x`: 22px white chip, 1.5px ink border, "square chip in the card's angular language"), landed **TOP-LEFT** on his word (-8/-8, the outer corner — clears the ref chip riding the inner frame); mobile-gated, the desktop My Lisst keeps its disc. **The selector is THREE classes deep on purpose** (`.shelfgrid .lisst-card .lisst-remove`): the light-ground disc override (`body.light .lisst-remove`, later in source) sits at two, and a two-class rule loses that tie silently on the forced-light phone. Side effect worth knowing: the old disc was OCCLUDING the JSPARK demo mark on OG 01's top-right corner — it shows now; pre-existing card furniture, not a bug. (2) **THE PHONE CHIP SPEAKS WORDS** — the want-word pool (`WANTS`: Want? / Like? / Need? / Mine !) lands across all mobile cards: `landWants(root)` (by `wantWord`, dockActive-gated, idempotent) statically lands a random word on grid cards at render (called from renderColPage / renderShopAll / renderLisst) — and a **✓ chip prints `Mine !`**, the pool's own word for the on-lisst state: the phone speaks words where the desktop speaks marks. The mobile lb joins via a `dockDress` beat (decodeInto on arrival — the desktop hover-dress's decode played at the landing; populate re-writes ✓ fresh each exchange, dockDress re-says it). **Scoped to `.scard`** so the tiny renditions (header minis, band clones, bay items) stay silent — ink at those scales is proportion, not reading. Verified: grid rest words (LIKE?/WANT?), lb arrival decode (NEED?, re-rolled per the no-repeat law), picks all Mine !, square ✕ seated top-left.

**TIDY REACT ROUND 5 (2026-08-13, same session — the blank mini takes over the chip; REVISES round 4 after one react):** (1) **the bar's empty blank is a real blank MINI** — barely smaller (25×38 → 22×33), white card + hard throw + **the fv3 inner frame drawn in** (`::before` inset 2, 1px line): card furniture with nothing on it. (2) **the same blank tucks into every mobile card's bottom-right PRE-decode** — `.fmini` span added to the framev3 template + the lb's static chip markup (desktop `display:none`, keeps the deck mark); **the mark stands down on the phone chip** and the 2026-08-08 mark-grab/+-pull choreography (fill L→R grabbing the mark, the white + riding the fill's clock, its `:has` suppressor and softout drain) **retired to git with the mark**. Rest = the mini ALONE (an empty .ft drops out — round 4's static grid want words retired after one react). (3) **the decode fill pulls from the card's RIGHT edge leftward** — the `.tset` fc-list origin flipped left→right (the chip is flush right, so the origin IS the card's edge; tset is dock-gated by construction so desktop is untouched). (4) **decodes read `ADD` + the mini** — dockDress decodes ADD on free boards; **✓ still prints `Mine !`** (the one want word that survived — landWants shrank to that conversion; WANTS/wantWord remain desktop-hover machinery). (5) **the offer box grew its WRITING LINE** (input border-bottom 1px against the frame's 2.5 — the letter's rule inside the drawn furniture) **and the side rails now BREAK around the mid dots** — the fv3 inframe's own 50%−6px gap grammar: side borders removed (`border-width: 2.5px 0`), each pseudo is one 5px column drawing rail-gap-dot-gap-rail as THREE layered backgrounds (radial dot + two linear segments — the dots used to BE the pseudos and a box only has two); the rail column seats where the border was (pseudo at ∓1.25px, rail 2.5px centred). Shared with the MESSAGE box. Verified 15 Pro sim (grid mini tucked silent, lb `ADD`+mini in the fill, picks `MINE !`+mini, ✕ removes in its new seat, empty state = the framed blank, boxes with gapped rails + writing line) + 1440 pane (mini hidden, mark shown, ✓ intact, console clean). (A mid-session scare — the sim's test lisst shrank 6→1 — was Dylan driving the sim for screenshots, confirmed by him at wrap. No bug.)

## The phone pass (2026-08-07) + the 8/8 day pass

  **THE PHONE PASS (2026-08-07, the night before the 8/8 shop event — the 15 Pro sim audit, five fixes + the slide):** the mobile lb header is CHROME now (≤720: the tag line is GONE — the card's corner chip already prints the same string — and the counter sits fixed top-left as CLOSE's mirror; the card's safe-area bottom padding RETIRED 2026-08-08 evening (Dylan: "remove the white strip at the bottom of the light box that blocks the address bar" — the env(safe-area-inset-bottom) reservation held a dead white band under the card; sheet content flows to its true bottom now, under iOS's floating bar like the collection page); `deckGeom` forces H=0 there so the headroom padding pass stands down and the card takes the room back; this is what fixed the card's bottom row falling below the fold on 852pt phones — the 17 sim's extra 22pt had been hiding it). **`fmtRef` is DELETED** — the THIRD ref producer, never given the INVREF delegation, printing site-ordinal refs into the lisst page and the DM/mailto prefills ("HS 09" for the board everything else calls "HS 16"); `lisstLabel` and `populate` ride jref/refOf now. **Never add a ref producer — call jref/refOf.** The gpick chips are hidden on mobile (they sat ON grid cards and covered the lisst page's SHAPED button) and the ground toggle is the AVRG menu's **GROUND row** (Dylan's control, prints the current pick, flips in place; a `<button>`, not an `<a>`, so the row-click close doesn't fire). The mobile drop is MONO (his call: accents stay a desktop mini-bay thing) and **dims the page behind it** (`body.menu-fade`, .15 — ordered BEFORE the lb-fade rules so an inspection's harder fade wins by source). The takeover head is **icon + count + skateboard** ("INTERESTED" clipped to "INTE" at half-screen; his pick "for the time being" — the rotating phrase and ?msgfx= stay desktop). **AND THE PHONE EXCHANGE IS A SLIDE:** `renderNeighbors` stands down under `dockActive` (ONE card on the sheet, ever — the flanks and the four-body conveyor are desktop physics), `slideExchange` replaces it — the REAL card rides off along the swipe's line, the next board in from the far edge, SILENT (no kclick, no landing press; tset survives populate so the card arrives dressed), ends rubber-band at a third rate instead of stepping — **EXCEPT a COMMITTED swipe into the wall (past 70px: rightward on 1/N, leftward on N/N), which EXITS to the collection since the 8/8 evening react** (Dylan: "should return you to collection") — the clamp still refuses the STEP, the flight takes the intent. Mechanics note at the code: the inline rubber pose is zeroed under a muted transition + reflow BEFORE closeLb runs, because the flight math anchors on the card's measured rect and a parked offset bakes itself into the landing as a visible seam at the slot. **AND THE PHONE CLOSE IS THE SOFT RETURN (2026-08-08 evening, Dylan: "on the phone it should be more fluid" — desktop's snap + sounds stay):** every mobile grid-return close is three staggered LINEAR ramps — the lb's ambient throw drains by HALF the flight (.215s), the page's opacity lands at full EXACTLY at touchdown (.43s, `body.softret`), and the dress (fills, rails, chip text, mark shadow, status word, Top/Bottom) drains until HALF A SECOND AFTER the landing (.93s). Mechanics at the `.softout` CSS (.snap0's slow sibling): closeLb sheds `.tset` with `.softout` on in the same recalc so transition-driven dress properties glide from their held values; **the FILLS drain by a slow `chipdrop` ANIMATION, not a transition — an animation-held value does not hand off to a transition on class removal (measured: the fill snapped to scaleY(0) frame one)**; the fly stays parked on the slot until 930ms and the real card takes over only once the drain has left it at rest (rest == rest, no seam). **The soft landing is SILENT — no landpress, no kclick** (his follow-up react same evening: "bounce and sound should both go"; the slide exchange had already set the phone's dialect — gestures land as visual fills, percussion is desktop's). **THE BOARD LIFT (same evening, his ask: "half way through [the fill + inner-edge draw] the board starts to lift slowly"):** every mobile arrival — open, slide, settle — arms `.blift` 440ms after `dockDress` (tokened on `dressN`; tset-guarded), which is the desktop hover grow as a RESTING state (`--bgrow: 1.07` + the deeper throw, .8s ease — the exact (hover:hover) values, so the two can't drift); it drains back down on the soft return's .93 clock and sheds everywhere tset sheds. **PINCH-TO-ZOOM (same evening, his spec verbatim: "2 inputs can pinch, one still there can hold the zoom, always retreats on input clear"):** two touches scale the lb card about the pinch's own start point (origin seated once per session — re-seating mid-zoom jumps the pose; midpoint travel pans instead), one remaining finger freezes the pose, a cleared hand plays the eased retreat. LOAD-BEARING GUARDS, both bitten in verification: (1) a pinch session voids the slide tracker AND the tracker's touchend re-checks `pzLive`/a 600ms `pzT` recency window before ANY step — the sim delivered a two-finger path in a way the slide read as a leftward swipe and walked the deck; (2) `#lb { touch-action: pan-y }` on mobile keeps sheet-air two-finger touches ours rather than Safari's page zoom; (3) `pinchReset()` runs at the top of closeLb and slideExchange — a flight measured off a zoomed rect lands wrong; (4) the retreat's ghost click rides the `swcAte` suppressor so a zoom session can't read as a board-flip tap. The swipe is **FINGER-TRACKED**: touchmove drags the card live (its `touch-action: none` gives the whole move stream), release past 70px commits from wherever the thumb parked it, vertical hands back to the sheet scroll, the drag engine outranks (a hold is a grab), and `slideT` is a self-expiring token so no void path owes cleanup. Slides ride `xactN`/`xactPending` like deckExchange — rapid swipes land instantly, every press counts — and `xactSettle` now also parks the card's inline transform (no-op for desktop). **`buzz()` is the haptics helper**: `navigator.vibrate` on Android, the iOS 18+ switch-input side door otherwise (grab / dock landings / slide commit; NOT gated on SNDON — a muted phone still taps back; UNTESTED on real hardware, the sim has none — rip the iOS hack out if 26/27 closed it). **Known open:** the commit threshold is distance-only — Dylan's claw-grip pointer-finger flicks felt wrong where thumb swipes felt right; a velocity OR-term (fast + short also commits) is the likely tune.
  **THE 8/8 DAY PASS (2026-08-08, event morning — six commits, all sim + headless verified; supersedes pieces of the passages above):** (1) **THE PHONE IS WHITE**: mobile forces light at boot AND at the 940/hover crossing, UNPERSISTED (`setGround(g, persist)`) — the stored `avrg-k-ground` is the desktop comparison state and must survive a phone visit; the menu GROUND row still flips live. (2) **THE CASE REWORK**: JUST PAST HALF screen (`left: 46vw` — the morning's 3/4 at 25vw was walked back the same evening, Dylan: "3/4 is to much"; the original was 50vw, so the knob has now been to both sides of this number), height `calc(100dvh − tbh)` NOT `bottom: 0` (fixed-bottom anchors under Safari's dynamic bar; dvh tracks it), the left face is LINE—DOT—LINE (`.bay-inner` re-shown at `left: 8px` inset — flush at 0 merged into the −7px announce shadow as one slab; the shadow is the edge ink, the marks are the inner frame line; `--rp: 1` on mobile `.bay`, `.bf-face` hidden, the `.shadowed` dot-yield overridden), **the close arrow is GONE** — the door is swipe-to-close (`swc` handlers by openCase: finger-tracked, commits past 70px, rubber-bands, leftward resists /3, cards excluded, vertical hands back to the column scroll, a time-boxed document-capture suppressor eats the release click) — **and since the evening react the door swings BOTH ways: a leftward swipe on the browse OPENS the case** (`swo`, seated by the suppressor: commit fires MID-MOVE at SWC_TH so the case starts arriving under the still-down finger — the swipe is caught, not answered at release; rightward is never claimed, Safari's edge nav owns that direction; vertical stands down to the page scroll with no preventDefault anywhere; home never arms per the showroom law; grid cards are fair launch surface because mobile grids have no horizontal verb; the committed release's ghost click rides the same `swcAte` box), head = MSG bar alone (tags off), shelves inset 14/10, cards SIT ON their strips (`padding-bottom: 6px` ≈ strip + shadow drop — they hung ~40px above under the desktop --shelfPad). (3) **THE DRESS GRAMMAR** (his three screenshots): shelf cards are ALWAYS dressed on mobile (the `.held` negative-delay idiom on `.bay-item` at rest — buildFly's held check reads CLASSES not CSS, so flights still ride clean off them, one gesture each end); the inspected/swiped-in card ARRIVES CLEAN and dresses AT the landing (`dockDress()` at the 430ms open beat / SEAT_TUNE.dur for shelf arcs / the slide's IN+30; `xactSettle` dresses instant settles); **`dockUndress()` is the inverse and it must be a SNAP** — shedding the previous inspection's tset while visible played the rails' .56s teardown on screen (closed→open→closed, Dylan's catch); `.snap0` mutes descendant transitions for one frame (descendants ONLY — the card node's own transform transition belongs to the slide). GENERAL LAW from the same session: **the two lb arcs anchor CARD BOX to CARD BOX** — bayFlight sizes the fly div (a whole template card) to sr.width, so a PANEL-rect sr rendered the fly under-sized with visibly different frame proportions (his "different widths" catch); `anchors()` stays panel-to-panel (a rigid zoom of the real card, same mapping). The lb→shelf arc keeps its dress mobile-side (`drain: dockActive() ? null : rest`) — it lands on a dressed shelf now.

## Mobile pass 1 (2026-07-20)

- **Mobile pass 1 — shipped 2026-07-20 in index.html.** Frame v3 is now the page default (localStorage still wins); the ground/card switcher collapses to an `SW` chip under 940px; the mobile home tiles are collection cards in a frame-idiom lite (white card, inner frame, count chip AND accent-dot "N BOARDS" line — both counts kept on purpose); no collection has its own page anymore — `renderColPage` renders the selected collection first then every other section below it, one scroll, on all screen sizes (desktop shares the renderer); on touch the fv3 lightbox corner chip decodes to `n/N` position on open and every swipe (swipe-sesh wayfinding, scoped to the collection chunk; desktop keeps ref→AVRG hover). **NEXT: mobile polish sprint — mobile is what most visitors see.** Open design questions from the pass: name-over-board legibility on busy cutouts (top-bold / bottom-bold are the fallback variants), and the collection-card decode flourish (name → "N BOARDS AVAILABLE") needs a touch trigger decision: scroll-into-view once, idle cycle like the taglines, or on-tap-before-navigate. DONE 2026-07-22: **mobile inspection rework** — tap = the hover: `.tset` on the lb card lands the chip fills/rail close/status decode as the zoom settles (dock gate). The tapped grid card lifts out (`visibility:hidden`, slot stays empty) and the close flight lands back in that slot — since 2026-07-22 anchored whole-panel-to-panel (the ONE CARD SPEC made the aspects equal; the original square-to-square anchor existed only because the two specs' aspects differed, which is also why frame↔panel used to land low), and the lift-out runs on desktop too. Card is the hero: desc/product/count hidden ≤720 (DM path = lisst), `.lb-rest` snaps away in .1s on close so thumbs don't ride the return. Sheet starts below the still-live topbar (`--tbh` measured at boot/resize) and flex-centers the card (`margin:auto` degrades to scroll on overflow). **fv3 card got top room** (22.41% of card width — 52px at the 232 reference) so the board sits optically centered — and since 2026-07-22 the `.corner` chip is a `.panel` child in both the renderer template and the lb markup (the stage-anchored negative-top compensation is retired). DONE 2026-07-22: **touch/pointer behavior forks are capability-keyed, not build-split** — all card `:hover` choreography (chip wipes, rail closes, board grow, panel shadows, wchip/bchip inverts, lb image grow) sits in `@media (hover: hover)` gates, mirroring the JS `canHover` gates. Reason: iOS sticky-hover fired the chip wipes as a truncated prelude on tap before the lightbox opened. Touch gets its own scripts (lb n/N decode, dock flight) instead. Pattern for future mobile-only behavior (scroll shadows etc.): add it behind `(hover: none)` / `dockActive()`, never a separate build. DONE 2026-07-21: **card proportion lock** (`lockCardScale`) — shelf-grid cards render at the 232px desktop reference width and transform-scale down to their slot, so narrow-grid cards are photographic reductions of the desktop card (one spec, no mobile override block; desktop slots are 232px so it's a no-op there). Gotchas: call it only when the grid is visible (hidden grids measure 0 → scale(0); route() re-runs it after unhiding), and the mobile shelfgrid tracks must be `minmax(0,1fr)` or the unscaled 232px card balloons the track's auto min-content and the page scrolls sideways. Follow-up DONE 2026-07-22: the d7f belt pass retired onto the cqw ONE CARD SPEC (the `.d5`/`.paper` belt furniture pass remains hand-tuned).

## Mobile deck ('the dock') — spec + retired takeover-case history

- **Mobile deck ("the dock") — specced 2026-07-20, not started.** Mobile doesn't get a smaller drawer, it gets a different organ; decisions from the scoping session:
  - *Topbar icon* replaces the lisst text on mobile: receipt glyph is the lead candidate (the count badge does the conventional "cart" work, so the glyph is free to have character). Empty-state escalation: first tap decodes to a red numerical **0**; a second tap while still empty gets an annoyed shake.
  - *Adds happen only from the zoomed card page* on mobile (no grid-level +List chips — thumb mis-tap territory, and tap-card → product view → add is the expected path). Confirmation is the topbar icon reacting: shake + count bump. Site-fade on inspection and the card animations stay; grid is 2 cards wide minimum before zoom.
  - *The dock itself*: right edge (matches desktop, dodges the iOS back-swipe), slides open and takes over the screen — a display case made for the cards. Presentation, not a cart page.
  - ***TAKEOVER CASE + MOBILE TOSS — SUPERSEDED 2026-08-12 (react round 6): the case is RETIRED on mobile — the deck is the YOUR PICKS page + header minis now; see the strip/deck passage in the k-home-dual bullet. openCase survives door-less (route/boundary cleanup only); the passage below is HISTORY.*** (Was: SHIPPED 2026-07-24 (evening session), awaiting Dylan's real-thumb react.) Dylan's standing rulings honored: deck-open + card-from-click animations kept, NO shop reshuffle on mobile, deck closes on collections browsing. What shipped, and where the bodies are buried:
    - **The case is `#bay` in a mobile costume, not a second organ** — a `dockActive` CSS block turns the desktop drawer into a **HALF-SCREEN cabinet (`left: 50vw`, Dylan's phone react 2026-07-24): the case hangs over the browse — the shop's right column reads as covered by it, the left column stays in view at the case-fade .22 faint** (slides as ONE piece on a CSS transition; the chrome column slims for shelf room — navfx art off, ✕ arrow at left 11, shelves at left 48/right 10 — so cards keep their ~101px height-capped size). `renderBay`, the FLIP re-stack, preseat/pendingSeat, chip law, and the ✕ all run unchanged — that was the point of one organ. `openCase()` dresses the bay in the full threshold-class outfit (`CASE_DRESS` = mopen/inked/shadowed/pulled/fullish/locked) so desktop chrome reads fully-open with no driver; `bayTick` BAILS under `dockActive()` (also keeps `bayPose`'s class toggles from undressing the case — bayP is 0 there); mobile-block `!important`s retire any stale inline pose from a resize across 940. **CASCADE GOTCHA: the costume block must sit BELOW the desktop bay CSS** — same-specificity overrides (the ✕'s opacity reveal, `border-left` vs the panel's `border:` shorthand) lose to later base rules otherwise (bitten once). Case z is **4800, UNDER the lb (5000) — the desktop inverse** — so a case inspection floats the card in front; `closeLb` unlocks scroll only `if (!caseOpen)` (a case inspection closes back INTO the locked case). **iOS scroll lock is a body PIN, not overflow:hidden** (bitten on the phone — iOS Safari scrolls through overflow:hidden): `lockScroll` under `dockActive` sets `position:fixed; top:-scrollY` on body and `unlockScroll` restores the exact spot; re-entrant locks (case open → sheet opens) keep the FIRST y via the `position !== "fixed"` guard. `.bay-panel` also wears `touch-action: none` so case touches never scroll-chain to the page. Door wiring: picon toggles (preventDefault under dockActive+framev3; other card modes keep the #my-lisst link), `route()` closes (modal law), gutter/anywhere tap closes via a capture click handler (guarded: not while lb open or dragging), Esc closes, a `matchMedia` change listener hands the deck between its two bodies on boundary crossings (drawer stands down going in — incl. `shopRoom(false)`, case going out). Empty-state escalation per spec: first picon tap decodes the count to an angry red 0 (`.pcnt.angry`), second tap = dockPing shake; an add forgives (in `lisstTab` — **`piconPoked` is declared next to `lisstTab`, NOT in the case block: lisstTab runs at boot and a later `let` is a TDZ crash**).
    - **One-card law is UNIVERSAL now** (`applyDeckHoles` ungated). The mobile add paths call `renderBay()` before `dockAdd` (visibility keeps layout, so the flight still measures the hidden slot's rect). **An lb add takes the card WITH it (Dylan's phone react, 2026-07-24): both lb add paths — the fv3 chip and the plain button — run `dockAdd` (measures the lb card's rect) then `closeLb(true)`; the toss-add does the same at release. A board must never sit in the dock AND the lightbox at once.** `closeLb` still guards `backCard.classList.contains("indeck")` → null as insurance: never fly a close at a hole.
    - **Mobile removes = the off-stage exit generalized** (`mobileExit`/`mobileExitFly`): the shop is never visible under the takeover, so nothing lands on screen — the card swings up-RIGHT (**negative `hook`** pulls bayFlight's control the other way) and exits LEFT through the gutter; pendingHole held to touchdown, no landing beat (the FLIP re-stack's settle click is the remove's one sound). The ✕ forks to it under `dockActive`.
    - **The touch door in the ONE drag engine** (no parallel mobile module): pointerdown accepts touch under `dockActive` — the SHEET card arms a **press-hold** (`holdMs` 190; pre-hold travel > `holdSlop` 9 cancels and hands the touch back to swipe-nav), CASE cards lift on travel alone (`touchTh` 12 — thumbs wobble bigger than mice); grid/belt cards stay tap-to-inspect on mobile (the 2026-07-20 adds-live-in-the-sheet ruling). **A mobile sheet grab does NOT convert the inspection at lift** (desktop closes the lb at lift; mobile keeps it — `src` = `lbCard` itself, hidden at lift, so a CANCEL springs back INTO the sheet and accidental lifts don't eject). The lb only releases when the card actually leaves: a toss-ADD closes it at release (+ manual `lbCard` visibility restore — the lift hid it and closeLb doesn't know; same restore on the toss-remove). Release rules: **rightward throw or release within `dockZone` 92 of the right edge = the add** (`dockHandOff`); **leftward throw or release past the case's own left face — anywhere over the browse half — = the remove** (the half-screen case makes "away from the drawer" a real place; measured live off the bay rect with a `.6·vw` cap, no knob); anywhere else springs back quiet. The lb swipe-nav handler is guarded `if (drag || dragStole)`. CSS: `.bay-item, #lb-card { touch-action: none; user-select: none; -webkit-touch-callout: none }` — touch-action none keeps pointermove alive mid-carry (no pointercancel ambush) and does NOT break swipe-nav (pure JS touch events still fire); cost: sheet overflow-scroll can't START on the card. `reShelve` (the by-id re-target spring) was extracted from dragEnd and is shared desktop/mobile.
    - **`dockPeek` + `dockHandOff`** (below `dockAdd`, sharing its seat math + landing): a sheet lift peeks the sliver to 58% (the catcher shows itself — pickup-wakes-the-case, mobile dialect); release-add rides a short tail (~.52s) from the hand into the sliver slot — shallow arc, seat, button press, kthunk + dockPing, carry-out — joining the sliver's open curve from the peek. **The peek is a CSS transition; every flight (`dockAdd` too) sets `kdock.style.transition = "none"` before per-frame transform writes** — a live transition tweens every frame write into mush otherwise. `dockFlying` guards both (concurrent chip-flight → instant seat fallback). The in-hand fly is the fv3 card (buildFly) — no mini-card swap seam; the mini-card stays the CHIP path's ceremony (dockAdd untouched, Dylan's keep).
    - **Verified** (chrome-devtools MCP, 390×844 touch + 1440 desktop): chip add → hole/sliver/case; hold-lift → peek → zone-add and throw-add; slop-cancel keeps swipe-nav; mid-screen release springs back to the sheet; case toss-out + ✕ exit with survivor FLIP; browse/gutter/Esc close; case inspection over the open case + arc back to shelf; desktop drawer/drag/chip-law regression clean. **Synthetic-touch caveat: pointerup coords don't update `d.px` (dragEnd reads the last MOVE) and no real finger was involved — the hold/pop/zone FEEL needs Dylan's phone.**
    - **React pass round 1 landed (2026-07-24, same day): case → half screen over the browse, iOS scroll pin, lb adds take the card with them, toss-out boundary = the case face.** Card size and the case-open/card-from-click animations confirmed good on the phone. Still open: `holdMs`/`holdPop` feel on a real thumb, a lift sound (the toss lands with kthunk+ping but lifts silent), and the 3-shelf escalation knob if the column ever wants bigger cards.
  - *Add animation* — **DOCK-LITE RETIRED 2026-08-08 (the one-body pass, Dylan's rule: mobile is the same object in a lighter costume — a costume may only remove or resize, never introduce a new organ).** The `#kdock` sliver, the `.kdock-fly` mini-card, `dockAdd`/`DOCK_TUNE`/`dockPeek`/`dockHandOff` and the `avrg-k-dock` pick are DELETED (the pick settled by deletion; dock-add-lab (vault) is historical). **The mobile add is `caseCatch` now — the shared engine end to end:** buildFly (the REAL fv3 card, dressed if its source was), the takeover case slides in to catch it (CASE_DRESS classes ONLY — no caseOpen/fade/scroll-lock, a catch is not a modal), seatFly lands it on its REAL preseat shelf slot (per-frame getEnd tracks the case mid-slide), then the case slides back out behind the landing — his call: **"catch and shut."** (Watch item 2026-08-08 evening: Dylan felt the catch-and-shut "a bit laggy" on his phone at the event while the same build ran smooth on the sim and desktop — possibly his phone's stale cache; recheck on real hardware before tuning anything.) Tokened (`catchN`): a second add mid-catch retokens so the shut waits for the last landing; a real openCase mid-catch wins (shut guarded on `!caseOpen`). `dockPing` + `buzz` survive as the topbar's confirmation; landing sounds are seatFly's own. seatFly gained an `opts.after` hook for this. Open for the thumb pass: whether a sheet LIFT should wake the case early (the old sliver peek retired with no replacement — the case arrives with the catch at release). The passage below is HISTORY only:
    (Was:) fully automated, no variance, beats overlapped. Choreography explored in `dock-add-lab.html` (vault) (see Layout — three path variants with saved presets). **DOCK-LITE SHIPPED in index.html (2026-07-21):** the lab flight runs on the real mobile layout. `#kdock` = right-edge display-case sliver (exists only during the flight for now; full takeover case still to build). Receipt icon + count badge replaces the My Lisst text tab on mobile (`.picon`, links #my-lisst, shake+bump on add — note it sits UNDER the open lightbox scrim, so drawer+thunk are the visible confirmation mid-lightbox). Engine: `dockAdd()` — the lab's spline driver with all three paths, presets baked as `DOCK_TUNE`, coordinates scaled from the lab's 390×720 to the viewport, start pose MEASURED from the real card rect (bayFlight's lesson), landing press/shadow/thunk ported. **DOCK IS THE ONE PICK THE 2026-08-01 SWITCHER REMOVAL DID NOT SETTLE.** Its row went with the panel, but the choice is still OPEN and `avrg-k-dock` still reads from storage (ramp/toss/swoop) — because it is a MOBILE flight, and the desktop-first rule says don't pick a phone feel from a resized window. To compare with a phone in hand, set the key by hand; there is no UI. At the mobile pass, bake the winner as a const and delete the key. Do NOT read the stored value as a decision. All three add entry points route through it when `(hover:none) or ≤940px`: fv3 lightbox chip, plain lightbox button, grid chips; desktop keeps flyToBay. Reduced-motion or mid-flight adds get icon-ping only. Tuning loop: retune in the lab → re-bake `DOCK_TUNE` (site reads baked numbers, no live knobs).
  - *Drag-to-deck (desktop)* — SHIPPED 2026-07-24 (see the bay bullet's DRAG-TO-DECK passage). The original "earned shortcut" gate (unlocked after first button add) was dropped: the affordance is visibly taught, so a dead drag would be an unfulfilled invitation. The tone half survives: snappy, not ceremonial — a drag is the user performing, ceremony stays with the chip.
  - *Tone law* (general, decided here): when a ceremony risks getting tiring, never shorten or hide it — escalate the absurdity at the same speed. Reserved variant for add-fatigue: wild spin into place, slam shut, sparkly rainbow trail, over as fast as it happened.

## The send takeover (2026-08-13, react round 1)

The YOUR PICKS send press stopped revealing doors inline and started CLEARING THE ROOM — Dylan's sketch, built as: press → `.picks-above` (masthead + links + cards, a new dock-only wrapper) fades to white AND rides up off screen in one motion (height+opacity+translate on one .5s transition; JS pins the measured height first; `flow-root` keeps child margins from collapsing through and shorting the pin). The document shrinks with it, so the viewport clamp does the scroll-up for free. What remains fits one screen, unscrollable — his law: "the site needs to stay here. no scroll." MESSAGE COPIED pops instantly in the h2 register (order -3), holds ~1.1s, fades over 2.4s, KEEPS ITS SPACE (opacity, never display — the doors must not jump); doors + paste decode ("— paste it to me here") arrive together at .48s (order -2), seated ~3s before the banner is fully gone ("buttons should have arrived for a second or two"). The msg box + SEND MESSAGE stay: a re-press in sendmode is the failed-paste recovery — bdmCopy re-rides the gesture (popup-jail law intact) and the banner re-pops via the animation-none/reflow restart; the collapse does NOT replay. `· done ·` small at the bottom = a fresh renderLisst() + scrollTo(0,0); offers survive in bdmComments. Timing knobs all named in the .picks-above CSS comment. Verified 15 Pro sim end-to-end (takeover, re-press re-pop, done restore); the pane only audited DOM states — its throttled transitions FROZE the collapse mid-flight (height stuck at start value, pointer-events/overflow landed — rule matching proven by the non-transitioned properties, the gotcha's signature).

### Round 2 — the lock (same day)

Dylan: "lock the screen from the message box down, it rides the scroll up at the moment." The collapse RETIRED — a shrinking document rides everything up on its own scroll clamp, which is exactly the motion he flagged. Now the press measures the message box's viewport seat, freezes the page under it (`body.sendlock` = body-fixed at -scrollY, the iOS-proof lock; undone by `sendUnlock()` — called by · done · AND at the top of route() so a topbar escape can't leave the page frozen), and pins the lower group out of the flow: `.picks-send.sendmode` fixed at `--lockY`, banner fixed under the topbar, doors fixed with their bottom at `--doorsUp` (14px above the box). The above only fades + rides (-55vh transform, zero layout cost). THE CLAMP PAIR: lockY clamps to `innerHeight - 250` (the done row's room below) and the regular page's `.picks-send` bottom buffer is 128px, sized so the full-scroll rest seat EQUALS the clamp — press at full scroll and the box moves zero pixels (sim-measured 1277→1275). Trim one, trim the other. px snapshots not vh (iOS toolbar lies; the lock holds the bar still anyway). Sim-verified: zero-move press, hard swipe frozen, done unlock + restore, doors/done seated in the cleared field.

### Round 3 — breathing room + big doors (same day)

His beats, all landed: the screen CLEARS FIRST (ride-out .55s), THEN the banner fades in (.58s), PULSES once (1.05s, scale 1.055), and only after the pulse do the doors + decode land (1.6s — the setTimeout and the animation chain are a knob PAIR, move both); the fade-out is 5.5s starting 1.8s ("slow fade out (5-6 seconds slow)"). Banner seat rose to tbh+8. Copy: "— paste it to me here" → "— send it here" (label uppercases it). Doors grew to center stage: 62px chips, 32px glyphs, 48px gap, 11px labels. The bottom stack tightened on his cuts — send→done space halved (done margin-top 26→4, +18 gap = 22), done→bar space quartered — which re-derived the clamp pair: lockY clamp 250→220, buffer 128→100 (the same-amount law). Sim-verified: zero-hop press intact at the new pair, all beats visible, SEND IT HERE + big doors seated.

### Round 4 — send it @, icon-centred fly-in doors (same day)

The paste line left the doors block: fixed under the banner (tbh+52) reading "send it @" (dash gone, same 1.6s reveal + decode beat). The doors grew to 76px chips / 40px glyphs and the row now CENTRES BY THE ICONS — each door column is exactly chip-wide, the label hangs absolutely below (nowrap), so the long gmail address can't drag the pair off-axis; the JS doorsUp offset grew 14→44 to cover the hanging labels. Entrances: reddit flies in from the LEFT edge, mail from the RIGHT (translateX ∓70vw, .6s eased, per-door keyframes on the ids — the container's rise retired). Gmail label displays without .com (his call); the mailto href keeps the full address — the labels-are-destinations law bends but the door still names its destination. Sim-verified: line under banner, pair dead-centred by chip midpoints, labels clear of the locked box.

### Round 5 — send it @ joins the banner register (same day)

"send it @" became the banner's sibling headline: same Satoshi-900 clamp size, same pcopIn entrance, .4s behind the banner's start (delay .98s vs .58s — a knob pair). It left the doors' reveal gate (own element before #picks-doors, unhidden with the banner; the decode + small-mono costume retired) and sits CENTERED in the field between the banner's bottom and the doors' top. TWO iOS BITES fixed en route: (1) the doors' bottom-anchor-in-px lied when Safari's toolbar re-expanded on the tap — everything in the locked state is TOP-anchored now (--doorsTop = lockY - 120: 76 chip + 23 label hang + 21 box clearance); (2) press-time getBoundingClientRect lied the same way (fixed-element rects lurch during the toolbar transition), so the line's seat is COMPUTED, not measured — both headlines share a font so the height terms cancel: --pasteY = (tbh + lockY - 112) / 2. Sim-verified centre 742 vs true midpoint 744.

### Round 6 — the cluster rides up + the line bounces (same day)

His annotated screenshot: the upper cluster compresses toward the top as one column. The midpoint seat retired — the cluster HANGS FROM THE TOPBAR now: --doorsTop = tbh + 206, --pasteY = doorsTop - 91, collision-capped at lockY - 120 so small rooms degrade to the round-4 box-relative seat (one setter, in the banner block). Sim vs sketch proportions: banner 19.5% vs 22, line 32 vs 33.5, chips 45 vs 46.5, box 70 vs 67 — on the drawing. And "send it @" got the banner's bounce: pcopPulse .55s at 1.45s — the same in-then-pulse rhythm, every beat .4s behind its twin (in .98 vs .58, pulse 1.45 vs 1.05).

### Round 7 — handles fly first, platforms move into the chips (same day)

"the buttons enter as soon as the user names land": the HANDLES took the doors' flight (same ∓70vw paths, same 1.6s beat, .6s eased) and the chips follow with a .6s delay — the flight's own length, so each chip launches the moment its handle touches down (lands ~2.8s). Handles moved ABOVE the chips (pd-l bottom:100%, still hung off the icon-wide column — centring untouched); the platform names moved INSIDE the chip bottoms as white 9px text (.pdt: "/reddit", "Gmail" — the .label class prints them caps, /REDDIT · GMAIL; drop .label from .pdt for literal case). Mail handle trimmed to the stem: AVRG.FB@ (gmail joined its chip; href keeps the full address). Glyphs nudged up 5px to cede the bottom band. The .pd-l flight keyframes CARRY the centring translateX(-50%) — a bare translateX would drop it (to/from both spelled). PLUS a phone-wide hardening: html painted white under (hover:none, ≤940) — iOS toolbar collapse offsets the layout viewport and was showing the dark ground as a black band above the fixed topbar.

### Round 8 — the verb swap (same day, his "final touch")

The button's label catches up to its function: every press in the locked state re-copies (always did — bdmCopy rides each gesture), so as the banner takes its 5.5s fade, SEND drains into the slab ON THE BANNER'S OWN CLOCK (the same pcopOut 5.5s @1.8s), and COPY fades into the emptied seat at 7.3s (.9s — the clock's end). Stacked verbs in one inline-grid cell; both are 4 mono chars so the slab never changes width. The re-press banner re-pop does NOT resurrect SEND (separate elements, forwards fill holds); · done ·'s re-render restores it. Under reduced motion the banner never fades, so SEND staying put matches. Sim-verified: COPY MESSAGE seated post-fade, re-press re-pops the banner over a COPY-labelled slab.

## THE PICKS CEREMONY (2026-08-15/16) — the arrival becomes a sequence

**THE LANDING BUG CAME FIRST, and it was one line shared with thread 3.** `route()` ended in a plain `window.scrollTo(0, 0)`; `html` carries `scroll-behavior: smooth`, so that scroll ANIMATED — a route fired from a scrolled page painted the NEW page at the OLD offset and glided ~420ms to 0 under the reader (measured 515 → 484 → 208 → … → 0). That is both Dylan's "you can land there and it can feel like a strange collections viewing" AND the drop menu's "how it jumps the page below it", since route() is shared by every hash change. `behavior: "instant"` — the same lesson `unlockScroll` paid for on 08-08. Verified per-animation-frame: twelve consecutive frames at 0.

**THE CEREMONY ITSELF (his beat-by-beat spec, after three quick variants — A deal / B draw / C state — were built, reacted to, and retired into it; C's state-and-pulse keyframe survives as the masthead's entrance).** In order: (1) **the departure** — tap the stack from anywhere and the rest of the site drains under the bar (`body.pxleave`) BEFORE the hash flips; the bar is exempt, being what you tapped and where the cards come from. All three doors to YOUR PICKS (stack, the browse's left-swipe commit, the case's MSG bar) route through `leaveToPicks()` so the page cannot arrive two ways. (2) **the masthead states and pulses**. (3) **the deal** — each pick flies OUT OF THE STACK into its slot on the house `bayFlight` arc, i.e. pickCatch run backwards. (4) **the offer box answers its own card** before the next launches ("card lands. then the offer box below it. then the next card"). (5) **the page walks itself down** — a below-fold slot is scrolled into view BEFORE its card launches ("rather than deal it off page"); `walkTo` returns 0 for an already-visible slot so a 2-wide row never pays for a scroll it doesn't need. (6) **MESSAGE, then SEND**, each landing with a rise-overshoot-settle (`paStamp`) and a deliberate 620ms pause between them — the pause is what makes them two statements. (7) **the page rides home and a caret starts blinking in the first offer box.** LOAD-BEARING: layout must never move during the ceremony or every measured landing rect is a lie, so held parts use visibility/opacity and NEVER display; the fly wears `.lisst-card` so it lands dressed against a dressed card (rest == rest); no `drain` on the flight (the deck's flights drain because they land on a REST card — the opposite case); and `.picks-deal` comes off at the end so an interrupted ceremony can never strand a part invisible. Plays from ONE call site (route()'s my-lisst branch) — never on a pick-removal re-render, · done ·, the lb's soft close, or the 940 boundary re-dress.

**THE FLY MUST BE BUILT AT THE DESTINATION'S SIZE.** First cut launched it at the header mini's box rect and it flew as a black slab of garbage (Dylan: "getting a weird animation on the card flight"). The header mini is NOT a small card — `.hpick .d7f` is a full 232px card under `transform: scale(.1078)` inside an 8px clipping window — so an 8px-wide fly rendered a whole template card into 8px. The fix is pickCatch's own shape reversed: build at the DESTINATION card's size (the template renders at the width it was drawn for), centre the launch rect on the mini, and let an inner `.deal-grow` wrapper carry the growth from mini scale to full. The wrapper is load-bearing — `bayFlight` writes the fly's own transform every frame and would overwrite a scale set there, so the two transforms must live on different elements. Measured: inner scale 0.114 → 1.000 across 618ms, landing on the slot centre at exactly the slot width.

**THE STACK IS DEALT AWAY, AND THE MINIS ARE THE CARDS** (his react: "have the deal happen from the top card... and have it stay gone on deal... a move anywhere else should repopulate them"). THE RULE: a pick is either in your hand or on the table, never both. Each deal takes the FRONT mini and it does not come back; the box collapses (`.hp-gone`) so the stack really shrinks (measured 4 → 3 → 2 → 1 → 0); leaving repopulates via route(); and `renderPicks` self-gates on a `stackDealt` flag so a re-render landing while you are still on the page (removing a pick) agrees instead of quietly refilling the bar. THE FLAG'S DIRECTION MATTERS: arriving must leave the stack FULL (the ceremony needs cards to deal), so only a COMPLETED deal sets it and every route clears it. An earlier cut had the mini RETURN at touchdown ("absent only while its card is in the air") — honest per-frame, but it lost the thing he was after.

**THE STACK'S SECOND PASS (his sketch): front facing LEFT, fanning behind to the right, smaller, diagonal, tighter, capped at 4.** Facing left needed an explicit z-index ladder — paint order used to invert the fan for free (sliver boxes, artwork overflowing right, last on top) and front-left means FIRST on top; `renderPicks` writes it descending since it is the only place that knows the count. It also renders NEWEST FIRST, which is what keeps the ceremony honest: the deal takes the front card and the grid renders newest-first too, so the mini that leaves IS the card that lands (re-verified 4 of 4 against a live ceremony). Artwork 25 → 22px, sliver 8 → 5px, a 2px per-card drop off `--i`, stack width 49 → 37px, `HPICK_MAX = 4` with the count carrying the rest (verified at 6 picks: 4 rendered, count "6"). `:last-of-type` carries the full 22px width so the container holds the rightmost card — `:last-child` could never work, the count span is the real last child, which is why that old rule had been silently dead.

**THE CLOSING ARC + THE BOTTOM BUFFER'S PAIR.** The ceremony used to end wherever the last walk left it, dropping SEND MESSAGE behind Safari's floating toolbar; it now scrolls to put 150px under the button first. That needed the buffer to grow — a buffer is only worth what the page can actually scroll to, and at 100px the document ran out with 103px under SEND. `.picks-send` 100 → 160 AND **the paired lock clamp 220 → 280** (they differ by a constant 120 — move one, move the other). Zero-move press re-verified at the new numbers: message box top 562 → 562 → 562 across the press. THE CARET is DRAWN, not focused — focusing would throw the iOS keyboard over the page the ceremony just laid out — and its seat is measured off the real input because the row is a flex box whose label width moves with the copy.

**THE OFFER FIELD'S KEYBOARD** (Dylan on real glass: "selecting the offer box... pulls the picks header and the sites header with it. then the header poofs until you scroll"). iOS opens the keyboard by shrinking the VISUAL viewport and scrolling it inside the LAYOUT viewport; `position: sticky` pins to the layout one, so pinned chrome stays glued to a top that is now off-screen. Two moves: a capture-phase `pointerdown` SEATS THE FIELD IN THE UPPER THIRD BEFORE FOCUS (iOS only hauls the viewport because the field sits where the keyboard is going — remove the reason and nothing is displaced), and `--vvTop` tracks `visualViewport.offsetTop` under `body.kbd` as the safety net. Scoped to while an offer field is focused: the address-bar collapse moves offsetTop during ordinary scrolling too, and tracking it then would walk the bar down the page. **Confirmed working on Dylan's phone 2026-08-16.** THE SIM CANNOT TEST THIS — it runs a HARDWARE keyboard, so the visual viewport never shrinks and the bug cannot reproduce there at all.

## THE PICKS PAGE FINALE (2026-08-16 — seven react rounds, one session; his close: "thats it for the picks page. Really happy with it")

**The ceremony's final clocks (all in `PX`):** the CARD DEAL went ×2 (hand 72 · deal 233 · settle 100 · ask 130 — the 25% cut's successor; hpOut .09s + the 90ms hp-gone timeout track hand, three places one beat) while the CLOSING went the other way — stamps .62→.92s, `tail` 1000 (SEND lands as MESSAGE's pop settles — round 5 tightened round 2's 1700), `rest` 1600, `home` 1240. **paLand is RETIRED** (the .35→1 landing ramp read as a flash; the fly holds its last frame and lands dressed on a dressed card — rest == rest needs no ramp). **THE WORD POP replaced the breathe after one react** (his correction: "grow big once and settle... more like the bounce message copied gets"): `paWordPop` (.55s, delay .82s INSIDE the .92s stamp — a knob PAIR, move together), one-shot per landing, `--pop` peaks 1.45 (label) / 1.2 (button text — must stay inside the slab's 24px side padding), independent `scale` property (the label holds a translateY seat), `.picks-deal`-scoped: a pop is a landing's answer, re-renders just place things.

**THE SCROLL IS TWO DRIVEN RIDES, nothing native:** `glide()` (rAF + easeInOut, every write `behavior:"instant"` — css smooth turns per-frame writes into mush, route()'s own lesson; native smooth owns its ~.4s clock so its duration can't be authored). Ride one: ONE CONTINUOUS PAGE-DOWN — the per-row walks and the closing seat scroll are FOLDED INTO a single glide launched with the first card, length = dealTotal × `PX.rideX` (1.5 — at 1:1 the eased middle outpaced the cards, his round-6 catch), ending at the seat that leaves SEND 150px of toolbar room (paired with `.picks-send`'s 160 buffer — the clamp pair lives on). The end seat is measurable BEFORE the first card flies because ceremony layout never moves (the held-state law), and bayFlight re-measures landings per frame so cards land true on the moving page. MESSAGE stamps as the last card settles (mid-ride, in frame by construction); the ride await sits BELOW msg.in so SEND still waits for the parked seat. Ride two: the ride home, unchanged. **walkTo, PX.walk and PX.seat are DELETED — do not resurrect the stepped walks.**

**THE DEAL SHIELD:** `body.dealing::after` — transparent fixed pseudo over EVERYTHING including the bar (the stack mid-deal is dealing itself), **z 6700 = a new top rung above the topbar's 6600** (the z-ladder gains a member). `touch-action:none` stops iOS panning; clicks resolve to BODY (a pseudo's events target its origin element) so no handler ever fires — proven by elementFromPoint. Up with the gate, DOWN the moment SEND lands (its pop plays on an unlocked page); route() clears it as the belt.

**THE PICKS-CARD TAP (round 5): flip + caret walk, never the lightbox** — on the phone this page IS the inspection. Tap flips the board to its canva-pair other face (`BOT`; no pair = no flip — originals/resale ride the reshoot interim) via squash-swap-expand on the independent `scale` property, both faces WARMED at render. **The debounce is a SELF-EXPIRING TOKEN (`flipT` timestamp, the slideT law) — a cleared-flag version died when frozen timers held the flag** (pane-reproduced; Safari freezing a tab mid-flip would lock the card for real). `showOfferCaret(inner, target)` — the caret WALKS to whichever card was last named; one caret ever; a tap on the offer box walks it without flipping. Desktop My Lisst keeps openLb (M forks).

**THE BENCH (round 7, the finale): the ✕ is TWO TAPS.** First tap sets the pick ASIDE — the card's ART dims to .38 (**the `.d7f`, never the scard — parent opacity would dim the armed ✕**), the offer box slides off toward ITS OWN COLUMN's screen edge (`bx-l`/`bx-r` off the cell's real midpoint) **keeping its space** (the bench never reflows; only the removal's re-render does), the ✕ arms RED (#d21f1f, at the same three-class depth as the seat rule — the light-ground disc override wins any two-class tie), and the board's chip leaves the MESSAGE line (`.ptag.off` by data-id — `[hidden]` would lose to the inline-block display rule). The armed red ✕ removes for real: the whole cell rides off its side (`cell-out-l/r`, double-tap guarded by `cell.dataset.leaving`), then the re-render reflows. **A tap anywhere on the dimmed card brings everything back** — no flip, no caret walk; a benched card's only answer is returning. **The letter is CURATED with the line:** `bdmLines/bdmText/bdmCopy` take an optional skip set; the picks press passes `benched` so the clipboard, subject and both door hrefs filter in one seat (the same-string law holds curated; typed offers survive benching). **PER-VISIT state** — every route clears the bench; myLisst is the bookmark shelf (his frame: "book mark selections and then chose the ones you are interested in"), the ceremony always deals a full hand. Mid-visit re-renders re-dress surviving benched picks in the same synchronous pass (no animation replay). Benched asks drop pointer-events (an invisible box must not catch caret taps); the caret leaves with a benched box.

**Offer boxes (rounds 2–4):** 78% of card width, CENTRED under the card (round 4 walked back round 3's left-rail), margin-top 14 (supersedes the 36px read-equal derivation — his catch: "everything is a bit uniform in a way that makes it hard to read at a glance"; card+offer bind as a pair, the untouched 30px row-gap is the between-pairs air), padding 6/10/5, **the drawn kit WHOLE-PIXEL** (border 3, rails 3, pseudo -1/-3, label -1.5 — see the DPR-3 gotcha; re-derive the offset pair if the width ever changes).

## THE LOAD INTRO + THE FOCUS VEIL (2026-08-16 late — thread 3's mobile pass, with THE WORD IS THE DOOR in header-bar.md)

**His spec verbatim: "on initial site load, the logo draw needs to be the only thing on the screen."** Phone + home + motion only (a deep link boots straight to its page — no logo there to draw); armed by a TINY SELF-CONTAINED SCRIPT at the top of `<body>` (`body.intro`) because the main script runs at the end of body and the browser can paint first — the first paint must already be the opaque veil. The arm script may reference nothing from the main script (the TDZ law's fourth seat).

**The beats (driver at the script's tail):** pose — the logo FLIPs off its real seat to ~78vw centre (inline transform, `.intro-pose` gates the pre-pose flash, `playLogo()` owns the clock) and draws big on bare white (bar transparent, brand/picon parked ±70vw off-screen, tick held) → 2375ms: `.intro-shrink` — the pose transitions home (.85s) while the veil ghosts opaque→frost (the page appears BEHIND it, dimmed + blurred) → 3375: `.intro-tick` + `.intro-bar` — the tick's FIRST decode (startTick was deferred at boot; one starter, two callers, called once) and the bar pieces ride in, brand from the left, picks seat from the right → 4125: the veil rides down → 5075: clear. Shield at 6700 the whole way; 12s hard failsafe; reduced motion / desktop never arm. (Beat map re-derived 2026-08-16 for the ×2 draw: SMIL ends ~2.28s, shrink = draw end + 100ms, and the downstream offsets +1000/+750/+950 are the approved choreography unchanged — the draw's own clocks live in the svg, see k-core.md. The whole ride is ~5.1s.)

**THE FOCUS VEIL (`.fveil`)** is the shared organ: fixed frost (white .62 + backdrop blur 9, blur ALWAYS on so only background-color/transform animate), z **6400 — a new ladder rung** (above bay/lb, under flights 6500/bar 6600; the intro lifts the masthead to **6450** so the drawing logo rides above it). "The site gains focus from the top working its way down" IS the veil riding down (`translateY(103%)`) — the top edge is the focus line, top-first by construction. `clear()` must SNAP (transition:none + reflow) or the frost rides back UP through the page. **Bottom overscan −160px: a fixed element ends at the LAYOUT viewport and iOS paints page content in the band behind the floating toolbar** (sim-caught: raw cards under the opaque intro) — see the gotcha. **And overscan alone is NOT a guarantee** (his real phone painted past it, amended gotcha) — so the intro also empties the band at the source: `body.intro:not(.intro-shrink) .spine > :not(.masthead)` holds the page below the masthead at `visibility: hidden` until the shrink beat, where the page appearing behind the frost was already the design. visibility never display — the pose is measured off real layout.

**THE EMPTY STACK IS THE SAME OBJECT NOW** — his react: "the same mini card the stacks get instead of the blank flat square... with a 0 in the count slot." renderPicks' empty branch renders the fv3 template's own boardless shell (`.d7f.mtblank`: panel, throw, inframe, gap dots, empty stage) + `.hcount` **0** — the hand-drawn `.hpick.blank` retired, and the no-count-when-empty rule with it (a quiet 0, not the angry one). The shell's height is pinned at 348 = 33/.0948 (the caption zone it doesn't have) — re-derive if the mini scale moves. Pane-measured 22×33 with the 0 on the front corner. **And the DECODE MOVED UP** (his screenshot arrow): phone `.tick { margin-top: 26px }` against the desktop's sits-low clamp.

**The ceremony pumps are TIMEOUT-DRIVEN, never rAF** — the chase's own clock is setTimeout, and a frozen frame loop (background-tab load, the pane — whose rAF wedge is BACK, reconfirmed this session) must not stall a ceremony the timeouts are still advancing through. Same reasoning put the rider's transition kick on the reflow idiom (`void offsetWidth`) and the intro's pose on a 0-timeout.

**Verified:** sim (intro caught mid-draw — first stroke huge on bare white — and settled: logo seated, decode tight under it, bar pieces in, `• SHAPED •` resting dress; word tap → engulf + centred menu; ROUTED selected → landed decoded with the page in) + pane state walks (arm classes at first paint, blank shell 22×33 + count 0, intro self-runs and clears). Every clock in the ride is a knob; his phone is the venue (LAN URL, `avrg-v1` on :8124). The stray cap-specks the first build flagged are DEAD — every animated path is opacity-gated until its own begin (the amended gotcha; the clocks + gates live in the svg, k-core.md).

**REACT ROUNDS 1–2 (2026-08-16 late, ALL PUSHED — "works great!"):** the bar word centres on the SCREEN now (absolute nbar; record in header-bar.md), the picked row's fill resets, the stack minis EAGER their images (his catch — a routed board went blank in the hand-shaped section: the lazy 0×0-box chicken-and-egg, gotchas.md) with buildFly guarded the same way, and the veil's top-down reveal actually RIDES (it was snapping — the state-transition-override law, gotchas.md). The intro + ceremonies are otherwise as-built and approved; **the intro timing/size asks + the board-dimension decode both LANDED 2026-08-16 — the entry below.**

### THE ARM GETS ITS OWN BELT (2026-08-21) — his stuck page, fixed at the source

**The why is a real sighting, not a hypothetical: "i had this bug actually happen a few nights ago. page got stuck on loading, had to refresh."** His phone, sealed on a white page. **THE HAZARD:** the arm writes `body.intro` before the first paint and that class holds every scrap of content invisible behind an opaque veil — but *every hand that lowers it* (the driver at the main script's tail, and the driver's own 12s dead-man) lived in the MAIN script. Any boot-time death of that script — a bad `data.js` row, an API an old phone browser doesn't have, a truncated file on a flaky connection — took the intro's exits down with it and stranded the phone sealed **forever**. Measured on the pre-fix file with the main script killed at boot: at 15.5s the body was still `light intro`, `.fveil` visible at opacity 1, content at `visibility: hidden`. That is exactly what he saw.

**THE FIX is the pattern THE LANDING ARM already shipped one screen below** (k-core.md's THE LANDING): a **self-contained belt inside the arm script itself**, referencing nothing from the main script — the only failsafe that survives the main script dying. It strips `intro` + every `intro-*` class, drops `.fveil`'s `on`/`down` (that class is UNSCOPED — the intro classes alone would leave the frost sheet behind if the script died *after* the driver raised it), and clears the pose's inline transform on `.masthead .brand` (the shrink beat's job, so a death between pose and shrink can't park the mark huge over the page we just uncovered).

**Two deliberate differences from the landing's belt, both phone reasons:**

1. **IT IS GATED ON THE CLASS** (`if (!document.body.classList.contains("intro")) return`) — the same idiom the driver's own dead-man uses, and *that gate is how it coordinates with the driver*: a ride that cleared normally took `intro` off first thing in `clearIntro`, so the belt wakes to nothing and touches nothing. This matters more up here than downstairs because **`.fveil` is SHARED with the menu-select ceremony** (`fveil.up()`), which is reachable seconds after the intro clears — an ungated strip would tear the frost off a ride that was going fine. (Downstairs is safe ungated: `fveil.up()` is a phone-only path, so on desktop the landing is the veil's only user.)
2. **13000, one second BEHIND the driver's own 12s dead-man, on purpose.** Both clocks start at load, but the main script's starts later — it has the whole file to parse first — so a matching 12000 here would fire FIRST and win a race it should lose. The driver's `clearIntro` is the better exit (it snaps the veil and calls `startTick()`; the belt can do neither). Behind it, a stalled-but-alive script still gets the full clear and the belt stays a no-op; the belt is reserved for a script that is genuinely dead. Past every beat either way — the ride's own clear lands ~5.1s in.

**THE NORMAL BEATS DID NOT MOVE.** The arm's gate was re-laid as three early returns (same three questions, same order, mirroring the landing arm) purely so the belt could hang off the armed branch.

**Verified** on a private headless Chrome (private port, `/json/list` identity check) at 393×852, syntax one-liner clean before and after:

| run | result |
|---|---|
| normal phone load | pose+2378 shrink · +3377 tick/bar · +4126 veil down · +5077 clear (spec 2375/3375/4125/5075) — and **nothing after 5529ms**: no late class-strip at 13s, the belt disarmed |
| phone, main script killed at boot (`getElementById("dlist").innerHTML` nulled → the shipped `TypeError`, ~9400 lines before the driver) | belt fires at **13062ms** → body `light`, veil `visibility: hidden` / opacity 0, content `visible`. **His repro dying.** Same run on the pre-fix file: still sealed at 15.5s |
| desktop | timeline structurally identical to pre-fix — same class sequence, same landing beats, no `intro*` anything, nothing at 13s |
| reduced-motion phone (armed = never), incl. with the script also killed | **zero** class mutations across 15.5s — the belt cannot fire on an unarmed boot |
| collateral check: `.fveil.on` raised at 12.5s on a normal load | still `on` / visible at 14.0s — the gate does no damage to a veil that isn't the intro's |

## THE MOBILE DIMS DECODE + THE FLIP CHIP (2026-08-16)

**THE DIMS DECODE — "cards decode to board dims everywhere on the mobile site besides the collections page" (his rule).** The desktop hover's name→dims decode translated to touch; arrival is the phone's hover. Four seats, one asymmetry: (1) **collection pages** — `armDimsDecode(colpage)` (beside `landWants`) puts one IntersectionObserver on every `.scard .nmt` carrying `data-dims`; a card decodes ONCE as it scrolls into view and HOLDS the dims. (2) **#shop-all is EXEMPT** — it IS the COLLECTIONS page (`barWordFor`); `renderShopAll` deliberately never arms it (the comment in the code carries the rule). (3) **YOUR PICKS lands dims statically** in `renderLisst`'s M-gated dress pass — rest IS the landed state, no scramble replay, and the deal fly clones the text so rest == rest holds at every touchdown. (4) **the lb decodes at the landing** via `dockDress` (`#lb-fv3-nmt`), and `setFlip`'s flip-back restore is dock-keyed to dims (`(dockActive() && nmt.dataset.dims) || nmt.dataset.nm`) — desktop keeps the print name. The asymmetry: boards without a `DIMS_MM` row keep their name everywhere, same as the desktop hover.

**THE FLIP CHIP — the hidden face as the affordance.** `.fflipc` renders from the fv3 template wherever a canva pair exists (`m.bot`; originals/resale stay chipless until the reshoot): the word FLIP over a mini of **whichever face the card is NOT showing**, top-right, `.tspark ~ .fflipc` dropping below the builder's mark. **The show gate is three seats ≤940** — `.scard .d7f .fflipc`, `.bay-fly .d7f .fflipc`, `#lb-card.canflip .fflipc` — **and the `.bay-fly` term is load-bearing: a phone flight must land carrying the same chip the card wears, or every touchdown pops furniture in (rest == rest).** Desktop shows none of it and keeps the lb's Top/Bottom `.find` words (`#lb-card .find` stands down only ≤940). **Doors:** grid chip tap = in-place squash-swap (`flipStage` — a dedicated corner target, so the board keeps its one tap-to-inspect verb; the kick-retirement ruling stands); lb chip tap = the words' own `setFlip` door; picks chip = no handler, the tap bubbles to `flipCaret` (any tap flips there). The mini swaps with every flip and **warms the swap by existing** — the incoming face IS the img the chip was showing. Two loading laws: the `.fface` carries an explicit box (9.4827 × 25cqw, contain) so lazy fires on real cards while hidden chips stay 0×0 and never load; and `renderPicks`' eager pass is `img[loading='lazy']:not(.fface)` because **eager ignores `display:none`** — without the guard every stack mini downloads its board's other face.

## THE TAKEOVER CASE'S BODY IS BURIED (2026-08-16 — the v1 port's cleanup)

**The case died as a DOOR in react round 6 (2026-08-12), when the picon went back to a plain `#my-lisst` link; what the v1 port removed is the corpse it left lying in the file.** `openCase` and its family (`CASE_DRESS`, `caseOpen`, `caseCatch` and the catch-and-shut tail), the `mopen`/case-fade CSS costume, and the swipe-to-CLOSE handlers (`swc`) are all DELETED in `index.html` — grep `openCase` and the only hit left is a comment at `bayTick` explaining what it no longer has to undress. **The retired-history passages above STAY, because they are the record** (the one-body law, the cascade-order lesson, the iOS body-pin lock, the "a costume may only remove or resize" rule all came out of that build and all still govern) — but read every `openCase` / `caseCatch` / `dockAdd` / `fmtRef` mention above as HISTORY, not as live code. **WHAT REMAINS LIVE from the case's block:** the bay's phone PARK rules — at ≤940 the bay parks off-screen right on its resting `translateX` with the `!important`s that retire any stale inline pose the desktop driver left behind on a mid-open resize across the boundary, and the block still sits BELOW the desktop bay CSS for the cascade — plus the `#lb-card` touch rules; and the **SWIPE-TO-OPEN** (`swo`, committing mid-move at `SWC_TH`, its release ghost-click eaten by the shared `swcAte` box), which no longer slides a case in: on the phone the left-swipe commit DEPARTS TO YOUR PICKS through `leaveToPicks()`, the same door the bar's stack and the drawer's MSG bar use. The deck's phone body is the bar's picks stack + the YOUR PICKS page, and now the file says so too.

## The A/B pass — mobile items (2026-08-16, ported from the winning vault run)

- **THE BAND SEATS BELOW THE LB CARD + the card shrinks slightly** (84vw → 80vw on the base fv3 card rule — only ≤720 sees it; the ≥721 height guard overrides above). Full lb-side mechanics in lightbox.md.
- **THE DOORS BOUNCE**: pdInL/pdInR gained the overshoot IN the arrival keyframes (±2.4vw at 68%, −.8vw at 86%, opacity landed by 55% so the bounce plays at full ink), duration .75s — the .6s DELAY is the handle-flight pairing and did not move. No forwards fill: doors end transform-free so the :active press stays honest. The desktop half (envelope chips) is in contact-composer.md.

## THE CEREMONY SHIELD STRAND — the law (2026-08-16, found + root-fixed in the winning vault run)

Pre-existing bug, surfaced by walking a door nobody had: tapping the picks stack FROM AN OPEN LIGHTBOX stranded the page behind the z-6700 deal shield — closeLb's landing belt re-rendered Lisst one macrotask after route's own render, detaching the ceremony's DOM; the deal stood down silently with the shield up. TWO FIXES: closeLb's belt stands down under a live ceremony (`!document.querySelector(".picks-deal")`), and playPicksDeal's `bail()` drops its own shield on every stand-down return (newest-deal-guarded). **THE GENERAL LAW: a same-page re-render belt must stand down under a live ceremony, and a ceremony that stands down without a route must drop its own shield — route() only covers the routed exits.**

## THE PHONE IS SILENT — the sound ruling (2026-08-21, Dylan)

**The phone makes NO sound but video.** Not a tidy-up of what was already true: the sound gate (`sndOk`, in the kit under `landBeat`) **took three phone voices that were live**. Measured at a ≤940 viewport, gate removed vs. gate on:

| phone path | pre-gate | now |
|---|---|---|
| deck remove, the ✕ → `mobileExitFly`'s re-stack settle | click @+321ms | silent |
| shop/lisst chip uncheck → `renderBay`'s re-stack settle | click @+320ms | silent |
| lightbox open flip | click @+589ms | silent |
| mobile add (`pickCatch`) | already silent | silent |

The first was DOCUMENTED as deliberate before this — `mobileExitFly`'s comment read *"its settle click is the remove's one sound"* (now struck through in the file with the ruling beside it). So the phone dialect the earlier passes built by hand — the soft return landing silent, the slide exchange answering in fills, `pickCatch`'s silent landing + `buzz` — **is now the whole rule, enforced in one place**, and those three exceptions are gone.

**What still answers a phone gesture:** the motion itself, and `buzz()`. Haptics are deliberately OUTSIDE the gate and outside `SNDON` — a muted Android still taps back, and iOS gets nothing either way (no web haptics; do not re-add the switch-input hack). Verified after the gate: a mobile add still reaches `navigator.vibrate(12)`.

**The gate reads the 940 line LIVE at fire time** (`dockActive()`), never cached at boot — a window dragged across the boundary changes dialect with it, the same law `renderLisst`'s costume swap obeys.

Full kit, the per-voice cooldown, the one-gesture-one-voice law and the trigger inventory: [sounds.md](sounds.md).
