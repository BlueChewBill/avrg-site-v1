# Open threads, parked passes & site thesis
V1 close-out status, undecided directions, parked passes, the site thesis + progressive-disclosure law.
> Carved verbatim out of the old monolithic CLAUDE.md on 2026-08-12 (full backup on Dylan's Desktop + in git history). Wording is preserved exactly — section headers were added for navigation only. New decisions on this surface get appended HERE, not to CLAUDE.md. Cross-references like “see X below” may now point at a sibling doc in this folder — check CLAUDE.md's index.
> **Carried into the v1 repo 2026-08-16.** The threads below were written in the vault; the section directly under this note is the v1 update. Paths and lab names in the older entries point at the VAULT (`~/Projects/avrg-site`) — see gotchas.md's v1 section.

## The repo split landed — v1 (2026-08-16)

- **The hosting repo the 2026-07-26 PORT plan called for now exists.** Clean slate, no history: `index.html` (the working page, paths repointed) + `sources/` + `build_site.py` + `site/`. The vault keeps everything else — full history, every lab, `retired/`, the Canva source batches, the raw photography, the generators — exactly as Dylan wanted the record of the work preserved. Two commits here so far; the takeover-case machine and `cardInner`'s photo branch came out on arrival (already unreachable).
- **DESIGN EXPLORATION STILL HAPPENS IN THE VAULT.** Every lab and tuner (`drop-lab`, the card-lab family, the decode labs, the h-blackout lineage, `story-lab`) lives there and did not come across. The pattern is unchanged — collide variants in a lab, let him pick, then ship the result into `index.html` here. When a thread below says "the lab", that lab is in the vault, catalogued in the vault's `labs.md`.
- **Until cutover, changes are made in the VAULT and PORTED here** (the recipe + the baseline hash live in `.claude/HANDOFF.md`). The cutover checklist — verify the live URL, domain, CNAME + DNS, flip the vault private, then work here directly — is parked on Dylan's word.
- **The remaining threads, his order:** (1) slight **INTRO TWEAKS** — timing and size, a react loop against named knobs in the intro driver, not a rebuild; (2) the **BOARD-DIMENSION DECODE + decode timing across the site** — he said he'd consult an already-built lab for it, so let HIM name the lab. Then: ~~thread 1's desktop half (the landing around the drawn logo)~~ **CLOSED 2026-08-22 — see the closure note at the foot of this file**, thread 2 (home collection cards redesign), thread 3's desktop half (drop-row navigations still teleport above 941).
- **Parked, unchanged by the split:** waking the relay (`RELAY_ON`) + its tuning/feel pass and phone translation; the **ORIGINALS RETHINK** (Dylan: they're "the most interesting thing i make and also the hardest to sell... they need a lot of explaining and a deeper per board lightbox rather than the single card" — reshoot/BG removal + a richer per-board inspection; his call, down the road — **ADVANCED 2026-08-19: his ruling "they just need a section" — originals leave the shelf grammar for their own section/vibe/visual language. Direction variants live in the vault: `redesign/originals-lab.html` (spread/plates/workbench, on the real 2026-08-17 shoot) + the card end of it in `redesign/card-lab-2.html`; labs.md has both entries. Copy is stand-in — the data.js originals descriptions are still placeholder and a wordy direction costs writing. **RULED 2026-08-20, and the ruling grew past originals into THE PLATE SYSTEM (vault `redesign/plates-lab.html`): the HOME goes to vertical plates per collection — split by the normal line, line play, white simple feel, the carousels step back ("the cards have their moment the entire rest of the ride") — and the originals section takes the same plates horizontal per board: images bracket-framed in the card language (no card dims/chips), info between frames, A CLIP PER BOARD when available (first clip cut + seated in the lab). This reshapes thread 2 (home collection cards) — the home surface direction now runs through the plate lab. **PARKED 2026-08-20 on his react ("after some hemming and hawing, thats maybe down the line") — his sketch's down-the-line notes, saved for the reopen: keep the BG-REMOVED cutouts but from DIFFERENT photos he likes, boards fade in/out per section, maybe the seam dots FOLLOW them. The near-term move he chose instead LANDED the same day: THE ORIGINALS GO DEEP — photos + info in each originals lightbox (lightbox.md's 2026-08-20 entry; the rethink's "deeper per board lightbox" half is now SHIPPED in both repos, desktop). Still open on that thread: his per-board description writing (the seat exists, the copy doesn't), the clip-per-board idea (the #vlb machinery is the obvious door, unwired on purpose), and the og mobile view. **ADVANCED AGAIN 2026-08-20 evening (his sketch + react to the ogarr variants): "we might need a new LB entirely for the originals... the originals need the whole page in light box view. no flankers" — strip up top ("use the minis that exist here but tuned. bigger minis wider spread"), same lb card in the middle, the board's VIDEO to its left, the extended photo set + text to its right; plus NEW top/bottom photos shot at an angle (also to set og apart on the collections page). Built same evening as ogarr variant D, THE SPREAD — **and PICKED the same night ("this one looks solid"): the spread SHIPPED as the og lightbox (the ogarr lab retired, lightbox.md's OG SPREAD SHIPS entry) and went TUNABLE as the bench's originals-lb wing on his ask.** His asset ends stay open: per-board clips (one stand-in cut serves all six — resolve before the Saturday push or gate the seat), the angled shoot, the descriptions. **ADVANCED 2026-08-22 (launch day, the Figma round): THE ZONE RULING — after six built variants + his own collages on the kit's "Originals LB Mockup" page, the og LB direction settled: the CARD STAYS THE CONSTANT (centered, the add-to-deck/picks doorway), clip + short text keep the left seats, and right of the card is a DEFINED 480×640 zone (1440×900 reference) each original fills with its OWN hand-composed sheet — no template law, per-board composition is the point ("its okay if the stuff to the right of it differs from original to original"). He composes at 2× on the kit's "07 · OG Sections" page (six canvases, OG 02 seeded with the D cluster); per board his loop is bg-removed top+bottom cutouts for the card → find a clip → compose the section → Claude exports to site/img/og/ and wires. Content law from the round: spec-only captions, no color-salesman copy, NO ply descriptor. The stand-in clip knowingly shipped with the launch push; it rides until this lands.**); the few **iPad changes** (LOWEST priority, still unenumerated — he'll raise them); the sound pass and the browser-compat sweep below.

## V1 close-out (status + mobile ruling)

- **V1 CLOSE-OUT (Dylan, 2026-08-01): target was 2026-08-09 — the week before it didn't move and the date slipped; recommitted ~08-08: "this is it though 100 pct."** Ship-next, no new theatrical deadline: the list below IS the remaining scope. **MOBILE RULING (same recommit): "the mobile site probably wont get the love it deserves, just the love it needs"** — v1 mobile bar = FUNCTIONAL (navigable, inspections open/close, the sheet add path works, nothing broken on a real phone), the feel/polish pass is post-v1; read the mobile queue through that filter. First outside reactions landed (homies via the live Pages link): "way better than professional", unprompted "you could be a web developer... like you said resume" — the reads-like-someone's-thing test and the portfolio rung both confirmed by strangers-to-the-code.** **SECOND, PROFESSIONAL-GRADE VALIDATION 2026-08-16: Drew (does web/app dev for a living), shown the site at the park — "more impressed with each click. the site doesnt read as flashy when its static, by design. the design is working =)" (Dylan's read). That is the THESIS confirmed by a practitioner: quiet at rest, alive in the hand — the discovery ladder doing its job on someone who builds these for a living.** Scope serves the date. The list, his order: (1) **Category headers — DONE 2026-08-01, see the CATEGORY HEADER POSTS passage in the k-home-dual bullet.** (2) **The board counter — DONE 2026-08-01, see THE DECK HEADER passage.** 

## Card & ground direction, parked passes

- Card direction: pick the winning frame-card interaction variant (j-cards-white `#s-frame` / `#s-framev` / `#s-framev3`) and the winning decode directions (decode-slowmo.html), then port into the card page for real and design the tagline rotation with the same picks. Real dims data feeds the `PH_DIMS` map as boards get calipered.
- Ground direction: black vs white for the home page (`k-home-dual.html` switcher) — may become an actual light/dark mode toggle on the site rather than a one-or-the-other pick. **RULED 2026-08-20 (pre-launch): LIGHT IS THE DIRECTION** — recent passes all centred the light ground, and **dark mode COMES OUT for the Saturday launch** (a pre-launch task: retire the toggle + the dark ground's costume; light becomes the one ground). The three-scheme picker below (sun / cloudy-sun / moon) is the surviving future of this thread — "light, medium, dark" icons, no time before launch. Bench note: the removal touches charted ground/ink params (`page-chrome` wing, the dual-ground ring override) — chart it as it lands (the change-with-charting law's first real exercise). **LANDED same day: the toggle, the menu GROUND row and the ground machine are OUT, `<body>` ships `class="light"`, the pin follows the CARD/LB idiom (stale stored picks never read) — the record is k-core.md's 2026-08-20 section. Still open on this thread: the dark base costume's CSS is unreachable-not-removed (a post-launch proven-identical sweep), and the page `<title>` still says "dual ground" (flagged for Dylan — outward-facing).**
- **Final-touches pass (parked 2026-07-22 — deliberately last, don't build early):** (1) THREE ground variants as small sun / cloudy-sun / moon buttons — white-on-white, black with white cards, black with black cards (the k switcher's black/white gains a third state). (2) The real audio pass: sounds chosen per animation once motion is final — "the right sound effect really makes a good animation" — possibly a sound SET per ground variant (white = snappy Nintendo-Switch-like clicks, dark = softer Apple drag-and-drop cah-thunk family). The SND mute row shipped 2026-07-22 as the placeholder; today's synth voices are stand-ins. **2026-08-21: the JANITOR HALF LANDED** — one gate on the kit (phone fully silent except video, Dylan's ruling; per-voice cooldown; one-gesture-one-voice with the toss brought under the law; the lb-open click settle-synced), record + trigger inventory in [sounds.md](sounds.md). What stays parked HERE is the design half: chosen voices per animation once motion is final (the per-ground sets note above predates the dark retire — one ground now).
- **Browser-compat pass (parked — end-design tier, noted 2026-07-25): when the site nears its end design, check Safari against Chrome** — some behaviors built in Chrome don't map to Safari. The bar is "whatever works": take the simple change where it keeps a movement working, otherwise remove that element of the site movement in Safari — no heroic rewrites. Known territory: the iOS `max-height`/`aspect-ratio` landmine (already in Gotchas), plus the newer surfaces lean on preserve-3d flips, container-query cqw sizing, and `:has()` dot-flips — sweep those first.


## Home page logo + THE SITE THESIS + progressive disclosure

- Home page logo — **LANDED 2026-08-16: the single-stroke draw-in (his SVG, inlined; see k-core.md).** ~~The remaining half of thread 1 is the landing AROUND it (tagline/cards choreography, replay policy).~~ **THREAD 1 IS CLOSED — 2026-08-22, THE LANDING BAKED.** The desktop half is built, tuned on the bench and shipped as THE desktop arrival: the mark draws big in the upper third, comes home, the veil leaves first over an empty page, then the tick, the video from screen-left, the clamp photo, and the sections one at a time. The `?land=` switch is gone — it plays on every fresh desktop home boot (941+, motion allowed, bare home route). **Both open questions the note names are answered:** the choreography is built (and Dylan ruled the section cascade final — *"you guys nailed the section load in ask. no tweaks there. its solid"*), and REPLAY POLICY is **ceremony on fresh boot, plain in-place rewind draw on a mid-session home return** — the same shape the phone intro already had. His bake index (travel 1000 · poseY .420 · drawSpeed 2 · vidAt 300) and the full record live in k-core.md's THE LANDING section. What stays open on this surface is only the sound TODO at the draw's touchdown (sounds.md) and the bench wing's re-chart. The original note stands for tone: over-the-top is on-brand (AVRG = "average" / "a very real goober" — the joke is a maximal site for a humble name). The site's thesis: the cards and coherent animation are what keep a visitor past 5 seconds; the polish is the setup and "it's just a fingerboard, man" is the punchline — the intended discovery ladder is angry cart → a "MINE!" decode → actually reading the taglines, at which point *you know that he knows*. **PROGRESSIVE DISCLOSURE IS DELIBERATE (Dylan, 2026-07-27): "im kind of into the idea of parts of the site not making themselves obvious. its fine that you can navigate the whole thing and never realize you can chunk a card in or out of the drawer deck manually."** Undiscovered depth is a feature here, not a usability debt — the same thesis as the ladder above, applied to interaction. **THE RULE THAT KEEPS IT SAFE: every hidden path has a VISIBLE TWIN.** Drag-to-deck is a shortcut for what the +List chip and the lightbox button already do in plain sight, which is exactly why hiding it costs nothing. Apply this as a test to anything new: hidden *delight* is on-brand, hidden *necessity* is a bug. It bears directly on the undecided contact flow — contact is the one path that must never need discovering.
- Caliper specs + nicknames for the 19 Classics (descriptions live in build_site.py / per-board `.txt` files). **SHAPED dims are measured and READY (Dylan, 2026-07-22) — a type-in session: he dictates, they land in the dims map feeding the fv3 name→dims decode (k-home-dual `jmeta`/`b.dims` path), permanent home build_site.py → data.js per the inventory-ID plumbing thread below.**
- Video section for the riding clips in `videos/web/` (transcoded, waiting on a home).

## Misc queue

- More board sections toward the end of the shop scroll — the full inventory goes up eventually, even the boards that aren't first-class citizens ("you never know what people will be into"). Bolt-on point undecided: a simple button somewhere, or something you discover at the bottom of the scroll. Fits naturally with the one-scroll shop model.
- Open question: should `.colpage-inner h2` match the Satoshi Black used on `.band h2`?
- ~~Eventually: port the blackout draft into production `site/` (see path gotcha above).~~ **DONE 2026-08-16, in a shape the old plan didn't predict:** the draft did not move into the vault's `site/` — it became `index.html` of THIS repo, with `site/` demoted to the asset folder it loads. The path fixes the plan called for are the port repoints in gotchas.md's v1 section. `PORT.md` stays in the vault as the record of the thinking.

## ~~The pickfly pick~~ RULED the same evening (2026-08-22): A WON

The phone add-to-picks flight got its pass on his react ("almost just reads like a glitch"): two real bugs fixed (the off-screen landing + the spoiled reveal), a three-shape collide (`?pickfly=a|b|c`), his mid-collide overlap ruling (the flight rides OVER the bar — the 6650 rung), and the pick: **"ohh this one is actually the winner" — A, the tuned arc, baked as `PICK_TUNE`**; the param and the B/C machinery retired. Full record: mobile.md's THE PHONE ADD LEARNS TO LAND + the pick addendum. Bench charting dispatched with the bake (the pick-flight wing).

## THE SUNDAY DISPATCH (2026-08-28 night) — eleven tickets through the batch loop, and what it left open

The pre-posting little-fixes batch ran as a full dispatch (worktree agents, six-angle review, bench sync) — the per-surface records live in the owning docs (cards.md THE ID FLIP + mobile amendment, header-bar.md THE DESKTOP SEAT WEARS THE STACK, k-core.md THE CLOCK THAT WAITS FOR LOAD + the dark sweep landing, lightbox.md the ogpb fix, mobile.md the whole-card pinch, gotchas.md the Safari lessons). All pushed to avrg.cards. **Dylan's real-glass round confirmed the phone healed everywhere; Mac Safari's shadow blob survived → T7b escalation** (in flight at wrap time — rect-shaped shadow, appears only on hover-leave settle; suspect: Safari re-rasterizing the filter at the inline→stock swap, dropping the cutout's alpha).

**Open decisions the batch minted (his words: taste calls, next session):**
- **THE SUN FEEL (T3's disproof):** og on-card sun measures IDENTICAL to classic — the "shallow" feel was the old flank bug's overdrive (flanks pre-armed the grow + drove amplitudes on-card can't reach). Three directions, his pick: accept · faster entry arm (value retune, bench-charted) · the "warm ring" (panel gaps ARM the sun, still can't drive it). The `?sun=` collide is the offered build.
- **+List on-state border** never gets its accent (a light-retire specificity loser, pre-existing): `body.light .lb-list-btn` outranks `.lb-list-btn.on`'s currentColor. One-selector fix on his word; sits beside the parked `.lb-thumbs img.on` accent wrinkle (same class of loser, manifest-documented as his call).
- **The stack seat's optics:** review flagged it ~25px right of the old glyph's left edge with a smaller click target; the cartographer measured the seat centres mirroring perfectly at 1920. Judge on glass.
- **LINK-PREVIEW META TAGS — the one Sunday-critical unstarted item** (his "for later"): zero og:/twitter:/description tags; the posted link renders bare. Needs a 1200×630 share image (covercard poster or board shot + logo are the on-hand candidates).
- The deckmark cleanup chip (phone bar's dead mark costume) runs in its own session; merges whenever it lands. **CAVEAT minted 2026-08-29 (the add-chip redesign): #picon's .deckmark svg is now the ONLY copy of the deck-mark artwork in the file (the bar seat and the add chip both retired theirs) — if that cleanup deletes it, the artwork leaves the repo entirely; review its diff with that in mind.**

## THE WEEKEND QUEUE (Dylan, 2026-08-28) — three named jobs

Set at the end of the sun-boundary session (site at `655ebac`, pushed). His list, his words in quotes:

1. **"finish og lightbox view." — THE MAIN THING (his own weighting, follow-up message), and it comes with a second half: "another pass on the BG removal because the difference in quality between them and the rest of the collection is really offputting."** The og cards still ride the old Vision cuts while every other collection wears the Canva cutout batch — that quality gap is the off-putting part, so the weekend og job is BOTH the remaining six sheets (the proven loop: compose on the kit's "07 · OG Sections" page → MCP-export the selected frame at its own 2× → seat in `site/img/og/` → one `OG_SHEETS` line) AND redone bg-removed cutouts for the og cards themselves. Also still open on the og thread: per-board clips (the stand-in cut still serves all seven), the left "short text" seat, og mobile.
2. **"fresh board batch upload — i have another 15-25 boards that need to go on the site probably."** The biggest inventory change since launch. THE LAW BEFORE TOUCHING IT: `sources/` ordering IS board identity — ids are positional, the card-art map is keyed to them, new photos MUST sort LAST (gotchas.md carries the full law). Plan the naming before the first file lands. Also touches: `build_site.py` re-run, card art for new boards (the canva batch lives in the vault), `DIMS_MM` entries if he measures, per-collection placement.
3. **"quality identifiers. (We havent touched this idea in a minute, but its needed.)"** RECOVERED same message — he "probably just called them something different": it is **THE SPARK**, the ✱ builder's mark already in the card's upper-right corner (`JSPARK` map keyed by padded ref → `.spark`/`.tspark`, accent orange, tooltip "Builder's mark — this one came out perfect"). The relic state: the MECHANISM shipped long ago but `JSPARK` holds exactly two hand-seeded DEMO entries (CL 07, OG 01) beside the other demo-state maps (JPEND/JGONE) — the real system was never built. The weekend job is designing that system with him: (a) real curation — which boards actually earn the mark, presumably re-judged against the incoming 15-25 board batch; (b) whether one tier is enough (his word is identifierS — grades/levels are an open question, not an assumption); (c) surfacing — today the meaning lives in a title-attr tooltip, which is desktop-hover-only and INVISIBLE on touch, and nothing explains the ✱ anywhere on the page; the LB is the natural place for the mark to say its name. Existing plumbing to build on: `jmeta.spark`, `sparkHtml`/corner seat, `.tspark ~ .find/.fflipc` displacement rules, `.scard.indeck .tspark` hide. **ADVANCED same message (2026-08-28): the demo seeds CAME OUT on his word (`JSPARK` ships empty; the mechanism stays live) and the tier question got its first real shape — at least TWO marks: an EXTRAS mark ("a good sized stack of boards that id just as happily give out in any order as an extra that some people might want over the ones i think are better — might as well surface them but with some kind of mark") and a PREMIUM-FINISH mark (for "the ones that are obsurdly well rounded / countersunk / finished"). How many tags total + their language: "details that can be hashed out in a dm" — a conversation with him opens the build, don't design it solo.** **BUILT, FIRST HALF (2026-08-29): the tier mechanism landed (JSPARK values are tiers; cards.md THE SPARK GROWS TIERS) and Dylan seeded tier 2 — the highest maker's mark — on HS 06/07/16/17/18. His collide verdict landed same night: highest = the filled ✦ (tier 3), and the black-chip ✱ became a live second mark (tier 2: HS 08/10/14). Tag language + touch surfacing still open; the accent ✱ (tier 1) is unheld.**
